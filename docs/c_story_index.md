# Story Index

**Last issued:** `00432`

**Note:** IDs 00175–00187 were previously allocated to Work Items feature area. PageBuilder stories start at 00188 (IDs 00188–00201, 14 stories total across 5 phases). IDs 00292–00309 allocated to PLA-0007 (data-driven RBAC). IDs 00310–00322 allocated to PLA-0006 Topology canvas-management UX (federated authoring surface). IDs 00323–00331 allocated to PLA-0008 Page Help Library (rich-content help docs per addressable, 9 stories). IDs 00332–00337 allocated to PLA-0006 Vector-standard refactor sweep (extract inline components, catalog CSS, migrate off @xyflow/react onto `<DiagramCanvas>`, sole-writer compliance, addressables wrap, lint:no-xyflow rule, 6 stories). IDs 00338–00345 allocated to PLA-0009 self-healing dev pipeline (air file-watcher, bootstatus tolerant boot, /api/status/pipeline endpoint, EnvBadge degraded-state + 3s prod hold-to-confirm, launcher observer mode; backfilled and accepted 2026-05-04). IDs 00346–00372 allocated to PLA-0010 R035 codebase-audit remediation (8 S5 + 11 S4 + 5 S3 + 3 S2 stories: WS auth gating, env-switch auth, clientIP whitelist, .gitignore + dead-file/dead-package cleanup, WS InsecureSkipVerify, per-route rate limits, cookie unify, /healthz redact, FRONTEND_ORIGIN required, audit-log gaps, DOMPurify, session revoke on role change, per-user rate-limit composition, migration reorg, branch/backup prune; 27 stories, 2026-05-04). IDs 00373–00382 allocated to PLA-0006 Workspaces tier (tenant→workspaces→org_nodes hierarchy: workspaces + workspace_roles tables, NOT NULL FK backfill, 5 RBAC permission codes, workspaces.Service sole-writer + REST surface, clamp predicate workspace scoping, switcher dropdown, Manage Workspaces panel, archived-workspace restore, docs + Default-on-signup; 10 stories, 2026-05-04). IDs 00383–00396 allocated to PLA-0011 R036 baseline-audit remediation (14 stories across 3 waves: portfoliomodels test fix, schema_migrations DOWN-row cleanup, COMMIT append on 4 migrations, 027-gap doc + library/down/ folder, 5 unused Go fns delete, OLLAMA env audit, marked patch bump, untracked migration commit, 3 fetch→api migrations, inline-style sweep, test DB reset + make target, 233-index tech-debt entry; 2026-05-05). IDs 00397–00401 allocated to PLA-0012 Page Summary Header (full-span stat strip primitive composing `<Panel>`, .page-summary catalog CSS, Work Items page first adopter with tree-state cells + hoisted search, page_help seed for `_panel.page_summary`, Vitest contract tests; 5 stories, 2026-05-05). IDs 00402–00412 allocated to PLA-0013 Dev-UI Primitives & Migration (dev-ui catalog stylesheet under dev/styles/, c_c_dev_ui_primitives.md doc + HARD RULE, lint:dev-css CI gate, skill/command rewires, then panel migrations Research → Reports → Plans → Retros → Page Help → Shortcuts → Library; evicts ui-retro__* and dev-shortcuts-th--* from app/globals.css; 11 stories, 2026-05-05). ID 00413 allocated to PLA-0007 catalogue-expansion blocker for 00301 (adds library.releases.view, portfolio.model.edit, work_items.settings.edit, portfolio_settings.view, portfolio_items.view permission codes via migration 104; 2026-05-05). IDs 00414–00415 allocated to PLA-0013 catalog-extension follow-up (extend dui-form__* primitives for editor composition, then migrate DevPageHelpPanel + evict dev-help-editor__* from app/globals.css; unblocks 00410; 2026-05-05). IDs 00416–00420 allocated to PLA-0014 Tab reorder primitive (dnd-kit convention doc, user_tab_order migration, Go service + REST surface, SecondaryNavigation reorder mode with numbered position chips + instant save, wire pageId into callers + lint rule; 5 stories, 2026-05-05). IDs 00421–00427 allocated to PLA-0015 `<Table>` component (component contract doc, scaffold + harness, expander + panel, inline-edit cell, pagination + search + dropdown filters, canary Permissions migration, full migration of 8 remaining flat call sites + lint:no-raw-table + strip deprecated .table* from globals.css; 7 stories, 2026-05-05). IDs 00428–00432 allocated to PLA-0016 Docs site as a fourth Launcher component (ServiceKind.docs wiring, DocsManager.swift with npm-start supervision + JSONL log + adopt-mode, Orchestrator + DashboardView fourth-row integration, HealthProbe + PortCheckView :3000 entry, c_launcher.md three→four update + smoke test; 5 stories, 2026-05-05).

## Active phase

`PH-0005`

## Mandatory labels (every card)

`AIGEN` · `PH-NNNN` · `FE-AAAANNNN` · `EST-FN` · `RISK-LOW/MED/HIGH`

## Format

`NNNNN — Title` (5-digit zero-padded ID, em dash, title)

## Deletion log

_(none)_
