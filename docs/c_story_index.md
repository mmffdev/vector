# Story Index

**Last issued:** `00382`

**Note:** IDs 00175–00187 were previously allocated to Work Items feature area. PageBuilder stories start at 00188 (IDs 00188–00201, 14 stories total across 5 phases). IDs 00292–00309 allocated to PLA-0007 (data-driven RBAC). IDs 00310–00322 allocated to PLA-0006 Topology canvas-management UX (federated authoring surface). IDs 00323–00331 allocated to PLA-0008 Page Help Library (rich-content help docs per addressable, 9 stories). IDs 00332–00337 allocated to PLA-0006 Vector-standard refactor sweep (extract inline components, catalog CSS, migrate off @xyflow/react onto `<DiagramCanvas>`, sole-writer compliance, addressables wrap, lint:no-xyflow rule, 6 stories). IDs 00338–00345 allocated to PLA-0009 self-healing dev pipeline (air file-watcher, bootstatus tolerant boot, /api/status/pipeline endpoint, EnvBadge degraded-state + 3s prod hold-to-confirm, launcher observer mode; backfilled and accepted 2026-05-04). IDs 00346–00372 allocated to PLA-0010 R035 codebase-audit remediation (8 S5 + 11 S4 + 5 S3 + 3 S2 stories: WS auth gating, env-switch auth, clientIP whitelist, .gitignore + dead-file/dead-package cleanup, WS InsecureSkipVerify, per-route rate limits, cookie unify, /healthz redact, FRONTEND_ORIGIN required, audit-log gaps, DOMPurify, session revoke on role change, per-user rate-limit composition, migration reorg, branch/backup prune; 27 stories, 2026-05-04). IDs 00373–00382 allocated to PLA-0006 Workspaces tier (tenant→workspaces→org_nodes hierarchy: workspaces + workspace_roles tables, NOT NULL FK backfill, 5 RBAC permission codes, workspaces.Service sole-writer + REST surface, clamp predicate workspace scoping, switcher dropdown, Manage Workspaces panel, archived-workspace restore, docs + Default-on-signup; 10 stories, 2026-05-04).

## Active phase

`PH-0005`

## Mandatory labels (every card)

`AIGEN` · `PH-NNNN` · `FE-AAAANNNN` · `EST-FN` · `RISK-LOW/MED/HIGH`

## Format

`NNNNN — Title` (5-digit zero-padded ID, em dash, title)

## Deletion log

_(none)_
