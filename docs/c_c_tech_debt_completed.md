# Technical debt — completed register

Resolved tech-debt entries, relocated from [`c_tech_debt.md`](c_tech_debt.md) to keep the active register lean.

Each row was marked `~~strikethrough~~` with a "Resolved YYYY-MM-DD" pay-down note before being moved here. This is an archive — nothing here is actionable. New completions land in the active register (struck through for one cycle) and are swept here periodically.

## Completed register

| ID | Added | Severity | Area | Debt | Trigger (what makes it bite) | Cap in place | Pay-down |
|---|---|---|---|---|---|---|---|
| ~~TD-LIB-006~~ | ~~2026-04-25~~ | ~~S3~~ | ~~Frontend / library catalogue~~ | `/portfolio-model` hardcodes the seeded MMFF family id (`00000000-0000-0000-0000-00000000a000`) because no list endpoint exists. Fine while there's exactly one bundle, but adding a second system bundle (SAFe / Jira / Rally / Kanban — Phase 6) means the page will only ever show MMFF. | Second model bundle ships from MMFF (Phase 6 first system-model release). At that point the page silently hides the new bundle. | Constant + comment at top of `app/(user)/portfolio-model/page.tsx` names the limitation; this register entry. | Add `GET /api/portfolio-models` list endpoint (paginated, filtered by `scope` + visibility), then replace the constant with a fetch + selector. UI-only swap once the endpoint exists. — **Resolved (verified 2026-06-07 TD-review).** `GET /api/portfolio-models` list endpoint exists (`backend/internal/portfoliomodels/list.go`, mounted `main.go:2219` with PortfolioList perm); frontend `WizardModelCardList` consumes `PORTFOLIO_MODELS_LIST_PATH` (`/portfolio-models`). Hardcoded `…a000` MMFF id no longer present anywhere. |
| ~~TD-NAV-CASCADE-BROKEN~~ | ~~2026-05-26~~ | ~~S2~~ | ~~DB / trigger functions~~ | `fn_users_roles_pages_cascade_nav_prefs()` references pre-Pillar-1 bare column names (`pages.id`, `pages.key_enum`, `users.id`, `users.role_id`, `users.subscription_id`) that no longer exist after the column-prefix sweep (commits `f6b3f3d2` through `bd1d7c52`). The function copied AS-IS from mmff_vector → vector_artefacts in Pillar 2 migration 117 to preserve mmff_v's broken behaviour exactly (no regression). Fires on DELETE of users_roles_pages rows. Currently dormant — the cascade triggers only on hard DELETE which doesn't happen in normal app flow (rows are soft-archived via `archived_at` instead). | (a) Hard DELETE on `users_roles_pages` ever fires (admin tooling, manual SQL cleanup, future migration). (b) A new admin route that DELETEs grants instead of soft-archiving them. The error will be `record "old" has no field "key_enum"` and will surface as a 500 to whoever triggered the cascade. | This register entry. The function is copied AS-IS and documented inline at the head of migration 117. The trigger fires on a code path that doesn't exist in production routes today. | Rewrite the function body to use the prefixed column names: `OLD.users_roles_pages_id_page` → `pages.pages_id` JOIN to get `pages.pages_key_enum`; `OLD.users_roles_pages_id_role` to find affected users via `users.users_id_role`; `NEW.users_id` / `users_id_subscription` for the cleanup INSERT. Single migration in vector_artefacts. Estimated 1-2 hours including a test that exercises the DELETE cascade. — **Resolved 2026-05-26 (mig 142, verified 2026-06-07).** `142_nav_cascade_triggers_after_split.sql` rewrote `fn_users_roles_pages_cascade_nav_prefs` to prefixed column names (`pages_id`, `pages_key_enum`, `users_id`, …); the bare-name bug is gone. Mig 142 header explicitly cites this TD. |
| ~~TD-SEC-DPOP-STALE-KEY~~ | ~~2026-05-31~~ | ~~S2~~ | ~~Auth / session security~~ | **Resolved at source 2026-05-31** (regression-tested fix; see Pay-down). Root cause of "logged out / page loads blank whenever I switch browser tabs and come back." The DPoP keypair store (`app/lib/dpopStore.ts`, IndexedDB `vector-dpop`) is keyed by `userId` and was only ever deleted one-at-a-time (logout, reparent). A session ending WITHOUT a clean logout — tab close, crash, backend-forced revoke, or the put-succeeds-delete-fails branch in `reparentKeyRecord` — orphaned its keypair in IDB. `ensureAnyActiveKeypair` (bootstrap + tab-refocus path) then picked an **arbitrary** real-user record via `Array.find`; when it picked an orphan whose jkt was never bound to the current session, `/auth/refresh` failed the backend's `cnf.jkt` binding check (`backend/internal/auth/service.go:709`), revoked the entire session family, and emitted `auth.refresh_dpop_binding_violation` → frontend `hardLogout` → logout / blank-content-on-refocus. **Diagnosed from the live audit log:** binding violations firing daily, the SAME stale `incoming_jkt` (`3lsPmZ7VKCh…`) against an ever-changing `bound_jkt` — proof of one orphaned key being reused across sessions. | Residual A — bootstrap selection is **newest-wins by `createdAt`, not jkt-verified** against the session's `cnf.jkt`. Correct in practice (the freshly-bound key is always newest) but it's a heuristic, not a proof. Bites if a future flow could persist a newer-but-unbound key before refresh resolves. Residual B — the binding-violation is a **terminal revoke-all** even on benign client-side key drift; a single stale-key bootstrap nukes every session for that user across all devices. | **Fixed** (closes the accumulation at the source): (1) `pruneKeysExcept(keepUserId)` added to `dpopStore.ts` + `pruneStaleKeys` wrapper in `dpop.ts` — `AuthContext.applyLogin` prunes all-but-current after every login/refresh once identity is known, so the store converges to exactly one record per browser. (2) `ensureAnyActiveKeypair` now selects the most-recent real-user key by `createdAt` instead of `Array.find`, so an orphan can never beat the freshly-bound key even before the next prune. Regression tests: `app/lib/__tests__/dpop.test.ts` § "stale-key selection (TD-SEC-DPOP-STALE-KEY)" (3 tests; full file 18/18 green). | Residual A: on bootstrap, after `/auth/refresh` returns the access token, decode its `cnf.jkt` and assert the active key's jkt matches; on mismatch, hard-reset the keypair + re-mint rather than trusting newest-wins. Residual B: consider downgrading the refresh-time binding mismatch from revoke-ALL-sessions to revoke-only-the-offending-session-family + single-device re-auth, so one stale browser key can't sign every device out — weigh against the stolen-refresh-token threat model (TD-SEC-DPOP-BINDING) before changing. Trigger to pay down: any further report of spurious logout, OR before a multi-device GA where Residual B's blast radius matters. **Update 2026-06-04:** the *live-tab* stale-in-memory-key path (a sibling tab prunes the IDB key while another live tab still holds it in memory) is now closed by the multi-tab auth coordinator — `AuthContext.applyLogin` broadcasts `dpop-key-changed` over the `vector-auth` BroadcastChannel and every live tab reloads its key from IDB via `ensureAnyActiveKeypair` (see `app/lib/authChannel.ts` + spec `docs/superpowers/specs/2026-06-04-multi-tab-auth-coordination-design.md`). **Residual A RESOLVED 2026-06-04** (the proper fix, after the bug recurred as "page refresh logs me out" — diagnosed live: `audit_logs.auth.refresh_dpop_binding_violation` with `bound_jkt != incoming_jkt`): key selection is now **JKT-verified, not newest-wins**. The session's `cnf.jkt` is persisted to `localStorage` (`vector-dpop-bound-jkt`, `app/lib/authChannel.ts`) on every login/refresh; `ensureAnyActiveKeypair(preferJKT)` selects the IDB key whose RFC 7638 thumbprint equals it (`app/lib/dpop.ts`), and returns `"unrecoverable"` (→ clean re-auth, no wrong-key refresh) when no key matches. The `dpop-key-changed` cross-tab handler is hardened to pass the bound JKT too. Regression guards: `dpop.test.ts` JKT-selection tests + a dev-only `DPOP_JKT_MISMATCH` console assert before every refresh. **Monitoring:** treat any `auth.refresh_dpop_binding_violation` in `audit_logs` as an alert — query: `SELECT count(*), max(audit_logs_created_at) FROM audit_logs WHERE audit_logs_action='auth.refresh_dpop_binding_violation' AND audit_logs_created_at > now() - interval '1 day';` (vector_artefacts/vaPool). Non-zero after 2026-06-04 = Residual A regressed. **Residual B (terminal revoke-all on benign drift) unchanged** — still open, but its blast radius is now far smaller since legitimate refreshes no longer mis-sign. — **Resolved at source 2026-05-31 (verified 2026-06-07).** `pruneKeysExcept(keepUserId)` at `app/lib/dpopStore.ts:165` (single atomic IDB tx) + regression suite `app/lib/__tests__/dpop.test.ts:240` ('dpop: stale-key selection'). Skeptic confirmed pruning + integration on main. |
| ~~TD-FIELD-LIBRARY-NAME-COLUMN-DRIFT~~ | ~~2026-05-21~~ | ~~S2~~ | ~~Backend / SQL constants~~ | `sqlListFieldValuesForArtefact` in [`backend/internal/artefactitems/sql.go:619`](../backend/internal/artefactitems/sql.go#L619) selects `fl.name, fl.label, fl.field_type, fl.options_json` from `artefacts_fields_library`. The live `dev` schema's column is `field_name`, not `name` (migration [006_field_library.sql:24](../db/vector_artefacts/schema/006_field_library.sql#L24) — never renamed). Any call to `GET /api/v2/work-items/{id}/field-values` against the live dev DB returns `column "name" does not exist (SQLSTATE 42703)`. Discovered while wiring the rule-engine hook for custom-field writes (my new `sqlSelectFieldLibraryNameAndType` initially had the same bug; corrected to `field_name` with a NOTE comment pointing here). | Anyone opens the work-item detail panel's custom-fields section and clicks anything that hits the GET — would return 500. Today the panel only PUTs (via `UpsertFieldValues`) and reads the field_values back from the PUT response, NOT from this GET, which is why nobody's noticed. Trigger: a new feature reads field-values via the GET endpoint, OR an automated test exercises `ListFieldValues`. | This register entry. The wider hook-wiring PR ships with the correct column name; the broken constant ships alongside, isolated. | Two-line fix: change `fl.name` → `fl.field_name` in both the SELECT and ORDER BY clauses of `sqlListFieldValuesForArtefact`. Add a `GET /work-items/{id}/field-values` integration test (~10 lines) to lock the contract. ~10 min total. — **Resolved (verified 2026-06-07).** `sqlListFieldValuesForArtefact` (`backend/internal/artefactitems/sql.go:1030-1044`) selects/orders by the live `fl.artefacts_fields_library_field_name`, matching mig 006. NOTE: the inline comment at `sql.go:1057-1060` still claims `fl.name` — stale comment, harmless; clean up when next in the file. |
| ~~TD-SCOPE-GHOST-LS-READS~~ | ~~2026-05-24~~ | ~~S1~~ | ~~Frontend / wire-layer scope clamp~~ | `app/lib/api.ts::withForwardedMeg` used to fall back to `localStorage.getItem("vector.scope.activeNodeId")` when the page URL had no `?meg=`. The writer for that key was deleted with `ScopeContext` in PLA062 S22 (commit `d14bcc70`), but the reader survived. So the localStorage value was a ghost — only ever containing stale residue from pre-S22 sessions. When `ObjectTreeV2` mounted on any page without `?meg=` in the URL (e.g. `/value-sprint`, `/value-status`, or `/dashboard` post-fresh-login), the function injected that stale UUID into wire requests under `(work-items\|portfolio-items)/*`. Backend handler `artefactitems.Facets` read the `?meg=` param, called `topology.CanReadScope` against the stale node, found no active grant for the current user, returned `ErrScopeForbidden` → 403 `scope_read_denied`. ObjectTreeV2 lost its facets + flow-states + types catalogue and rendered as an empty grid. The bug was invisible on `/work-items` itself because SentinelProvider mirrors `sentinel_focus_node` into `?meg=` on that route, so the URL value won over localStorage. Compounded by [TD-SENT-URL-MIRROR-PATHNAME] below: the URL-mirror effect didn't re-fire on client-side navigation, so `?meg=` was bare on every fresh route. | Anyone writes new code that reads a retired ScopeContext localStorage key. The retired key list (extracted from pre-S22 git history): `vector.scope.activeNodeId`, `vector.scope.direction`, `vector.scope.activeWorkspaceId`, `vector.scope.tenantId`. | **Fix shipped 2026-05-24** — `app/lib/api.ts:153` no longer reads localStorage; URL is sole source. SentinelProvider URL-mirror effect (`app/sentinel/SentinelProvider.tsx:357`) now depends on `pathname` and re-stamps `?meg=` after every nav. Regression test pinned in `app/sentinel/__tests__/sentinel_provider.test.tsx` Case 20. | Add a lint rule banning `localStorage.{get,set,remove}Item("vector.scope.*")` outside an exempt registry (start the registry empty — no production code should read these). Pattern matches the existing `lint:no-direct-workspace-id` + `lint:no-old-context-imports` registry-with-allowlist shape (see `dev/scripts/lint_no_old_context_imports.py`). Single Python lint script + a small registry JSON; ~30 min. — **Resolved (bleeding bug closed; verified 2026-06-07).** Zero reads of `localStorage 'vector.scope.*'` remain (grep clean; only an explanatory comment at `api.ts:174-178`); `withForwardedMeg` no longer falls back to LS. NOTE: the lint rule to ban future LS-scope reads is still pending — track as a fresh S3 if you want the guardrail. |
| ~~TD-SENT-URL-MIRROR-PATHNAME~~ | ~~2026-05-24~~ | ~~S2~~ | ~~Frontend / Sentinel URL contract~~ | The Sentinel URL-mirror effect (`app/sentinel/SentinelProvider.tsx:357`) writes `?meg=<focus>` to the address bar via `replaceState` after boot, but originally depended only on internal state (`state.loading`, `state.focus_override`, `state.url_focus`, `state.user`, `state.tenant_root`) — not on `pathname`. Next.js client-side `router.push("/somewhere")` drops query params; the effect didn't re-fire on the new route, so `?meg=` was missing on every navigation target until Sentinel state next changed (often never). The downstream cost — combined with TD-SCOPE-GHOST-LS-READS above — was wire-layer scope clamps degrading to either nothing (post-fix) or stale ghost values (pre-fix). The "bookmarkable scope" promise of `?meg=` was effectively broken: refreshing a page mid-session preserved scope, but clicking a nav link silently dropped it. | Any new client-side nav lands on a page where `?meg=` should appear in the URL and doesn't. Hard refresh would then show the page differently — the "share-this-URL works" guarantee falls over. | **Fix shipped 2026-05-24** — added `usePathname()` to SentinelProvider; `pathname` included in the URL-mirror effect dep array. Regression test pinned in `app/sentinel/__tests__/sentinel_provider.test.tsx` Case 20. | Add a Playwright spec under `dev/tests/playwright/` that logs in, navigates through 3+ pages (Dashboard → Value Status → Work Items), and asserts `?meg=` is present in the URL at each landing. Catches regressions from any future refactor that drops the `pathname` dep or moves URL-mirror logic out of SentinelProvider. ~30 min once a Playwright auth fixture exists for `claude_3_test@mmffdev.com`. — **Resolved 2026-05-24 (verified 2026-06-07).** `usePathname()` imported (`SentinelProvider.tsx:39`), `pathname` in the URL-mirror effect dep array (`:472`); effect now re-fires on route change. NOTE: a Playwright regression for `?meg=` persistence across nav is still pending. |
| ~~TD-SENT-CACHE-EXPAND-COVERAGE~~ | ~~2026-05-28~~ | ~~RESOLVED~~ | ~~Backend / sentinel cache~~ | **CLOSED 2026-05-28** (cycle 3, commits `2761d762` + `9f5f4038`). Final scope of the cache rollout: (a) sentinel middleware — `FocusWorkspace`, `GrantOnNode`, `ResolveSubtree` cached; (b) `/sentinel/boot` sub-reads — `auth.Service.LoadRoleAndPermissions` (3 sequential SQL → 1 cache GET) and `topology.Service.ListMyGrants` (+ gadmin variant). Invalidation: existing `subtreeCacheInvalidator` adapter extended to also wipe `topology:mygrants:{tenant}:*` on every topology structure write (16 callsites already in place — zero new code in topology); new `permCacheInvalidator` adapter wires 4 new callsites in `roles.Service` (AssignPermissions / RevokePermissions / Archive — namespace-wide wipe) and `users.Service.Update` on role change (per-user surgical DEL). `HasActiveRole` remains UNCACHED (~30ms doesn't justify the invalidation surface). Backend curl warm /sentinel/boot dropped from 0.309s (cycle 2 baseline) → 0.222s (cycle 3 post-commit, 10-run median); cold→warm shows ~350ms saved per boot (0.544s → 0.18s with cache populated). | N/A — closed. Re-open if `/value-sprint` warm regresses above 700ms or a perm-revoke leaks a stale YES (would be caught by RBAC test surface; lint:no-direct-workspace-id covers the read side). | This register entry. Cycle 3 audit found 4 write callsites total — well below cycle 2's >10 stop-gate. | N/A. — **Closed 2026-05-28 (cycle 3, commits `2761d762`+`9f5f4038`; verified 2026-06-07).** Cache present on `FocusWorkspace`/`GrantOnNode`/`ResolveSubtree` + boot sub-reads (`LoadRoleAndPermissions`, `ListMyGrants`); 4 invalidation callsites wired. Skeptic confirmed all cache + invalidation sites. Moved to completed register. |
