# `<npmrun>` — start the Next.js dev server

Starts the Next dev server on `http://localhost:3000`.

```bash
cd /Users/rick/Documents/MMFFDev-Projects/MMFFDev\ -\ PM && npm run dev
```

Prompt for confirmation before running — dev servers are long-running and may collide with an existing process on port 3000.
