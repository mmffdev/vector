# Story Labels — Setup (archived)

This document captured the one-time setup for the kanban tag catalogue (AIGEN / EST-F# / RISK-* / FE-* / PH-*) on the project's Planka board. Planka was decommissioned 2026-05-15; tags are now plain strings in each `work_item_backlog[].tags` array inside `dev/plans/PLA-NNNN.json` and need no external creation step.

For the current catalogue see [`c_story_labels.md`](c_story_labels.md). For the workflow see the `<stories>` skill at [`.claude/skills/stories/SKILL.md`](../.claude/skills/stories/SKILL.md).
