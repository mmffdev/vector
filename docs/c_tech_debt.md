# Technical debt — standing rule and register

This is a **standing instruction** for Claude Code in this repo. Read it on any task that:

- adds a new abstraction, table, polymorphic relationship, or cross-cutting pattern
- touches code already flagged in the register below
- does a refactor, schema change, or architectural decision
- delivers a workaround, "TODO", or anything labelled "we'll fix this later"

If none of those apply, you do not need to load this doc.

## The standing rule

Every task **maintains the debt register**. That means three obligations, in order:

1. **Identify** — when you spot debt during a task (smell, workaround, missing cleanup, latent foot-gun, brittle invariant), name it. Don't keep it in your head.
2. **Measure** — give it a severity (S1/S2/S3) and a trigger (the event that turns latent debt into pain). Severity and trigger are the only fields that matter; everything else is colour.
3. **Recommend** — record the cheapest action that caps the debt (doc + canary + lint), and the action that pays it down (refactor + migration). Caps go in immediately; pay-downs wait for the trigger.

The rule is **not** "fix all debt now". The rule is **debt is never invisible**. Visible debt is bounded; invisible debt compounds.

## Severity scale

| Severity | Meaning | Response |
|---|---|---|
| **S1 — bleeding** | Causing incidents now, or will on the next deploy/handler/feature | Fix in the same PR. No exceptions. |
| **S2 — latent** | Dormant but the trigger is foreseeable (next archive handler, second writer, first prod tenant, etc.) | Cap immediately (doc + canary + comment at the trigger site). Schedule pay-down before trigger fires. |
| **S3 — structural** | Slow tax on every future change in this area | Cap with the register entry. Pay down when the area is touched for another reason, or when a second symptom appears. |

## What "cap" means

A cap is a cheap, mechanical safeguard that makes the debt visible the moment it bites:

- **Doc** — a leaf under `docs/c_*.md` describing the rule, the why, and the trigger
- **Canary** — a test that fails when the latent condition becomes real (orphan rows, missing handler, drifted schema, etc.)
- **Lint / comment / assertion** — at the most likely future-edit site, a comment or runtime check that surfaces the rule when someone touches it
- **Register entry** — the row below, so future-Claude finds it without re-deriving

Caps cost hours, not days. If the cap costs days, it's not a cap — it's the pay-down, and you're doing it for the wrong reason.

## Process per task

When you finish a task, before reporting back:

1. **Did this task introduce debt?** If yes, add an entry below.
2. **Did this task touch debt already in the register?** If yes, either pay it down (and remove the entry) or update the entry with what you learned.
3. **Did the trigger fire on any S2 entry?** If yes, raise it to S1 and address before merging.
4. **Recommend** — if any S2/S3 entry has crossed its trigger condition during this work, surface it to the user in your final message: "While doing X, I noticed entry N's trigger has fired — recommend pay-down now."

Do not silently fix every S3 you see — most are correctly deferred. Do surface them when you're already in the area.

## Register

One row per item. Keep entries terse. Date format: `YYYY-MM-DD`. Mark resolved items with `~~strikethrough~~` and a resolved-on date for one cycle, then remove.

| ID | Added | Severity | Area | Debt | Trigger (what makes it bite) | Cap in place | Pay-down |
|---|---|---|---|---|---|---|---|
| TD-FE-VITEST-RED-FUTURE-STORIES | 2026-06-01 | S2 | Frontend / Vitest suite reliability | `npm test` is red on 2026-06-01 even though focused FLB suites pass: feature tests import not-yet-landed hooks/stores (`useArtefactTypeBySlot`, `useStatusChipOptions`, priority hooks, `workspaceFilterStore`), `useStepUpAction` tests mount without `SentinelProvider`, and topology/risk page tests have stale Next navigation mocks. | Any contributor relies on full `npm test` as a regression gate; real failures can hide behind expected-red future-story/provider-mock noise. | This register entry plus the 2026-06-01 verification note in the FLB merge-detection task; focused command `npx vitest run app/components/FormLayoutBuilder/__tests__/mergeTransitions.test.ts app/components/FormLayoutBuilder/__tests__/bandLayout.test.tsx app/components/FormLayoutBuilder/__tests__/undoRedo.test.ts` is green. | Either quarantine future-story tests behind explicit feature flags/`describe.skip` until their stories land, or land the missing modules; update `useStepUpAction` and topology/risk tests to mount the required providers and complete `next/navigation` mocks. |
| TD-FE-ESLINT-ABSENT | 2026-06-01 | S3 | Frontend / lint coverage | `npm run lint` no longer invokes `next lint` because this repo has no ESLint config or `eslint`/`eslint-config-next` dependencies, so Next opens an interactive setup prompt. The script now runs TypeScript plus the project lint suite, leaving general React, hooks, import, accessibility, and unused-symbol lint coverage absent. | The next frontend refactor relies on lint to catch React/hook/import/a11y issues, or a second lint-related miss reaches review. | `package.json` now makes `npm run lint` deterministic and this register entry names the missing ESLint coverage; custom project lints still run. | Add explicit ESLint deps and config, wire a non-interactive `lint:eslint` script, then decide whether `npm run lint` should aggregate `typecheck`, `lint:eslint`, and `lint:project` or keep them as separate CI gates. |
| ~~TD-INFRA-MONITORING-FLOATING-TAGS~~ | 2026-05-27 | ~~S3~~ | ~~Infra / monitoring~~ | ~~The dev monitoring compose intentionally uses floating tags for fast setup (`grafana/alloy:latest`, `prom/alertmanager:latest`, `prom/blackbox-exporter:latest`, exporter images). This is acceptable for dev observability, but it means a future `docker compose pull && up` can change collector behaviour without a code diff.~~ | ~~Monitoring stack becomes procurement evidence, moves toward staging/prod, or an unexplained exporter/dashboard change appears after image refresh.~~ | ~~This register entry plus the repo-owned compose file under `infra/monitoring/dev/`; no auto-update tool is configured.~~ | **Resolved 2026-05-27.** Every monitoring compose image is pinned by immutable digest in [`infra/monitoring/dev/docker-compose.yml`](../infra/monitoring/dev/docker-compose.yml), including Loki, Grafana, Prometheus, Alertmanager, Alloy, node-exporter, cAdvisor, Postgres exporter, Valkey exporter, RabbitMQ exporter, and blackbox exporter. [`infra/monitoring/dev/README.md`](../infra/monitoring/dev/README.md) records the pinned image inventory and a monthly manual refresh checklist. |
| ~~TD-001~~ | 2026-04-23 | ~~S2~~ | ~~DB / polymorphic FKs~~ | ~~Four polymorphic relationships have no Postgres-enforced RI; only `page_entity_refs` has a live writer, and it has no archive cleanup.~~ | ~~First archive/delete handler for `workspace`, `portfolio`, or `product` ships without calling `cleanupPolymorphicChildren`.~~ | ~~[`docs/c_polymorphic_writes.md`](c_polymorphic_writes.md) + canary `backend/internal/dbcheck/orphans_test.go`~~ | **Resolved 2026-04-23 (capped + enforced + service-routed).** Migration 013 dispatch triggers reject orphan inserts at the DB layer; `backend/internal/entityrefs` centralises every polymorphic writer; `bookmarks.go` migrated; `CleanupChildren` ready for archive handlers (none exist yet — capped via registry comment + lifecycle test scaffolding). Phase 4 (per-kind tables) deferred per decision gate — re-open if external review flags the polymorphic shape. See `dev/planning/plan_db_polymorphic_paydown.md`. |
| ~~TD-LIB-001~~ | 2026-04-24 | ~~S2~~ | ~~Auth / wire format~~ | ~~JWT dual-accept on `subscription_id` ↔ legacy `tenant_id` claim via a custom `AccessClaims.UnmarshalJSON`.~~ | ~~Trigger: one full refresh-token cycle post-deploy + a release we want to trim.~~ | ~~`UnmarshalJSON` with grace-window comment in `backend/internal/auth/tokens.go`.~~ | **Resolved 2026-05-16.** Grace window (post-mig-017, 2026-04-24) drained tens of refresh cycles ago. Deleted the custom `UnmarshalJSON` + dropped the `encoding/json` import from `backend/internal/auth/tokens.go`. Natural unmarshal of `AccessClaims` (with the `subscription_id` JSON tag) is authoritative again. Verified via Playwright: fresh logout + login as `claude_3_test@mmffdev.com`, navigated to `/work-items`, protected `/_site/work-items/summary` returned `TOTAL 55674 / EPICS 503 / TASKS 46709 / DEFECTS 2757 / BLOCKED 0` — token's `subscription_id` claim routes through cleanly with no fallback path. `go build ./...` clean. |
| TD-LIB-002 | 2026-04-24 | S3 | DB / billing surface | `subscriptions.tier` defaults to `'pro'` for every existing row. The entitlements service that actually maps a paying customer to a tier doesn't exist yet; until it does, every subscription is implicitly pro and the `mmff_library` reconciler will hand out pro presets to free accounts. | First production customer enrols on a non-pro plan, OR `mmff_library` reconciler ships and starts enforcing tier-gated reads. | CHECK constraint pins the column to `(free, pro, enterprise)` so writers can't smuggle bogus values. Column comment names the temporary default. | Wire the billing/entitlements service to write `tier` on subscription creation and on plan change; backfill existing rows from billing source-of-truth before the reconciler enforces tier gating. |
| TD-LIB-003 | 2026-04-24 | S2 | DB / cross-DB consistency | `pending_library_cleanup_jobs` exists but has no worker. Once the first writer enqueues a job (preset-archive propagation, template-instance unlink, library-mirror purge) the row sits forever. | First archive/adopt handler against a `mmff_library`-derived entity ships and INSERTs into the queue. The DB-side enqueue is harmless on its own; the user-visible bug is "I archived a preset and the cross-DB mirror still serves it." | Table + indexes + constraints exist; the contract (claim with `FOR UPDATE SKIP LOCKED`, exp backoff, `dead` status at `max_attempts`) is documented in the migration header and `feature_library_db_and_portfolio_presets_v3.md` §4. No writer can enqueue yet, so no orphan jobs accrue. | Implement the worker (Go binary or in-process goroutine — TBD per v3 plan §4) before merging the first writer that calls into it. Add a CI canary `SELECT count(*) WHERE status = 'dead'` once the worker exists. |
| ~~TD-LIB-004~~ | 2026-04-24 | ~~S3~~ | ~~DB / migration tooling~~ | ~~Naive `BEGIN; \i file; ROLLBACK;` pattern silently commits because inner BEGIN finalises first.~~ | ~~Next dry-run on shared infra.~~ | ~~Register entry.~~ | **Resolved 2026-05-19.** [`dev/scripts/dry_run_migration.sh`](../dev/scripts/dry_run_migration.sh) copies the migration to a scratch file with top-level `BEGIN;` / `COMMIT;` stripped (inner DO `$$` block delimiters are left alone — indentation-sensitive regex), then runs it inside `BEGIN; … ROLLBACK;` so the schema is restored at end. Supports all three DBs (`mmff_vector` / `vector_artefacts` / `mmff_library`). Smoke-tested against mig 218 — DDL ran, ROLLBACK fired, schema unchanged. |
| TD-LIB-006 | 2026-04-25 | S3 | Frontend / library catalogue | `/portfolio-model` hardcodes the seeded MMFF family id (`00000000-0000-0000-0000-00000000a000`) because no list endpoint exists. Fine while there's exactly one bundle, but adding a second system bundle (SAFe / Jira / Rally / Kanban — Phase 6) means the page will only ever show MMFF. | Second model bundle ships from MMFF (Phase 6 first system-model release). At that point the page silently hides the new bundle. | Constant + comment at top of `app/(user)/portfolio-model/page.tsx` names the limitation; this register entry. | Add `GET /api/portfolio-models` list endpoint (paginated, filtered by `scope` + visibility), then replace the constant with a fetch + selector. UI-only swap once the endpoint exists. |
| TD-LIB-007 | 2026-04-25 | S2 | DB / cross-DB consistency | `subscription_portfolio_model_state.adopted_model_id` references `mmff_library.portfolio_models.id` but Postgres has no cross-DB FKs, so the reference is app-enforced only. If the adoption handler skips the validation step, or if a library row is hard-deleted (today blocked by soft-archive convention only), the adoption row becomes a dangling pointer. | First adoption-handler writer ships and either (a) forgets to validate against `mmff_library` before INSERT, or (b) the library reconciler ever does a hard DELETE on `portfolio_models`. | Migration 026 column comment + table comment name the limitation. CHECK constraint on `status` + partial unique constrain the row vocabulary so a reconciler can mark stale adoptions `failed`/`rolled_back` cleanly. | Wire the adoption handler through `backend/internal/entityrefs` (or a sibling cross-DB writer service) so every INSERT goes through one validating path; add nightly reconciler SELECT to compare `adopted_model_id` against live `mmff_library.portfolio_models.id` (per `feature_library_db_and_portfolio_presets_v3.md` §8 orphan sweep). |
| TD-LIB-008 | 2026-04-25 | S3 | DB / cross-DB consistency | `error_events.code` is a string FK by value to `mmff_library.error_codes.code`. Postgres can't enforce a cross-DB FK. Same class of debt as TD-LIB-007 but lower severity: an unknown/renamed code degrades a dashboard label, not a saga. | First reporter ships and (a) writes a code that doesn't exist in the library catalogue, or (b) the library renames a code without a vector-side rename pass. The reader sees a row with no library label. | Migration 028 column + table comments name the limitation. Index on `(subscription_id, code, occurred_at DESC)` keeps "find unknown codes" SELECTs cheap. | Add a writer-side validator (`reportError(code, …)` SHOULD reject unknown codes in dev mode and silently downgrade in prod), then a nightly reconciler SELECT comparing `DISTINCT error_events.code` against `mmff_library.error_codes.code`. Cheap UI-only follow-up — no schema change required. |
| ~~TD-PERM-001~~ | 2026-05-03 | ~~S2~~ | ~~Auth / RBAC catalogue~~ | ~~Six route surfaces gated by stop-gap permission codes during PLA-0007 G2 because no exact code exists yet.~~ | ~~First custom role assigns one of the stop-gap codes.~~ | ~~This register entry.~~ | **Resolved 2026-05-17.** `internal/permissions/catalogue.go` carries scoped codes (`library.releases.view`, `topology.grants.manage_others`, etc.); no stop-gap wildcard patterns (`menu.admin.view` / `portfolio.list` as catch-alls) remain. Verified by source inspection 2026-05-17. PLA-0007 G2/G3 pay-down complete. |
| TD-LIB-009 | 2026-04-25 | S2 | DB / cross-DB consistency | All five adoption mirror tables (`subscription_layers`, `subscription_workflows`, `subscription_workflow_transitions`, `subscription_artifacts`, `subscription_terminology`) carry `source_library_id` + `source_library_version` referencing `mmff_library.portfolio_model_*` rows with no DB-enforced cross-DB FK. If the adoption orchestrator skips validation, or the library hard-deletes a row, every dependent mirror row becomes a dangling snapshot. Severity is S2 not S3 because the mirrors are the subscription's source-of-truth for layers/workflows — a dangling mirror row breaks state transitions, not just a dashboard label. | First write through the adoption orchestrator (card 00008) that bypasses the library-validation step, OR the library reconciler ever performs a hard DELETE on a `portfolio_model_*` row. The user-visible bug: workflow that points at a `source_library_id` for which no library row exists, blocking upgrade reconciliation. | Migration 029 header + per-table `COMMENT ON TABLE/COLUMN` name the limitation. `source_library_version > 0` CHECK keeps the snapshot column non-zero so a reconciler can tell "stamped" from "uninitialised". `idx_<table>_source ON (subscription_id, source_library_id) WHERE archived_at IS NULL` makes orphan-sweep SELECTs cheap. Same pattern + writer-rules contract as TD-LIB-007. | Route every mirror INSERT through one validating path (e.g. extend `backend/internal/entityrefs` or a sibling adoption writer) that confirms `(source_library_id, source_library_version)` exists in `mmff_library` before commit. Add nightly reconciler SELECT (per `feature_library_db_and_portfolio_presets_v3.md` §8) that joins each mirror table's `(source_library_id, source_library_version)` against `mmff_library.portfolio_model_*` and reports orphans. |
| ~~TD-PERM-002~~ | 2026-05-03 | ~~S3~~ | ~~Backend / test env~~ | ~~`.env.local` pointed at port 5434 (legacy); mig 088 not applied there.~~ | ~~Anyone runs `go test ./...` and sees noise.~~ | ~~Register entry.~~ | **Resolved 2026-05-19 (audit).** `backend/.env.local` now points at port 5435 (the dev tunnel with current migrations). Full `go test ./...` is green. Resolution happened during the broader dev-tunnel cutover; entry was stale. |
| ~~TD-PERM-003~~ | 2026-05-03 | ~~S3~~ | ~~Backend / users service~~ | ~~users.Service.Create translated legacy enum to UUID via opaque subquery.~~ | ~~Code rename → silent NULL role_id.~~ | ~~Source comment.~~ | **Resolved 2026-05-19 (audit).** Path (a) shipped: `users/service.go` now uses `legacyRoleToGrpCode(role)` — a typed Go switch — to translate the legacy enum to a `users_roles_code`. The subquery still resolves the UUID, but the *mapping* is now compiler-checked. With TD-ROLE-001 resolved (RoleID UUID comparisons now used everywhere), Migration Z can drop the enum entirely on its own schedule — this entry is the cap that gets us safely there. |
| TD-PERM-004 | 2026-05-03 | S3 | Backend / users list DTO → Frontend follow-up | **Backend half resolved 2026-05-19:** `sqlListUsersBySubscription` now includes `role_id` in the SELECT; `users.Service.List` scans it into `roletypes.User.RoleID`. Wire shape now carries both the legacy `role` enum AND the structured `role_id` UUID — frontend can migrate incrementally without breaking the legacy consumers. **Frontend half pending:** `app/(user)/_shared.tsx` carries `type AdminUserRole = string` (already loosened from the legacy 3-value union). `user-management/page.tsx` consumes `role` as a string for the dropdown filter; should switch to `role_id` comparison against the roles list endpoint. | First tenant-defined (non-system) role is created and assigned to a user. The workspace-settings filter still matches on the legacy enum string; custom-role users render with no group filter (would be admitted by the role-code matching once the frontend migrates). | This register entry + backend-half ship. The new wire field is non-breaking — legacy code still reads `role`; new code can read `role_id`. | Frontend migration: switch `user-management/page.tsx` role filter from `u.role === filter` to `u.role_id === filter` (comparing UUID to the roles-list endpoint UUIDs); delete `AdminUserRole` once no consumers remain. Single-file frontend diff. |
| ~~TD-DB-001~~ | 2026-05-05 | ~~S3~~ | ~~DB / migration files~~ | ~~13 migrations had no top-level BEGIN/COMMIT wrapping.~~ | ~~Half-applied schema in a future batched-apply runner.~~ | ~~Register entry.~~ | **Resolved 2026-05-19.** 12 of 13 wrapped with explicit `BEGIN;` / `COMMIT;`: mmff_vector {041,046,047,048,066,068,086,088,093,096} + mmff_library {009,011}. The 13th (`mmff_library/001_init_library.sql`) intentionally skipped because it contains `CREATE DATABASE` via `\gexec` which cannot run inside a transaction block. Verified all 12 wrapped files have exactly one top-level `BEGIN;` and one `COMMIT;`. |
| ~~TD-DB-002~~ | 2026-05-05 | ~~S3~~ | ~~DB / unused indexes~~ | ~~233 unused indexes detected on dev (148 droppable candidates).~~ | ~~Write-amplification on artefact INSERT/UPDATE.~~ | ~~Register entry.~~ | **Audit re-run 2026-05-19** (`dev/reports/unused_indexes_2026_05_19.json`): only **27 droppable secondary non-unique** indexes remain at `idx_scan=0` (down from 148). Examined the list: every one is either (a) tiny (<20 KB, write-amplification cost negligible), or (b) on a code path that's clearly production-relevant but hasn't fired on dev (`users_sessions_*`, `users_password_resets_*`, `users_roles_pages_*`, `users_nav_prefs_*`, `pages_*`, etc.). **Decision: do NOT drop.** Dev shows `idx_scan=0` because dev has small data; these indexes will fire under prod query plans. Conservative call wins because the cost of keeping them is microscopic and the cost of a wrong drop is a tail latency regression. Re-run the audit after the first prod traffic week — that's when the signal matures. |
| TD-DB-003 | 2026-05-07 | S3 | DB / schema drift | `workspace` (singular, from 004) and `master_record_workspaces` (plural, formerly `workspaces` from 098, renamed in migration 131 on 2026-05-08) coexist in mmff_vector. Both are subscription-scoped after migration 017 renamed `tenant_id` → `subscription_id`. Production code (`adopt.go`, `fields/handler.go`, `portfolio/master_record_handler.go`, `wsperms/service.go`) reads from the singular `workspace` table. Test files and the adoption saga (`adoptomodels/adopt.go`) read from the plural `master_record_workspaces` table. This naming inconsistency is a latent source of bugs: a future change that hardcodes one table name without checking both will silently miss rows from the other. The tables serve different purposes: `workspace` (legacy, part of portfolio stack) is being gradually replaced by `master_record_workspaces` (new, subscription-scoped, anchor-record naming). Once migration M7 lands (which drops legacy portfolio-stack tables), the singular `workspace` will be retired entirely. | Either the multi-tenancy cutover (PLA-0023+) begins retiring `workspace` OR a third piece of code assumes one table and gets rows from the wrong scope. | This register entry. Any code query that touches either table should be documented/code-reviewed for which one it targets and why. | Once M7 ships (retirement of legacy adoption mirrors + strategy layers): write migration M8 that drops the singular `workspace` table entirely. Pair a diff of all queries reading from both tables to confirm they've migrated. The migration is mechanical once the legacy stack is gone; the structural coupling is the risk. |
| ~~TD-DB-004~~ | 2026-05-08 | ~~RESOLVED 2026-05-08~~ | ~~DB / naming consistency~~ | Migrations 132 (`workspace_roles` → `roles_workspaces`), 133 (`org_node_roles` → `roles_org_nodes`), 134 (`page_roles` → `roles_pages`), and 135 (`role_permissions` → `roles_permissions`) clustered all four role-grant tables under the `roles_*` prefix. `\dt roles*` now returns the complete catalogue: `roles, roles_org_nodes, roles_pages, roles_permissions, roles_workspaces`. Each rename was paired with a Python-regex sweep over Go SQL literals; smoke-tested per migration (135's hot path verified by login → 29 permissions resolved for gadmin). | n/a — closed. | n/a — closed. | n/a — closed. |
| ~~TD-DB-005~~ | 2026-05-08 | ~~RESOLVED 2026-05-08~~ | ~~DB / dead ACL surface~~ | Legacy boolean ACL `user_workspace_permissions` (created 002, renamed 007) and its Go owner package `internal/wsperms` (sole-writer service + 3 HTTP routes under `/api/permissions`) were superseded by `roles_workspaces` (named-role grants with soft-delete + tenant scope, FK to `master_record_workspaces`). The old surface had 2 read callers (`fields/handler.go`, `portfolio/master_record_handler.go`) and 0 frontend consumers — dev DB held 0 rows in the legacy table. Migration 136 dropped the table; `internal/wsperms/` package, route block in `main.go`, and `models.UserWorkspacePermission` struct removed; the 2 callers now read `roles_workspaces` directly with `revoked_at IS NULL`. Comment-only references in `clientip.go`, `roles/service.go`, `addressables/help_lifecycle_test.go`, `portfolio/master_record_handler.go`, and `portfoliomodels/handler_workspace_layers.go` cleaned up. Backend rebuilt clean; gadmin login → master_record GET 200 → fields GET 200 verified on `:5100` against dev DB. | n/a — closed. | n/a — closed. | n/a — closed. |
| TD-FE-002 | 2026-05-07 | S3 | Frontend / workspace context | **Trigger fired 2026-05-19**: three production call sites now duplicate `workspacesApi.list()` — `app/components/topology/useTopologyData.ts:47`, `app/(user)/workspace-admin/workspaces/page.tsx:346`, `app/(user)/vector-admin/api-manager/webhooks/page.tsx:39`. Original entry said "third real production page needs workspace ID from a fetch" — that's where we are. The `/v2/custom-fields` PoC + `workspace-admin/workspaces/page.tsx` listArchived call don't count (former is a constant; latter is the admin list, not a "get the active workspace" fetch). | Trigger fired. Next page that needs a workspace ID by fetch will duplicate the pattern a fourth time. | This register entry + the three open call sites. | Extract a `WorkspaceContext` provider (wraps `workspacesApi.list()` + loading/error state) mounted at the `(user)` layout; switch all call sites to `useContext(WorkspaceContext)`. Single-file layout change + one `useWorkspace()` hook. UX-touching (loading state visible during initial fetch) so left for user to schedule. |
| ~~TD-FE-001~~ | 2026-05-05 | ~~S2~~ | ~~Frontend / AdoptionOverlay SSE handler~~ | ~~`dispatchFrame` (pure state machine, line 115–185) and `handleFrame` (runtime closure with side-effects, line 312–393) duplicated all state-transition logic. The two functions were a copy-paste divergence: every change to step-frame handling, done-frame handling, or fail-frame handling required edits in both places to stay in lockstep.~~ | ~~New feature touching adoption SSE handling without syncing both functions, or a refactor that changes only one and breaks the contract the tests verify against.~~ | ~~Tests pinned the `dispatchFrame` contract, which is the unit-test surface; the comment on line 95 acknowledged the lockstep requirement.~~ | **Resolved 2026-05-07 (refactored away).** Extracted the pure state-transition logic into `dispatchFrame` and made `handleFrame` call it, then apply React side-effects. Eliminated 51 lines of duplication. Tests pass unchanged. |
| ~~TD-CSS-001~~ | 2026-05-05 | ~~S2~~ | ~~Frontend / CSS catalog~~ | ~~Tables across the user-facing app are split between two systems: the legacy `.table-wrap` / `.table` / `.table__head` / `.table__row` / `.table__cell` family, and the canonical `.tree_accordion-dense__scroll` / `__table` / `__head` / `__th` / `__row` / `__cell` family. 17 TSX files still emitting `.table*` classes.~~ | ~~Any future site-wide table refresh requires a synchronised edit to two systems instead of one.~~ | ~~Rule codified in `docs/css-guide.md` Tables section.~~ | **Resolved 2026-05-18.** All production TSX callers migrated to `tree_accordion-dense__*`. Exemptions: `theme-classic/page.tsx` (legacy showcase — intentional), plus tree exceptions + provisional category in `raw_table_exempt.json`. Dead CSS stripped: `.table__row--saving`, `.table__cell-meta`, `.table--auto-expander` (renamed to `.fs-table--auto-expander`). Legacy `.table*` rules retained in `globals.css` solely to support the `theme-classic` showcase page. |
| ~~TD-WI-001~~ | 2026-05-07 | ~~S2~~ | ~~Backend / work-items v1 deprecation~~ | ~~v1 `obj_work_items` handler + routes (`backend/internal/workitems/`) still live alongside the v2 `vector_artefacts` path. Any new feature built against v1 accrues migration debt.~~ | ~~WORK_ITEMS_V2=true in prod AND v2 parity-clean for ≥1 week → delete `backend/internal/workitems/` handler + routes + obj_work_items references.~~ | ~~PLA-0023 complete; v2 live in dev since 2026-05-07. This register entry + `docs/c_c_v2_workitems_cutover_followups.md` deferred-work register. Do not build new features against v1.~~ | **Resolved 2026-05-08.** `backend/internal/workitems/` deleted (8 files); 4 v1 route blocks removed from `main.go`; rank service retargeted to `vector_artefacts.artefacts` (`vaPool`); migration 137 drops `obj_work_items` + `obj_work_items_field_values` from `mmff_vector`; ETL backfill files deleted; frontend `CORE_FIELDS` + stale comments updated to `artefacts.*`; integration test rewritten against `vector_artefacts`. Dev DB migration applied 2026-05-08. |
| ~~TD-LIB-010~~ | 2026-05-05 | ~~S2~~ | ~~Backend / librarydb tests + portfoliomodels handler~~ | ~~R010 dropped portfolio_models* family; librarydb still carried dead fetch paths and the grant matrix referenced dropped tables.~~ | ~~5 tests permanently red.~~ | ~~Register entry.~~ | **Resolved 2026-05-19.** Deleted dead `FetchByModelID` / `FetchLatestByFamily` / `fetchInTx` / `loadLayers` / `loadWorkflows` / `loadTransitions` / `loadArtifacts` / `loadTerminology` / `loadModelByID` / `loadLatestByFamily` / `scanModel` + their SQL constants (none called from production after R010). Rewrote `fetch_test.go` against the live `FetchTemplateByID` path. Fixed `grants_test.go` `bundleTableList()` → `portfolio_templates` + `portfolio_template_layer_definitions`; publish role grant expectations adjusted to match reality (admin-only on templates post-R010). Dropped `Model.Key == "mmff"` assertion (no key column post-R010); test asserts `Model.Name` non-empty instead. Fixed `releases_test.go` acks pool routing (`library_releases_acknowledgements` lives in vector_artefacts not mmff_vector). Full librarydb + portfoliomodels suites green. |
| ~~TD-DB-006~~ | 2026-05-09 | ~~S3~~ | ~~DB / legacy mmff_vector cleanup~~ | ~~Three topology V1 tables sat inert.~~ | ~~Monitoring tools querying legacy tables.~~ | ~~Register entry.~~ | **Resolved by migration 138** (`138_drop_legacy_topology_v1_tables.sql`). Verified: dev DB has no `org_nodes` / `org_levels` / `org_node_roles` tables; backend Go references are comments only. Entry was stale. |

| ~~TD-DB-007~~ | 2026-05-10 | ~~S2~~ | ~~DB / extensions + migration drift~~ | ~~Migrations 035 (`search_outbox`) and 036 (`master_record_tenant`) against `vector_artefacts` were not applied because the swarm `vector-dev_postgres` ran `postgres:16-alpine` (no pgvector), and 035 issues `CREATE EXTENSION vector`. The sequential runner stayed blocked on 035, leaving both perpetually "pending".~~ | ~~n/a — closed.~~ | ~~n/a — closed.~~ | **Resolved 2026-05-18 (verified during pg_stat_statements rollout).** Live `docker service inspect vector-dev_postgres` confirms image is `pgvector/pgvector@sha256:a132765e…` — the image swap recommended here was performed out-of-band at some prior date. `vector_artefacts.pg_available_extensions` includes `vector`; `schema_migrations` shows 035 and 036 both applied. Staging/prod parity check is the only remaining hygiene — picked up separately if/when those tiers diverge. |
| TD-OPS-SWARM-STACKFILE | 2026-05-18 | S2 | Ops / infra-as-code | The `vector-dev` Docker Swarm stack (`vector-dev_postgres` + `_adminer` + `_homepage` on `vector-dev-pg` / 77.68.33.216) had **no repo-side stack file** until 2026-05-18. Every service config — including `pg_stat_statements` preload args added the same day, the pgvector image digest pin, and the external `postgres_password` secret — lived only in the live Swarm spec. A `docker stack rm vector-dev && docker stack deploy …` from any forgotten compose file would have silently lost the preload (and any other out-of-band `docker service update` not back-ported). Defence/finance audit posture also requires infra-as-code for the dev DB tier. | (a) Anyone redeploys the stack from a stale file (a future contributor, a node rebuild, an "I'll just recreate it" recovery action). (b) Procurement audit asks for the dev infra spec and the answer is "it's in a running Swarm task." (c) Out-of-band `docker service update` drifts the live spec away from the file without anyone noticing. | [`infra/swarm/vector-dev-stack.yml`](../infra/swarm/vector-dev-stack.yml) captures all three services (postgres image pinned by digest, pg_stat_statements args inline, pgdata volume, external secret). [`infra/swarm/README.md`](../infra/swarm/README.md) documents deploy / diff / re-sync rituals. `dev/scripts/ssh_manager.sh` gained `verify_pg_stat_statements()` — runs on every laptop-setup pass, fails loudly with a pointer to the README if the preload is missing. | (1) On any out-of-band `docker service update`, immediately re-sync `vector-dev-stack.yml` from the live spec (procedure documented in README). (2) Decide whether to also publish a `vector-staging-stack.yml` and `vector-prod-stack.yml` — the same drift class applies on those tiers and is invisible from here. (3) Optional: add a CI job that SSHs to the manager node, diffs `docker service inspect` output against the committed stack file, and fails the build on drift. |
| TD-AUTH-001 | 2026-05-13 | S2 | Auth / refresh-cookie cleanup | `backend/internal/auth/handler.go:230` clears refresh cookies on five legacy paths (`/`, `/auth`, `/samantha/v1/auth`, `/v1/api/auth`, `/api/auth`) on every logout / refresh. Browsers retain `rt` cookies path-scoped, so users who logged in before the route migration carry a phantom cookie at `Path=/samantha/v1/auth` that this loop evicts. The `/samantha/v1` block is now deleted (commit `4ab58a3`, 2026-05-13), and the deprecation Sunset date is 2026-08-07; once a full refresh-token lifecycle has elapsed past that date, no live browser still has a cookie at `/samantha/v1/auth` and the entry is dead weight. Same logic applies to the even-older `/v1/api/auth` and `/api/auth` entries from the pre-rename era. | 2026-09-01 — Sunset (2026-08-07) + one refresh-token TTL (default 30d, conservative). After that date no live browser cookie is anchored at any of the legacy paths, and the clear-list entries are forever dead. Either nothing breaks (the no-op clear is harmless) or someone reads the code and wastes a session asking "why are we clearing /v1/api/auth, who uses that". | This register entry. Inline comment in `auth/handler.go:228-229` already names the legacy-path eviction intent. The `/samantha/v1` chi block deletion (commit `4ab58a3`) means no handler ever sets a cookie at those paths anymore — only legacy ones in browsers remain. | At 2026-09-01: open `backend/internal/auth/handler.go`, find the path slice at line 230, drop the four legacy entries (`/auth`, `/samantha/v1/auth`, `/v1/api/auth`, `/api/auth`), keep `/`. Single-file edit, ~5 LOC removed. Verify no regressions by signing in fresh + logging out + checking cookie jar empty. |
| ~~TD-API-AUDIT-001~~ | 2026-05-19 | ~~S2~~ | ~~Frontend / shadow-backend bypass~~ | ~~Two live touchpoints bypassed siteAPI via Next.js shadow handlers: CustomFieldsTree → /api/dev/artefact-types/*, and useRelationsData → /api/v2/work-items/relations.~~ | ~~n/a — closed.~~ | ~~n/a — closed.~~ | **Resolved 2026-05-19 via dead-code deletion, not migration.** Investigation revealed BOTH remaining shadow handlers had been broken since the RF1.4.2 column-prefix rename: they queried `artefact_types` / `artefact_field_library` / `flow_states` (singular, pre-prefix names) which no longer exist. Curl-tested both: `{"error":"relation \"artefact_types\" does not exist"}`. Consumers had been dead-on-arrival for months without anyone noticing — `WorkItemRelations` was only mounted from `MapRelationship3D`, which always passes its own `payload` prop (so the broken hook fired and was discarded); the Custom Fields admin page was mounted but error-screen-only on every load. Action taken: deleted the entire dead-surface stack — `app/v2/` PoC pages (5 routes), `app/api/v2/work-items/`, `app/api/dev/{field-library,artefact-types}/`, `app/components/{CustomFieldManager,CustomFieldsTree,CustomFieldsWorkItemsBody}.tsx`, `app/(user)/workspace-admin/custom-fields/**`, `app/components/WorkItemRelations/index.tsx`, `app/hooks/useRelationsData.ts`, and `app/lib/v2/db.ts` (the last Next.js pg pool). Types moved to `app/components/MapRelationship3D/types.ts`; `MapRelationship3D`'s `payload` prop is now required. Nav row dropped via migration 227. Final audit: 0 red, 0 black. Lints `lint:api-caller-discipline` + `lint:api-helper-exclusive` enforce the rule going forward. |
| TD-API-002 | 2026-05-13 | S2 | API / openapi.yaml retirement | `openapi.yaml` (root) declares 100 spec paths under `servers: /samantha/v1`, but post-route-migration only ~1 maps to a live Go handler — the other 99 are documentation of a ghost API. `check_routes.sh --spec openapi.yaml` currently passes (0 errors, 99 warnings) because the gate is "spec-first OK" on warnings. The spec is also mirrored at `api-reference/static/openapi.yaml` (deployed Scalar playground at `/openapi`) via manual `cp` — the two files have drifted (root last edited 2026-05-09, static last edited 2026-05-08). Both still advertise `servers: /samantha/v1` to external readers, pointing at a dead route prefix. | Anything that reads `openapi.yaml` as authoritative: (a) an external consumer who lands on the Scalar playground and tries to call `/samantha/v1/work-items` (404); (b) a CI gate someone adds that promotes warnings to errors; (c) a future SDK-generation pass (deferred per TD-API-001 item 11). Probability rises with each new external touch-point of the API. | This register entry. The CI gate `check_routes.sh --spec openapi.yaml` continues to pass on warnings, so PRs don't fail. The deployed Scalar playground is the only externally visible artefact still serving the wrong base URL. | Three-step sequence, can be one PLA card or three small commits: (1) Archive `openapi.yaml` → `openapi-v1-archived.yaml` and remove the `api:check:v1` npm script + the v1 workflow step in `.github/workflows/api-contracts.yml`. (2) Strip the 99 orphan path entries from `openapi-v2.yaml` if any were mirrored, or build a fresh `openapi.yaml` from scratch reflecting live `/_site` + `/samantha/v2` routes. (3) Manual `cp` to `api-reference/static/openapi.yaml` to resync the Scalar playground, then `npm run api:snap` to refresh the baseline. Needs `<grill-with-docs>` to choose between "retire v1 entirely" vs "v1 = /_site BFF surface, v2 = /samantha/v2 public surface". |
| TD-API-003 | 2026-05-13 | S3 | API / CI gate fidelity | `dev/scripts/check_callers.py` regex `\bapi(?:<[^>]*>)?\s*\(\s*["\']([^"\'?#`]+)` matches any local identifier named `api` followed by a path string — but the project uses `import { apiSite as api }` extensively (40+ files). Those callers hit `/_site/...`, not `/samantha/v1/...`, but the gate treats them as v1 callers because the bare path strings (e.g. `/auth/login`) match. Current run: 20 "mapped endpoints" reported, of which majority are aliased apiSite calls. Net effect: the gate's CALLER coverage is real for the bare paths, but the REPORTING is misleading — it claims to validate v1 callers but mostly validates `/_site` ones. | (a) Anyone uses the `caller-map.json` output to reason about v1 traffic (it's lying — those callers are /_site). (b) Anyone deletes a `/_site/auth/login` route and the gate doesn't fail because the caller IS in the v1 spec (`/auth/login` matches by bare path). The gate softens silently because aliased apiSite calls pollute the v1 caller set. | This register entry. The gate still hard-fails on undocumented bare paths, so the spec-fidelity intent partly survives. | Tighten the regex to require an unaliased `api(` — match `\bapi(?:<[^>]*>)?\s*\(\s*["\']` ONLY when the file does NOT contain `import { apiSite as api }` or similar alias for the identifier `api`. Alternative: rename the legacy helper from `api` to `apiV1` in `app/lib/api.ts`, force every caller to be explicit, then the regex correctness becomes trivial. Single-script + small frontend rename. |
| ~~TD-API-004~~ | 2026-05-13 | ~~S3~~ | ~~CI / workflow trigger gap~~ | ~~Contract gates only fire on PR-against-main, never on feature-branch push.~~ | ~~Late noisy PR.~~ | ~~Register entry.~~ | **Resolved 2026-05-19 (option B).** `dev/git-hooks/pre-push` (the tracked source symlinked from `.git/hooks/pre-push`) now chains `dev/scripts/pre-push.sh` after the backup + rg-rerun steps. Runs check_routes.sh + check_callers.py + oasdiff breaking-change check on every push in soft mode (`|| true`) so drift surfaces per-commit on long-lived feature branches. Escape hatch: `SKIP_API_CONTRACT=1 git push`. Existing `[breaking]` commit-message escape hatch inside pre-push.sh remains. Option A (workflow trigger change) deferred — option B covers this developer; CI gate remains the safety net on PR. |
| TD-AUTH-ARGON2ID | 2026-05-26 | S2 | Auth / password hashing | Vector hashes `users.users_password_hash` (and `users.users_mfa_recovery_codes`) with bcrypt cost=12 via `backend/internal/auth/password.go` + `mfa.go`. bcrypt is still ACCEPTABLE under NIST SP 800-63B-4 but no longer PREFERRED — OWASP Password Storage Cheat Sheet 2024+ and RFC 9106 (Aug 2021) both name Argon2id (memory-hard) as the modern recommendation. Defence/finance procurement reviewers (per `context/USER.md` — NIST 800-53 SC-13, PCI-DSS 4.0 §8.3.2, FFIEC AIO 4.13, JSP 440 Annex C) increasingly flag bcrypt as "compliant but dated" in 2026. Full research + parameter recommendation + implementation plan filed as **RES060** on Dev → Reporting. | (a) First defence/finance SOC 2 Type II audit. (b) GPU/ASIC attack feasibility rising; memory-hard KDF closes a known weakness bcrypt doesn't. (c) Any procurement RFP that asks "is the password store memory-hard?" — bcrypt's answer is "no". | This register entry. RES060 captures the parameter selection (OWASP Profile B: m=19 MiB, t=2, p=1), dual-hash lazy-rehash migration plan (no global password reset), MFA recovery code parallel migration, and concurrency-cap concern (8-semaphore guard against memory exhaustion on the auth handler). | Five-pillar rollout per RES060: **Pillar A** — add `golang.org/x/crypto/argon2` import; PHC-string prefix detection (`$argon2id$` vs `$2a$`) so no schema column needed; lazy rehash on successful login (verify with current algo, rehash + write argon2id, flip the stored hash atomically); 180-day force-rotate deadline writes `users_force_password_change=true` for any account still on bcrypt at the cutoff. **Parallel:** rewrite `mfa.go` recovery codes the same way. Estimated 6–8 hours. Pillars B–D extend the cryptographic foundation to at-rest data encryption (see TD-ARTEFACT-ENCRYPTION-AT-REST). |
| TD-ARTEFACT-ENCRYPTION-AT-REST | 2026-05-26 | S2 | Security / at-rest encryption | Artefact text fields (`artefacts.artefacts_title`, `artefacts.artefacts_description`, `artefacts.artefacts_description_doc`, and every text/string value in `artefacts_fields_values.fields_values_value_text`) are stored in plaintext in `vector_artefacts`. The only protection today is disk-level encryption on the Postgres volume (managed by the host filesystem / cloud provider EBS) — which fails PCI-DSS 4.0 §3.5.1.2 ("disk encryption alone is insufficient"), JSP 440 Annex C, and the NIST 800-53 SC-28 control narrative for data-at-rest protection in a multi-tenant SaaS context. Auditors explicitly disqualify disk encryption because it doesn't protect against the most common attack vectors: compromised DB backup, dropped table dump, malicious DBA, cloud provider subpoena. Full research filed as **RES060** with approach comparison (envelope vs pgcrypto vs pg_tde), searchability impact analysis, key-rotation strategy, and BYOK posture for defence customers. | (a) First defence customer asks "is artefact text encrypted at rest in a way that survives a DB dump leak?" — current answer is "no". (b) First PCI-DSS 4.0 or SOC 2 Type II audit. (c) Any data-residency requirement that forces per-tenant key isolation (BYOK / HSM). | This register entry. Disk encryption + DB-level access controls (Sentinel clamp, role-based row visibility) provide defence in depth but do not satisfy "encrypted at rest" in the audit narrative sense. The search outbox (`artefacts_search_outbox`) currently holds plaintext extract for search-index emission — needs co-ordinated treatment with this entry. | Per RES060: application-level envelope encryption with **AES-256-GCM**, per-tenant DEK wrapped by a KMS-managed KEK. Schema: add `<col>_ciphertext bytea` + `<col>_nonce bytea` columns alongside each encrypted text field (preferred over sidecar table for atomicity). Backfill migration encrypts existing rows then drops the plaintext columns. Search index becomes a trusted zone (encrypted itself, narrow IAM); the search-outbox handler is the sole decrypt-at-emit point. BYOK for defence: PKCS#11 path via `crypto11`; AWS KMS / GCP KMS / Azure Key Vault for finance. **Five-pillar rollout mirroring the just-completed DB refactor cadence** (RES060 §"Pillar-by-pillar rollout"): Pillar B = pilot on `artefacts.artefacts_title` only; Pillar C = extend to all artefacts text columns; Pillar D = extend to `artefacts_fields_values`; Pillar E = procurement deck mapping each control to NIST 800-53 SC-13/SC-28 + PCI-DSS 4.0 §3.5.1.2 + JSP 440 Annex C + FFIEC AIO 4.13 + SOC 2 CC6.7. Total estimated: 27–36 hours of focused work. |
| TD-NAV-CASCADE-BROKEN | 2026-05-26 | S2 | DB / trigger functions | `fn_users_roles_pages_cascade_nav_prefs()` references pre-Pillar-1 bare column names (`pages.id`, `pages.key_enum`, `users.id`, `users.role_id`, `users.subscription_id`) that no longer exist after the column-prefix sweep (commits `f6b3f3d2` through `bd1d7c52`). The function copied AS-IS from mmff_vector → vector_artefacts in Pillar 2 migration 117 to preserve mmff_v's broken behaviour exactly (no regression). Fires on DELETE of users_roles_pages rows. Currently dormant — the cascade triggers only on hard DELETE which doesn't happen in normal app flow (rows are soft-archived via `archived_at` instead). | (a) Hard DELETE on `users_roles_pages` ever fires (admin tooling, manual SQL cleanup, future migration). (b) A new admin route that DELETEs grants instead of soft-archiving them. The error will be `record "old" has no field "key_enum"` and will surface as a 500 to whoever triggered the cascade. | This register entry. The function is copied AS-IS and documented inline at the head of migration 117. The trigger fires on a code path that doesn't exist in production routes today. | Rewrite the function body to use the prefixed column names: `OLD.users_roles_pages_id_page` → `pages.pages_id` JOIN to get `pages.pages_key_enum`; `OLD.users_roles_pages_id_role` to find affected users via `users.users_id_role`; `NEW.users_id` / `users_id_subscription` for the cleanup INSERT. Single migration in vector_artefacts. Estimated 1-2 hours including a test that exercises the DELETE cascade. |
| TD-POLYMORPHIC-DISPATCH | 2026-05-26 | S2 | DB / orphan trigger | `trg_subscriptions_stakeholders_dispatch` was intentionally OMITTED from vector_artefacts in Pillar 2 migration 116 because its target function `dispatch_polymorphic_parent()` doesn't exist (referenced but never defined — its original parent tables `company_roadmap`, `workspace`, `portfolio`, `product` are all DEAD per the pre-refactor SY003 inventory). The `subscriptions_stakeholders` table itself is empty (0 rows) and has been since before the refactor; the trigger therefore can't fire even if it existed. | First INSERT into `subscriptions_stakeholders` would fail with "function dispatch_polymorphic_parent does not exist" if the trigger ever DID exist — but since we omitted it, the INSERT will succeed without the polymorphic dispatch happening. The user-visible bug would be: stakeholder rows accumulate with no downstream notification or audit trail (whatever the dispatch was supposed to do). | This register entry. Table has 0 rows; no downstream code currently inserts into it. CHECK constraint on the polymorphic-parent column tightened to a single surviving kind (`'master_record_workspaces'`) in the same migration so any future insert is at least pointing at a real table. | Either (a) DROP the `subscriptions_stakeholders` table entirely if confirmed unused (audit grep for INSERT INTO subscriptions_stakeholders across backend/internal/) — likely zero hits since the table was abandoned with the pre-PLA-0023 work-items substrate; or (b) define `dispatch_polymorphic_parent()` with a body that does whatever the original intent was, then ADD the trigger to vector_artefacts. Decision deferred until a feature actually needs stakeholder records. Likely (a) — clean drop in a future cleanup migration. |
| TD-JSON-WIRE-TAGS | 2026-05-26 | S3 | Backend / wire contract uniformity | The 23-table column-prefix sweep (Pillar 1) renamed every DB column to `<table>_<col>` but DELIBERATELY left every `json:"…"` struct tag on `users.User`, `artefacts.Artefact`, etc. EXACTLY as they were (`json:"id"`, `json:"email"`, `json:"title"`, etc.). This preserves wire-contract stability through the refactor — frontend reads `.id` / `.email` / `.title` off API responses and didn't need a coordinated rewrite. The cost: there is now an internal mismatch between DB column names (`users_id`, `artefacts_title`) and wire field names (`id`, `title`). | (a) New backend code introduces field-level access that confuses the two namespaces (e.g. `user.UsersID` Go field with `json:"id"` tag — easy to typo). (b) A future contributor renames a struct field thinking the wire contract changes, breaking the frontend. (c) Procurement audit asks "what's the canonical name of a user's identifier — `id` or `users_id`?" and the answer requires explanation. | This register entry. The discipline is documented in every wave-N commit message ("JSON tags untouched — wire contract preserved"). Test suite passes because the JSON marshal/unmarshal still produces the expected wire shape. | Two-wave migration (mirroring the wave-6a/6b split that was deferred): **Wave A** — rewrite every `json:"<col>"` tag on the moved-table struct types to match the new column name (`json:"users_id"`, `json:"artefacts_title"`, etc.). Backend test suite will pass automatically (Go struct fields didn't change). **Wave B** — coordinate frontend rewrite: TypeScript types, API client (`app/lib/apiSite/`), every component reading `.id` / `.email` / etc. off API responses. Estimated 8–12 hours for the coordinated wave (most of which is mechanical search-and-replace; the risk is missing a consumer). NOT urgent — wire contract is stable and consistent; this is uniformity-of-naming polish, not correctness. |
| TD-DB-POOL-COLLAPSE | 2026-05-26 | S3 | Backend / connection pool redundancy | After Pillar 3 step 3, `backend/cmd/server/main.go` opens TWO pgxpools to vector_artefacts: the legacy `pool` (via `db.New(ctx)` reading `$DB_NAME` which is now `vector_artefacts`) AND `vaPool` (via `$VECTOR_ARTEFACTS_DB_URL`). Both connect to the same database. `servicePool = vaPool ?? pool` selects vaPool when available, falling back to pool. The duplication wastes ~2 idle connections per backend instance and adds 80ms to startup. The pre-refactor design had these as TWO DIFFERENT databases; after the merge they're one. | (a) Backend instance count scales up and the redundant connections matter. (b) Anyone reads main.go and the dual-pool logic confuses them ("which is canonical?"). (c) Connection pool exhaustion ever bites; the duplication makes the problem harder to diagnose. | This register entry. Both pools work; the duplication is wasteful but not incorrect. The `servicePool` selection abstracts the duplication from downstream service constructors. | Pick one pool and remove the other. Recommended: keep `pool` (the canonical one driven by `$DB_NAME`), delete `vaPool` + the conditional logic + the misleading log strings. `servicePool := pool` becomes one line. Or alternatively: keep `vaPool` as the explicit "this is the tenant DB" variable, retire `pool`. Either way, single-file diff in main.go (~50 LOC removed), small commit. Pair with renaming the `$VECTOR_ARTEFACTS_DB_URL` env to `$DATABASE_URL` (or similar generic name) to complete the cleanup. Estimated 1-2 hours. Defer until next backend startup-config change touches the area for an unrelated reason. |
| ~~TD-ORG-001~~ | 2026-05-13 | ~~S3~~ | ~~Backend / orgdesign service~~ | ~~`orgdesign.Service.pool` struct field was dead weight after PLA-0023 P6.~~ | ~~Constructor confusion; new code written against a dead pool field.~~ | ~~This register entry.~~ | **Resolved 2026-05-17.** `backend/internal/orgdesign/` package does not exist — fully removed as part of PLA-0023 refactor. Dead field and constructor concern are moot. Verified by source inspection 2026-05-17. |
| ~~TD-TOP-001~~ | 2026-05-14 | ~~S2~~ | ~~Backend / topology boundary~~ | ~~`backend/internal/portfoliomodels/dev_reset.go` wrote four topology tables directly, bypassing the sole-writer boundary.~~ | n/a — closed. | n/a — closed. | **Resolved 2026-05-17.** `masterResetVA` already delegates to `topology.Service.PurgeTenantTopologyData(ctx, subscriptionID, tx)` (step 6) and `topology.Service.SeedRootNode(ctx, devWorkspaceID, subscriptionID, "ACME Bank", tx)` (step 9). Both methods were added to topology.Service, backed by SQL constants in `topology/sql.go`. Boundary test `TestPackageBoundary` confirmed GREEN. Boundary test comments + error message updated to reflect post-RF1.4.2 table names (users_roles_topology_nodes, topology_view_states). Sibling TD-TOPOLOGY-BOUNDARY-PORTFOLIOMODELS resolved in same pass. |
| TD-API-001 | 2026-05-08 | S3 | API / v3 strategy | REST v1→v2 cutover (PLA-0030) is in progress. A v3 public API platform was scoped on 2026-05-08 covering: (1) GraphQL supplement for nested data queries (deferred — would add a third surface mid-cutover); (2) async real-time push — no general pub/sub layer exists beyond adoption SSE; (3) `POST /search` DSL query endpoint — `searchworker` indexes TSVECTOR + pgvector embeddings via outbox but `coreTableMap` is empty post-artefact-cutover (worker is broken); (4) webhook delivery (item.status_changed, sprint.closed, etc.) — no webhook table, outbox, or retry infrastructure; (5) async report jobs (burndown, cumulative-flow, cycle-time) — no 202/job-poll infrastructure; (6) hierarchy endpoint (`GET /items/{id}/children` depth-annotated) — not built; (7) state-machine transition enforcement on write — flow states exist but API does not validate transitions; (8) `GET /workspaces/{id}/active-sprint` pre-computed summary — not built; (9) rate-limit response headers (`X-RateLimit-Remaining`, `Reset`, `Reset-Duration`) — `httprate` emits basic headers but sliding-window variant not configured; (10) n8n integration — API keys (`sam_live_*`, Blake3-hashed, scoped, revokable) exist and auth middleware honours them, but n8n requires a trigger-node (webhook inbound) + action-node (REST outbound) pair with OAuth2 or API-key credential type in a published n8n community node, none of which exist; (11) OpenAPI v2 spec + TypeScript/Python SDK generation — not yet created (PLA-0030 Task 1). **Already built:** full CRUD + bulk work items (v2), custom fields schema + field_values, sprint lifecycle + velocity, per-user and per-IP rate limiting, API key issuance/validation/revoke. | v1→v2 cutover completes (PLA-0030 done) — that is the gate for v3 design to start. Individual triggers: (a) first external integration consumer needs webhooks; (b) second real-time surface beyond adoption SSE needed; (c) n8n marketplace listing is pursued; (d) `searchworker` coreTableMap must be rewired before any search query endpoint is exposed. | This register entry. API keys are the foundation for all external integrations — do not add a competing auth scheme before v3 is designed. `searchworker` coreTableMap gap documented here; rewire is a pre-req for any `POST /search` endpoint. For n8n: the API key + webhook infra must exist before a community node is worth building. | After v2 stable in prod ≥1 sprint: (1) ADR: GraphQL vs sparse-fieldset REST + WebSocket/NATS async — 1-day spike; (2) design webhook table + outbox + retry worker as a single PLA; (3) rewire `searchworker.coreTableMap` for `vector_artefacts`; (4) expose `POST /search` endpoint backed by TSVECTOR + optional pgvector rerank; (5) async job infrastructure (202 + poll) shared by reports + future bulk ops; (6) n8n community node (trigger + action) once webhook + API key surface is stable; (7) `openapi-v2.yaml` + codegen for TS + Python SDKs. Each item is a separate PLA card; sequence matters (webhooks before n8n, search rewrite before query endpoint). |
| ~~TD-RESET-001~~ | 2026-05-14 | ~~S2~~ | ~~Backend / portfoliomodels dev_reset~~ | ~~`sqlDeleteAllArtefactFieldValuesForSubscription` referenced nonexistent `subscription_id` on `artefacts_fields_values`.~~ | ~~MasterReset fails for any tenant with field values.~~ | ~~Register entry.~~ | **Resolved 2026-05-14 (same day).** Query rewritten to `DELETE FROM artefacts_fields_values fv USING artefacts a WHERE fv.artefacts_fields_values_id_artefact = a.id AND a.subscription_id = $1`. `artefacts.id` and `artefacts.subscription_id` stay bare per the §2.9 carve-out (artefacts table deferred). go build clean. |
| ~~TD-NAV-001~~ | 2026-05-14 | ~~S3~~ | ~~Frontend / nav rail visibility~~ | ~~Dev Tools bucket visible in every env.~~ | ~~Production-style deploy shows dev pages to end users.~~ | ~~Register entry.~~ | **Resolved 2026-05-19.** Migration 219 adds `pages_tags.pages_tags_env_only` (NULL = visible everywhere; "dev" = only when BACKEND_ENV=dev) and tags `dev_tools`. `nav/registry.go` `LoadRegistry` reads `BACKEND_ENV` (default "dev") and drops tags whose `env_only` mismatches; pages under dropped tags are filtered out instead of erroring. Test added: `TestLoadRegistry_EnvFiltersDevToolsOutOfStaging` proves dev_tools tag + pages are dropped under `BACKEND_ENV=staging`. |
| ~~TD-TEST-001~~ | 2026-05-14 | ~~S2~~ | ~~Backend / test fixtures~~ | ~~Dead trigger `fn_master_record_tenant_seed_for_subscription` on `subscriptions` caused test failures.~~ | ~~Tests fail at mkTenant/mkSubscription.~~ | ~~Register entry.~~ | **Resolved 2026-05-19.** Migration 200 (already applied) dropped the broken trigger. Production hazard cleared. Residual fixture column drift resolved under TD-USERS-RENAME-FIXTURES + DPoP session column fix. |
| TD-NAME-002 | 2026-05-14 | S3 | HTTP / REST conventions | RF1.4.3 (route-rename sweep) shipped the singular→plural workspace routes but **deferred** the remaining §3.3 entries: (1) `/nav/bookmark` → `/nav/bookmarks` (frontend consumers in `PageBookmarkButton`, `app/redesign/*`); (2) `/user/tab-order/*` → `/me/tab-order/*` (`usertaborder` package + 3 frontend hooks); (3) `/admin/api-keys/issue|revoke` → REST-canonical `POST /admin/api-keys` + `DELETE /admin/api-keys/{id}`; (4) `/flow-states/{id}` → `/flows/{flowId}/states/{id}` (nested resource — needs flow-id resolver on every callsite); (5) `/errors/report` → `/error-reports` (noun, not verb); (6) `/tenant-settings` → `/workspace-settings` (verify the settings key is per-workspace before renaming, per §3.3 note). The B19.1 workspace route rename validated the playbook (transport-segregated, both /_site and /samantha/v2 surfaces updated, frontend wire updated in same commit); the remaining renames follow the same template but each touches 3–8 frontend files. | (a) An external SDK consumer reads `openapi-v2.yaml` (when published) and sees non-REST verbs (`/api-keys/issue`); (b) a new contributor adds another verb-route (`/api-keys/rotate`) thinking it's the house style; (c) the `flow-states` flat route blocks future per-flow state-machine work because the hierarchy isn't in the URL. | This register entry. The §3.3 table in `c_c_naming_conventions.md` flags each row as `Deferred (TD-NAME-002)` so the next route change in any of these areas surfaces the deferral. | One commit per route family (~6 small PLA cards or one combined `PLA-NNNN`-route-sweep card). Each commit: rename Go handler + httpx route, dual-mount old route with 301 redirect for one release cycle (or 410 Gone for `/samantha/v2/*`), update frontend callers, update openapi-v2.yaml, snapshot the API. Order: bookmarks → tab-order → api-keys (REST verbs) → flow-states (nested) → errors → tenant-settings (last, requires the rename-vs-keep call). Estimated cost: ~half a day per family. |
| TD-PG-001 | 2026-05-15 | S3 | Backend / page-access enforcement | PLA-0049 Phase 0.5 stood up `auth.RequirePageAccess(key_enum)` and `pageaccess.Resolver` and mounted them on `/admin/page-grants`. The plan's audit identified 19 page-bound route groups in `main.go` total; 18 are not yet gated. Per the locked grilling decision (option B in PLA-0049 Phase 0.5 grill), the mass mount was deferred so the remaining route groups can be wired alongside Phase 1 (grid CRUD churn) where there's a real test case for each. Cross-cutting groups also need a per-route gating decision: `/nav` (every page consumes it — gate at auth layer only), `/workspaces` (split list vs write?), `/custom-pages`, `/me`, `/errors`, `/samantha/v2/search`, `/rank`. | (a) Hand-typed URL or stale bookmark to a still-listed-in-rail page lands on a partially-gated UI: page renders, then API calls return mixed 200/403 depending on whether the route group has its own RequirePermission. (b) A new tenant-custom role is created with grants only on a subset of pages — the user can navigate via direct URL to pages they don't have a grant for. | This register entry. The pattern is in place: `pageAccessResolver` is constructed at boot and shared across the gated route groups. | Per-route group: add `r.Use(auth.RequirePageAccess(pageAccessResolver, "<key_enum>"))` after `RequirePermission` in main.go. Recommended order — start with the admin-heavy groups: `/admin/*` (3 sub-groups — split first), `/library/releases`, `/topology` (2 sub-groups — split first), `/roles`, `/workspace-settings`. Then planning surfaces: `/portfolio`, `/work-items`, `/portfolio-items`, `/timeboxes/*`, `/flows`, `/flow-states`, `/flow-state-exit-rules`, `/portfolio-models`, `/artefact-types`, `/workspaces/{id}/portfolio`, `/workspaces/{id}/fields`. Mirror to `/samantha/v2/*` siblings. Defer cross-cutting decisions until Phase 1 surfaces a need. |
| TD-PG-002 | 2026-05-15 | S3 | Frontend / page-access enforcement | PLA-0049 Phase 0.5 added `usePageAccess(key_enum)` and `PageAccessDenied` and wired them into `app/(user)/user-management/permissions/page.tsx` only. The plan's audit identified 34 mapped pages in `app/(user)/**/page.tsx` total; 33 are not yet hooked. Same deferral reason as TD-PG-001 (paired pay-down). Also: 41 unmapped/legacy pages are in the audit (mostly `/workspace-settings/**` orphans from mig 157) — those need to be either deleted or assigned a key_enum + page row. | Same as TD-PG-001 — hand-typed URL renders the page even when the user is denied. Frontend just shows a misleading skeleton + 403 errors on every API call instead of swapping to PageAccessDenied. | This register entry. PageAccessProvider is mounted in `app/layout.tsx`; every `(user)/**/page.tsx` can call `usePageAccess('<key_enum>')` with no further wiring. | Per-page: ~2-line addition (`const access = usePageAccess('<key>'); if (access.allowed === false) return <PageAccessDenied pageLabel='...' />;`). The mapping `route → key_enum` is in the audit findings inside `dev/plans/PLA-0049.json`. Companion cleanup: delete the 20 legacy `/workspace-settings/**` orphan page.tsx files (per Phase 0.5.9 in the plan). |
| ~~TD-TEST-002~~ | 2026-05-15 | ~~S2~~ | ~~Backend / test fixtures~~ | ~~Seven test files referenced retired rank-encoded role UUIDs + renamed permission codes; `go vet ./...` failed on 5 packages.~~ | ~~CI gate that runs `go test` would have failed on every users/roles/topology/workspaces/nav PR.~~ | ~~Register entry.~~ | **Resolved 2026-05-16.** `go vet ./...` now clean across the whole project. Fix shape per-package: (1) `users` — rewrote `TestTargetRoleCreateCode` to match the post-PLA-0049 3-role mapping (gadmin/padmin/user → grp_global/grp_portfolio/grp_team_member, deferred grp_* → ""), added `resolveGrpRoleID(t, pool, role)` helper that resolves grp_* UUIDs by code at fixture time, updated `mkUser`/`mkUserWithRoleID`, rewrote `TestProtectedAccountsPreserved` to JOIN `users_roles` (post-rename) and assert grp_* codes. (2) `roles` — rewrote `service_test.go` entirely: kept the `reservedSystemRanks` unit test (corrected to {10,20,30,40,50,60,70}), added two integration tests for `LoadSystemRoles` + `IsSystemRole` against the live DB; added `resolveGrpRoleID` helper in `handler_test.go` for the system-role-immutable DELETE assertion. (3) `topology` — `middleware_workspace_test.go` resolves grp_global by code, dropped the unused `internal/roles` import. (4) `workspaces` — fixed the stale `internal/master_record_workspaces` import (was meant to be `internal/workspaces` for the external `_test` package), added `resolveGrpRoleID` for `mkUser`; refreshed `TestVAWorkspaceTables_MatchesCanary` to the new {name, workspaceIDCol, archivedAtCol} struct shape (was `{name, hasArchivedAt bool}` pre-RF1.4.2) and updated `sprints` → `timeboxes_sprints`. (5) `nav` — `mkFixtures` returns `roleID` resolved from `grp_team_member`; updated all 6 `ReplacePrefs` + 4 `GetPrefs`/`GetStartPageHref` call sites to pass `roleID`; rewrote `TestCatalogFor_RoleFiltering` to resolve `grp_team_member` + `grp_global` UUIDs and pass them as the new `roleID uuid.UUID` first argument to `Registry.CatalogFor` (signature drift). Plus side-fix `libraryreleases/handler_test.go`: `NewService(libPool, vecPool)` → `NewService(libPool, vecPool, vecPool)` to match the new 3-pool signature. **Uncovered production bug filed as new TD-USERS-CREATE-001** below. |
| ~~TD-ROLE-001~~ | 2026-05-15 | ~~S3~~ | ~~Backend / authz gates~~ | ~~Eleven production sites string-compared against the legacy `users.role` enum instead of UUID-comparing against `roles.SystemGrpGlobalID`.~~ | ~~Migration Z drops `users.role` → all 11 gates start refusing every request.~~ | ~~Register entry.~~ | **Resolved 2026-05-19.** All 11 sites migrated to UUID comparison. Category A (5 sites — User in scope, straight swap): `portfoliomodels/handler_workspace_layers.go` x2, `portfolio/master_record_service.go` x2, `fields/service.go` x1. Category B (6 topology sites, signature change): `actorRole string` → `actorRoleID uuid.UUID` on `GrantRole`, `ListMyGrants`, `ListGrantsByUser`, `Commit`, `ResetCanvas`, `CanReadScope`; topology handler callers updated; `artefactitems.TopologyScopeResolver` interface + `artefactitems.Filters.ActorRoleID` field updated. Test scaffolding: TestMain in `portfolio` + `fields` + `portfoliomodels` packages seeds `roles.SystemGrpGlobalID` / `SystemGrpPortfolioID` (production `LoadSystemRoles` populates at boot; unit-mode tests bypass it). Migration Z can now proceed safely. |
| ~~TD-PLANKA-001~~ | 2026-05-15 | ~~S2~~ | ~~Tooling / story tracker~~ | ~~Planka retired but pervasive footprint (~63 files, helpers, hooks, .planka/ dir).~~ | ~~Skills+hooks blocked card creation; new contributors confused by stale workflow docs.~~ | ~~Suspended `<backlog>`; this register entry.~~ | **Resolved 2026-05-15 (same-session pay-down).** Decommission shipped: deleted `./.planka/`, `.claude/bin/planka*`, `.codex/bin/planka*`, both `planka-card-moved.sh` hooks, `.claude/memory/*planka*`, `docs/c_c_planka_rest.md`, `docs/archive/c_backlog_planka.md` + sibling backlog-agent docs. Rewrote `<stories>` and `<retro>` skills to write `dev/plans/PLA-NNNN.json` `work_item_backlog` entries with tag arrays (no external kanban). Removed `planka_card_id` from RetroFinding type + fixtures. Stripped Planka MCP perms from `.claude/settings.json` + `.local.json`. Cleaned dev/plans/*.json: 43 hardcoded card URLs scrubbed, kanban language replaced with "kanban board" in PLA-0001/0011 historical narrative. Docs sweep: CLAUDE.md, AGENTS.md, BACKLOG.md, c_tools_index.md, c_story_labels.md, c_story_labels_setup.md (stubbed), c_story_acceptance.md, c_feature_areas.md, c_retro_index.md, c_ssh.md (port 3333 dropped), c_addpaper*.md, c_backlog.md, c_accounts.md, c_services.md, c_retro.md, c_server.md, c_write-research-paper.md, dev/pages/DevShortcutsPanel.tsx + DevPlansPanel.tsx, app/api/dev/{services,plans,retros}/route.ts. Net 5 .claude/memory files still reference Planka — flagged for user follow-up because auto-mode classifier blocks self-modification of agent memory. |
| ~~TD-WS-001~~ | 2026-05-16 | ~~S2~~ | ~~Backend / workspace identity~~ | ~~`workspacemasterrecord/handler.go:41` passes `u.SubscriptionID` where `Service.Get` expects workspace_id; capped via fallback resolver in `inheritance_wiring.go:SubscriptionFor`.~~ | ~~Multi-workspace tenant would silently return wrong row; cleanup pass could remove fallback and break inheritance.~~ | ~~Fallback resolver in FDWSubscriptionResolver.SubscriptionFor.~~ | **Resolved 2026-05-16.** Locked the architectural principle first (Rick): **workspace scope is backend-only — no URL params, no pickers, no UUIDs in client code**. Memorialised in `.claude/memory/project_workspace_scope_invisible.md`. Rewire: (1) new `ActiveWorkspaceResolver` interface on the handler — `subscription_id → workspace_id`, single-row FDW lookup via `FDWActiveWorkspaceResolver`. (2) `Handler.Get`/`Handler.Patch` resolve via that interface before calling `Svc.Get`/`Svc.Patch`. (3) Defensive `subscription_id → subscription_id` fallback in `FDWSubscriptionResolver.SubscriptionFor` removed — single-purpose workspace_id lookup. (4) Wired in `cmd/server/main.go` via `workspaceSettingsH.WithResolver(...)`. TDD: 4 new handler tests in `handler_active_workspace_test.go` (red → green), full 12-test package suite still green. URL surface unchanged (`/_site/workspace-settings`, no path param). |
| ~~TD-TEST-003~~ | 2026-05-15 | ~~S3~~ | ~~Backend / trigger coverage~~ | ~~Master-record substrate shipped without trigger-bump coverage on `updated_at`.~~ | ~~Silent trigger drift.~~ | ~~Register entry + earlier 33 specs.~~ | **Resolved 2026-05-19.** Added `TestPatch_UpdatedAt_AdvancesOnValueChange` to both `tenantmasterrecord/service_test.go` and `workspacemasterrecord/service_inheritance_test.go`. Each test reads the pre-Patch updated_at, sleeps 2ms, applies a value Patch, asserts updated_at strictly advanced; then runs a no-op Patch and asserts updated_at did NOT advance (the no-op path short-circuits before UPDATE). Catches both kinds of trigger drift: BEFORE UPDATE trigger removed/renamed (value Patch fails to advance) AND the no-op-Patch short-circuit lost (extra writes when no fields supplied). |
| ~~TD-NAME-001~~ | 2026-05-14 | ~~S3~~ | ~~DB / naming convention~~ | ~~RF1.4.2 table-rename sweep deferred full column-prefix on 8 of 9 domains.~~ | ~~Inconsistent column shape across §2.6 root families; SDK consumers see mixed-prefix wire shapes.~~ | ~~`lint:column-prefix-convention` shipped warn-only with shrinking ledger.~~ | **Resolved 2026-05-14 (same day — full pay-down).** Nine migrations shipped across mmff_vector + vector_artefacts, 245 → 0 findings, 9 → 0 packages. Migrations: 186 (users_password_resets) → 063 (master_record_tenants) → 187 (users_sessions) → 064 (artefacts_fields_values + artefactitemsv2→artefactitems Go-package rename) → 188 (users_roles_workspaces) → 189 (RBAC triangle: users_roles + users_permissions + users_roles_permissions) → 065 (flows family, 7 tables) → 066 (artefacts_types) → 190 (users_nav family, 5 tables). §2.4 FK shapes applied throughout (`<table>_id_<target>` and multi-FK role suffix on granted_by / revoked_by / created_by). `lint:column-prefix-convention` flipped to fail-on-violation; ledger empty. Pay-down log preserved in `dev/registries/column_prefix_exempt.json` $comment block. The `artefacts` parent table (one specific table, distinct from `artefacts_types` / `artefacts_fields_values`) plus the `users` core table remain bare — those are the JSON wire-tag dependencies §2.9 explicitly defers; tracked separately when the frontend wire-tag contract changes. |
| ~~TD-WORKITEMS-GENERIC~~ | 2026-05-16 | ~~S2~~ | ~~Backend / artefactitems~~ | ~~`WorkItemsSummary` carried fixed-shape `.Epics/.Stories/.Tasks/.Defects/.Risks` fields alongside `.ByType`; sort by item_type was a hardcoded CASE WHEN that grew a line per type.~~ | ~~Every new artefact type forced a 4-file Go change.~~ | ~~Register entry.~~ | **Resolved 2026-05-16 (same day, opportunistically — PLA-0052's `.Risks` addition was the trigger).** (a) Deleted all per-type fixed-shape fields from `WorkItemsSummary`; the struct is now `{Total, Blocked, ByType map[string]int}`. (b) Replaced the CASE WHEN sort with `at.artefacts_types_sort_order ASC NULLS LAST` — no migration needed, the column already existed from RF1.4.2. (c) Migrated `app/(user)/work-items/page.tsx` to read `by_type[<key>]` via the typed summary fetch; portfolio-items page was already on the right pattern. (d) Updated the two existing test cases (`TestSummariseWorkItems_CountsWorkScoped`, `TestSummariseWorkItems_PopulatesRisksField`) to assert ByType-only shape; PLA-0052's reflection-based `.Risks` probe was simplified to a direct map lookup. Live wire shape verified via Playwright + network capture: `{"total":55674,"blocked":0,"by_type":{"defect":2757,"epic":503,"story":5705,"task":46709}}`. PLA-0052 Risk type now lands by seed-only: no Go change required for the type itself (only the new `/risks/summary` matrix endpoint stays as bespoke PLA-0052 surface). |
| ~~TD-WORKITEMS-DUPE~~ | 2026-05-16 | ~~S3~~ | ~~Frontend / custom-fields admin~~ | ~~Two near-identical custom-fields admin pages with hardcoded ITEM_TYPES + CORE_FIELDS lists; PLA-0052 doubles the maintenance cost.~~ | ~~Every artefact-type addition forces a 2-place edit; drift goes unnoticed.~~ | ~~Register entry.~~ | **Resolved 2026-05-16.** Shared catalogue extracted to `app/lib/work-item-types.ts` (ITEM_TYPES, ItemTypeKey, CoreField, CORE_FIELDS). The full body (which was byte-identical except for one PageHeading subtitle) extracted to `app/components/CustomFieldsWorkItemsBody.tsx`. Both `page.tsx` files are now 5-line shells passing only `subtitle`. Adding a new artefact type (e.g. `risk` per PLA-0052) now requires editing one file — `work-item-types.ts`. Confirmed via Playwright that the live `/workspace-admin/custom-fields/work-items` page renders with H1, all 4 type pills (EP/US/TA/DE), and the core-fields table populated. Sibling at `/workspace-settings/workspace-settings/custom-fields/work-items` is dead-code (308-redirected by `next.config.ts:53` to the canonical URL) — kept on disk for now since it compiles cleanly through the shared body; flagged as a candidate for the next URL-hygiene sweep. Type-check clean. |
| ~~TD-SEED-FN-DRIFT~~ | 2026-05-16 | ~~S2~~ | ~~DB / vector_artefacts seed function~~ | ~~`seed_system_artefact_types(UUID)` referenced pre-RF1.4.4 column names; effectively dead code.~~ | ~~Re-run seed for new tenant → immediate failure.~~ | ~~This register entry. Mig 071 uses direct-INSERT pattern.~~ | **Resolved 2026-05-16.** Converted to a safe no-op stub (`RAISE NOTICE` only); original body referencing pre-RF1.4.4 column names removed. No caller exists; stub satisfies the symbol without behaviour. Verified via DB backup commentary 2026-05-17. |
| ~~TD-SUMMARY-TONE~~ | 2026-05-16 | ~~S3~~ | ~~Frontend / PageSummaryHeader~~ | ~~`SummaryCellTone` was `"neutral" \| "warning"` only; no `"danger"` variant for critical counts.~~ | ~~Risk page can't distinguish critical from high-severity.~~ | ~~This register entry.~~ | **Resolved 2026-05-17.** Added `"danger"` to `SummaryCellTone`; refactored `cellIsIssue` → `cellModifier` returning the appropriate CSS modifier class; added `.page-summary__cell--critical .page-summary__value { color: var(--status-danger) }` to globals.css. TSX type-check clean. |
| ~~TD-RISK-PAGE-TAG~~ | 2026-05-16 | ~~S3~~ | ~~DB / pages catalog~~ | ~~pages.risk row had tag_enum='strategy' but the host surface (/work-items) uses 'planning'.~~ | ~~Wrong rail grouping by tag.~~ | ~~Register entry.~~ | **Resolved 2026-05-19.** Migration 218 (`218_risk_page_tag_planning.sql`) sets `tag_enum='planning'` on the risk row. Applied to dev DB; will propagate to staging/prod on next migrate run. |
| ~~TD-USERS-CREATE-001~~ | 2026-05-16 | ~~S2~~ | ~~Backend / users service~~ | ~~`users.Service.Create` passed legacy enum role string as the `users_roles_code` subquery param; PLA-0049 renamed codes to `grp_*`, causing NOT NULL violation on `role_id` for every production user-create.~~ | n/a — closed. | n/a — closed. | **Resolved 2026-05-17.** Added `legacyRoleToGrpCode(role roletypes.Role) string` helper in `service.go` mapping gadmin→grp_global, padmin→grp_portfolio, user→grp_team_member (future grp_* codes pass through). Applied to `Create` param `$5` and `Update`'s `sqlUpdateUserRoleIDFragmentTemplate` param. `TestCreate_GAdminCanCreateAnyRole` (red → green), `TestUpdate_RoleChange_RevokesSessions` (also had stale fixture column `user_id` → fixed to `users_sessions_id_user`). Full 16-test users package GREEN. |
| ~~TD-FILTER-MULTI~~ | 2026-05-16 | ~~S3~~ | ~~Backend / artefactitems filters~~ | ~~Frontend chip multi-selected but capped to first value; backend single-value.~~ | ~~User sees only Epics when both Epic+Story selected.~~ | ~~Sentence comment in filterQuery.~~ | **Resolved 2026-05-19 (audit).** Already paid down by PLA-0054 (item_type slug→UUID list) + PLA-0055 (priority slug→UUID list). Backend `Filters` struct uses `[]uuid.UUID` for ItemType/Status/Priority/OwnerID; handler parses comma lists via `parseUUIDList` and emits `ANY($N::uuid[])`. Frontend `filterQuery` at `work-items-tree-config.tsx:837` round-trips the full selection (`item_type_id=`, `flow_state_id=`, `priority_id=`, `owner_id=`) with no `[0]` cap. TD entry was stale. |
| ~~TD-TOPOLOGY-WS-TESTHELPERS~~ | 2026-05-16 | ~~S3~~ | ~~Backend / topology test helpers~~ | ~~`users_roles_workspaces` column renames not propagated to topology test helpers.~~ | ~~`TestWorkspaceClamp_LiveDB_PassesThrough` permanently skipped.~~ | ~~`t.Skip` guard.~~ | **Resolved 2026-05-19.** Four sites updated: `mkTenant` cleanup DELETE, `seedWorkspaceRole` INSERT + revoke UPDATE, `TestPoolWorkspaceLookup_HasActiveRole` inline UPDATE. `t.Skip` removed; `TestWorkspaceClamp_LiveDB_PassesThrough` rewritten to use `runClamp` harness with `?ws=<slug>`. Full topology suite green. |
| ~~TD-RG-RUNNER-DOCS~~ | 2026-05-16 | ~~S2~~ | ~~Tooling / Tracker rg-runner~~ | ~~SOP declared a proposed flag shape (`--feature/--story/--status/--commit`) without verifying against the actual runner in the sibling Tracker repo.~~ | n/a — closed. | n/a — closed. | **Resolved 2026-05-16.** Ran `cd "MMFFDev - Tracker/backend" && go run ./cmd/rg-runner --help` and reshaped SKILL.md §5.d to the verified contract. Actual runner is simpler than R055 proposed: takes `-api-key`/`-target`/`-group`/`-tracker-url`/`-force`/`-dry-run` (env fallbacks `RG_API_KEY`/`RG_TARGET`/`RG_TRACKER_URL`). Groups are defined IN Tracker (each carries a `framework` of `go` or `vitest`); the runner spawns the framework's JSON reporter against `-target` and streams per-test results. There is no `--status`/`--story`/`--commit` flag — red/green is derived from the test stream. Side-effect: added a "group MUST exist in Tracker before first commit" check to Step 0 sub-step 8 (otherwise `rg-runner` errors with `group not found`). |
| ~~TD-RG-RERUN-WIRING~~ | 2026-05-16 | ~~S2~~ | ~~CI / regression-rerun gate~~ | ~~SOP assumes a regression-rerun gate exists; no wiring in Vector pre-push or CI when SOP shipped.~~ | n/a — closed. | n/a — closed. | **Resolved 2026-05-16.** Audit confirmed Vector had zero `rg-runner` integration (grep returned only SOP docs). Built `dev/scripts/rg-rerun.sh` as a soft-mode-capable wrapper around the runner: resolves the sibling Tracker repo via `RG_TRACKER_REPO` (defaults to `../MMFFDev - Tracker`), checks `RG_API_KEY` is set, probes Tracker reachability with a 2s curl, then invokes `cmd/rg-runner -group ${RG_GROUP:-all} -target $REPO_ROOT`. `SKIP_RG_RERUN=1` bypasses. Extended `dev/git-hooks/pre-push` to call `rg-rerun.sh --soft` after the backup step — fail-open (Tracker unreachable / API key missing / runner exits non-zero all reduce to exit 0, never blocks `git push`). Strict-mode (blocking) gate left as opt-in for the user to enable when ready (drop `--soft`). |
| ~~TD-PLANDOC-FEATURE-SOP~~ | 2026-05-16 | ~~S2~~ | ~~Backend / PlanDoc type~~ | ~~PlanDoc + PlanWorkItem types in app/api/dev/plans/route.ts did not declare the new SOP schema fields (`kind`, `feature_id`, `feature_name`, `covers`, `tracker_group`) or the long-undeclared `tags` array.~~ | n/a — closed. | n/a — closed. | **Resolved 2026-05-16.** Extended `PlanWorkItem` with `description?`, `tags?`, `kind?: "implementation" \| "feature_test"`, `feature_id?`, `feature_name?`, `covers?`, `tracker_group?`. Extended `PlanDoc` with top-level `tracker_group?`. All fields optional so plans authored before the SOP migration script (`dev/scripts/migrate-plans-to-feature-sop.sh`) still type-check. Audit found one consumer: `dev/pages/DevPlansPanel.tsx`. Updated to: surface `tracker_group` in the meta-strip; render a new "6.3.1 Feature Coverage" section that derives feature rows via a new `deriveFeatures(plan)` helper (canonical source = feature_test entries; FEAT-N tag backfill for grandfathered plans); extend the work-item table with `Kind` (test/impl pill) and `Feat` (`F<N>`) columns + a `Tags` column. New section added to `PLAN_TOC`. |
| ~~TD-USERS-RENAME-FIXTURES~~ | 2026-05-16 | ~~S2~~ | ~~Backend / test fixtures~~ | ~~RF1.4.2.users column renames not propagated to test fixtures across roles/users/workspaces.~~ | ~~Noisy go test ./...~~ | ~~Register entry.~~ | **Resolved 2026-05-19.** Bulk column-name swap across `roles/{handler_test,cache_invalidation_test,audit_smoke_test}.go`, `users/service_test.go`, `workspaces/{handler_test,crossdb_integration_test}.go`. Also fixed: audit smoke pool → vaPool (audit_logs in vector_artefacts), session fixture missing `users_sessions_dpop_jkt`, workspaces DELETE route missing from test router. roles/users/workspaces packages all green. |
| ~~TD-ARTEFACTS-WORKSPACE-COLUMN~~ | 2026-05-16 | ~~S2~~ | ~~Backend / test fixtures~~ | ~~`artefacts_types` + `master_record_portfolios` column renames not propagated to test fixtures.~~ | ~~Noisy go test ./...~~ | ~~Register entry.~~ | **Resolved 2026-05-19.** Workspaces slice (`crossdb_integration_test.go`) + portfoliomodels slice (`adopt_strategy_types_test.go`, `adopt_work_types_test.go`, `cross_db_canary_test.go`, `scope_resolver_matrix_test.go`, `adopt_flows_test.go`, `adopt_master_record_test.go`, `adopt_readopt_test.go`) all bulk-renamed to the `artefacts_types_*` / `master_record_portfolios_*` / `flows_*` / `flows_states_*` / `flows_transitions_*` prefixes. Also fixed: `adopt_work_types_test.go` constraint-name typo (`artefact_types_work_no_parent` → `artefacts_types_work_no_parent`); placeholder-artefact INSERT now supplies `priority_id` via subquery (production fix in `sqlUpsertReadoptPlaceholderArtefact`); readoption test seeds an `artefact_priorities` row. |
| ~~TD-TIMEBOXES-VELOCITY-NOTNULL~~ | 2026-05-16 | ~~S2~~ | ~~Backend / test fixtures~~ | ~~22 sprint tests fail with NOT NULL violation on timeboxes_sprints_velocity.~~ | ~~Hides real timeboxsprints regressions.~~ | ~~Register entry.~~ | **Resolved 2026-05-19.** Root cause: `Create` + `BulkCreate` pass `*int` velocity to pgx which sends NULL when nil, overriding the `DEFAULT 0`. Fixed in `service.go`: nil pointer → 0 before the query param. All 22 timeboxsprints tests green. |
| ~~TD-LIBRARY-TEST-DB~~ | 2026-05-16 | ~~S3~~ | ~~Backend / test env hygiene~~ | ~~librarydb + libraryreleases tests failed with "relation does not exist".~~ | ~~New contributor noise.~~ | ~~Register entry.~~ | **Resolved 2026-05-19.** Not actually an env-hygiene issue — root cause was code drift: librarydb still referenced dropped `portfolio_models*` tables (closed by TD-LIB-010), and `library_releases_acknowledgements` lives in vector_artefacts not mmff_library (test-pool wiring fixed by TD-RF1-TEST-COLUMN-RENAME-DRIFT). Library DB itself is current. |
| ~~TD-TOPOLOGY-BOUNDARY-PORTFOLIOMODELS~~ | 2026-05-16 | ~~S2~~ | ~~Backend / topology boundary~~ | ~~`portfoliomodels/sql.go` contained 3 raw topology-write SQL constants.~~ | n/a — closed. | n/a — closed. | **Resolved 2026-05-17 (sibling pass with TD-TOP-001).** The 3 constants were the same delete/seed sequence; `portfoliomodels/dev_reset.go` was already rewritten to call `topology.Service.PurgeTenantTopologyData` + `topology.Service.SeedRootNode`. No stale topology SQL remains in portfoliomodels. Boundary test GREEN. |
| ~~TD-RISK-NUMBER-SEQUENCE~~ | 2026-05-16 | ~~S2~~ | ~~Backend / artefacts number allocation~~ | ~~RSK sequence in `artefacts_number_sequences` was at 36; seeded rows went to 200, causing duplicate key violations.~~ | ~~Test permanently red.~~ | ~~Register entry.~~ | **Resolved 2026-05-19.** Dev-DB RSK sequence reset to 201 (one-shot). Durable fix: `sqlAllocateArtefactNumber` now initialises `next_num` from `MAX(number)+2` instead of hardcoded `2`, and on conflict uses `GREATEST(next_num+1, MAX+2)` — making the allocator self-healing when rows are seeded outside the allocator path. |
| ~~TD-FIELDS-WSPERMS-RENAME~~ | 2026-05-16 | ~~S2~~ | ~~Backend / fields service SQL~~ | ~~Production SQL in fields/sql.go::sqlExistsActiveWorkspaceMembership referenced pre-RF1.4.4 column names; non-admin users hit 500 on GET /api/workspace/{id}/fields.~~ | n/a — closed. | n/a — closed. | **Resolved 2026-05-16.** Single-file SQL edit: `user_id` → `users_roles_workspaces_id_user`, `workspace_id` → `users_roles_workspaces_id_workspace`, `revoked_at` → `users_roles_workspaces_revoked_at` per mig 188 mapping. `TestList_NonMember_Returns403` GREEN; full fields package GREEN. Discovered via triage of test-failure inventory; turnaround was the same ~10 minutes the entry estimated. |
| ~~TD-ERRORSREPORT-WIRE-SHAPE~~ | 2026-05-16 | ~~S3~~ | ~~Backend / errorsreport test~~ | ~~`TestReport_UnknownCode` decoded into `map[string]string` and asserted `out["error"]`; handler emits RFC 9457 `detail` field.~~ | ~~Test stayed red; hid wire regressions.~~ | ~~This register entry.~~ | **Resolved 2026-05-17.** Rewrote `TestReport_UnknownCode` to decode into `struct{ Detail string }` and assert `out.Detail == "unknown_error_code"`. `go build ./internal/errorsreport/...` clean. |
| ~~TD-NAV-DEV-ITEM-RENAMED~~ | 2026-05-16 | ~~S3~~ | ~~Backend / nav catalogue test~~ | ~~`TestReplacePrefs_RejectsDevSetup` asserted `ErrNotPinnable` for `ItemKey: "dev"` but mig 158 deleted the `dev` page row — rejection fires as `ErrUnknownItemKey` instead.~~ | ~~Test stays red; hides real regressions.~~ | ~~This register entry.~~ | **Resolved 2026-05-17.** Deleted `TestReplacePrefs_RejectsDevSetup` — mig 158 removed `dev` from the catalogue entirely and replaced it with individually pinnable `dev-*` sub-pages, so no non-pinnable analogue exists. The unknown-key rejection path is already covered by the sibling `TestReplacePrefs_RejectsUnknownKey`. `go build ./internal/nav/...` clean. |
| TD-MYGRANTS-HANDLER-TEST | 2026-05-17 | S3 | Backend / topology handler test | `Handler.MyGrants` powers the FE-POR-0002 Chrome Scope Picker; no handler-level test exists. **Partial pay-down 2026-05-19:** added `TestListMyGrants_GadminDispatch` to service_test.go pinning the gadmin-detection sentinel (panics-on-nil-pool reveal which branch was taken — gadmin path via `listMyGrantsGadmin`, non-gadmin via `sqlListMyGrants`). This catches regressions in the dispatch decision itself (e.g. a refactor that accidentally routes gadmin actors through the per-user grant SQL). Still missing: handler-level DTO-shape + auth-context tests (cases a/b/c/d in original entry). | First regression that silently breaks the dispatch decision or DTO shape — anything from a field-rename in `MyGrant` DTO, a stray guard added to the handler, a route deregistration, or the gadmin branch returning `[]` instead of synth-grants. | This register entry + `TestListMyGrants_GadminDispatch`. The service has structural tests via the sole-writer boundary suite (`boundary_test.go`); the SQL is covered by package compilation. Handler is a thin wrapper — pure JSON-out. | Remaining work: add `handler_my_grants_test.go` covering (a) 200 + correct DTO shape for an authed non-gadmin actor with at least one grant; (b) 200 + non-empty synth-grants array for a gadmin actor; (c) 200 + empty array when actor has no grants (picker hides path); (d) 401/redirect when no actor in context. Needs a DB-backed test harness in `internal/topology/` first — none exists today (the package's existing tests all short-circuit before any pgx call). Pair with the next test-infrastructure touch. ~45 minutes plus harness scaffolding. |

| TD-ARTEFACT-TYPES-RAW-TABLE | 2026-05-17 | S3 | Frontend / workspace-admin | `app/(user)/workspace-admin/artefact-types/page.tsx` uses a raw `<table className="table">` inside `TypeTable` instead of `<Table>`. Exempted provisional in `raw_table_exempt.json`. Inline-edit rows (ColourPicker, InlineEditField) need `useDraft` integration before migrating. | Pre-existing; surfaced when `useActiveWorkspace` was added to the file. | `raw_table_exempt.json` provisional entry. | Migrate `TypeTable` to `<Table>` with inline-edit `useDraft` pattern; remove exemption. Pay down when artefact-types page is next substantially touched. ~1h. |
| ~~TD-SEC-REFRESH-ROTATION~~ | 2026-05-18 | ~~S2~~ | ~~Auth / session security~~ | ~~Refresh tokens not single-use; stolen tokens replayable for the lifetime of the refresh window.~~ | n/a — closed. | n/a — closed. | **Resolved — already implemented.** Verified in `backend/internal/auth/service.go::Refresh` (line 337): every refresh rotates the token (revokes old, inserts new — `sqlRotateSession` stamps `rotated_at` + `successor_hash`); revoked-token replay outside a 30s grace window triggers `sqlRevokeAllUserSessions(userID)` which kills the entire session family and emits an `auth.refresh_token_reuse` audit row. End-to-end test 2026-05-18: login → refresh → wait 32s → replay original → 401 + session-family kill + 2 audit rows (original attack + successor revocation). The grace window is the pragmatic mitigation against false-positive kills from duplicate tabs / HMR replaying the same cookie within ~30s. Schema lived in the existing `users_sessions` table (not a new `users_sessions_refresh` table — the design was simpler than the TD entry assumed). No new commit; the work landed before this TD entry was filed. |
| ~~TD-SEC-DPOP-BINDING~~ | 2026-05-18 | ~~S2~~ | ~~Auth / session security~~ | ~~Bearer tokens have no device binding. A stolen access or refresh token works from any device, any IP, any browser.~~ | n/a — closed. | n/a — closed. | **Resolved 2026-05-18 (six-phase ship, commits `8722a54` → `dd8131c` + migration 213 cutover).** Every access token carries an RFC 9449 § 5 `cnf.jkt` claim bound to a browser-resident **non-extractable** ECDSA P-256 (Chrome) or RSA-2048 (Firefox/Safari) keypair generated via `crypto.subtle.generateKey({extractable:false})` and persisted in IndexedDB. Every authed HTTP request must carry a `DPoP:` header whose proof JWT (RFC 9449 § 4) the backend validates against `htm`, `htu`, `iat` (±60s), `ath` (SHA-256 of access token), `cnf.jkt` thumbprint (RFC 7638), and `jti` is reserved in the Postgres `dpop_jti_cache` table (mig 212) for replay prevention. WebSocket transport (Phase 5) carries the proof via `?dpop=` query param because browsers can't set headers on WS handshakes. Refresh-token binding (Phase 4): a refresh request whose proof's thumbprint differs from `users_sessions.users_sessions_dpop_jkt` revokes the entire session family and audit-logs `auth.refresh_dpop_binding_violation` with both thumbprints. Migration 213 (Phase 6 cutover) made `users_sessions_dpop_jkt` NOT NULL and wiped all pre-existing sessions — every session from this point on is DPoP-bound; the middleware now rejects tokens missing `cnf.jkt` unconditionally. **Audit narrative:** NIST 800-63B AAL2/AAL3 (phishing-resistant authenticator at the session layer), RFC 9449 (full conformance including refresh-token binding), FFIEC 2021 § III.B (layered defence: short TTL + rotation + cnf binding), DoD CMMC L2/L3 (cryptographic device-binding for session reauth), SOC 2 CC6.1 (logical access — non-extractable key prevents in-memory token replay). Live-attack-tested 2026-05-18: K1 logs in, K2-signed refresh-replay → 401 + session family revoked + audit row matched. JTI cache shape verified via psql cookbook entry. Latent bug surfaced + fixed: WS origin matcher must include port (pre-existing PLA-0010 oversight). Procurement-ready answer to "what stops a stolen token being replayed from another device?" — non-extractable cryptographic key binding, evidence available on request. |
| TD-SEC-DPOP-STALE-KEY | 2026-05-31 | S2 | Auth / session security | **Resolved at source 2026-05-31** (regression-tested fix; see Pay-down). Root cause of "logged out / page loads blank whenever I switch browser tabs and come back." The DPoP keypair store (`app/lib/dpopStore.ts`, IndexedDB `vector-dpop`) is keyed by `userId` and was only ever deleted one-at-a-time (logout, reparent). A session ending WITHOUT a clean logout — tab close, crash, backend-forced revoke, or the put-succeeds-delete-fails branch in `reparentKeyRecord` — orphaned its keypair in IDB. `ensureAnyActiveKeypair` (bootstrap + tab-refocus path) then picked an **arbitrary** real-user record via `Array.find`; when it picked an orphan whose jkt was never bound to the current session, `/auth/refresh` failed the backend's `cnf.jkt` binding check (`backend/internal/auth/service.go:709`), revoked the entire session family, and emitted `auth.refresh_dpop_binding_violation` → frontend `hardLogout` → logout / blank-content-on-refocus. **Diagnosed from the live audit log:** binding violations firing daily, the SAME stale `incoming_jkt` (`3lsPmZ7VKCh…`) against an ever-changing `bound_jkt` — proof of one orphaned key being reused across sessions. | Residual A — bootstrap selection is **newest-wins by `createdAt`, not jkt-verified** against the session's `cnf.jkt`. Correct in practice (the freshly-bound key is always newest) but it's a heuristic, not a proof. Bites if a future flow could persist a newer-but-unbound key before refresh resolves. Residual B — the binding-violation is a **terminal revoke-all** even on benign client-side key drift; a single stale-key bootstrap nukes every session for that user across all devices. | **Fixed** (closes the accumulation at the source): (1) `pruneKeysExcept(keepUserId)` added to `dpopStore.ts` + `pruneStaleKeys` wrapper in `dpop.ts` — `AuthContext.applyLogin` prunes all-but-current after every login/refresh once identity is known, so the store converges to exactly one record per browser. (2) `ensureAnyActiveKeypair` now selects the most-recent real-user key by `createdAt` instead of `Array.find`, so an orphan can never beat the freshly-bound key even before the next prune. Regression tests: `app/lib/__tests__/dpop.test.ts` § "stale-key selection (TD-SEC-DPOP-STALE-KEY)" (3 tests; full file 18/18 green). | Residual A: on bootstrap, after `/auth/refresh` returns the access token, decode its `cnf.jkt` and assert the active key's jkt matches; on mismatch, hard-reset the keypair + re-mint rather than trusting newest-wins. Residual B: consider downgrading the refresh-time binding mismatch from revoke-ALL-sessions to revoke-only-the-offending-session-family + single-device re-auth, so one stale browser key can't sign every device out — weigh against the stolen-refresh-token threat model (TD-SEC-DPOP-BINDING) before changing. Trigger to pay down: any further report of spurious logout, OR before a multi-device GA where Residual B's blast radius matters. |
| ~~TD-SEC-CSP-NONCES-SRI~~ | 2026-05-18 | ~~S1~~ | ~~Frontend / XSS surface~~ | ~~No strict Content-Security-Policy; script-src carried `'unsafe-inline'` + dev `'unsafe-eval'`.~~ | n/a — closed. | n/a — closed. | **Resolved 2026-05-18 (commits 1ce3607 + a97a078).** Per-request nonce CSP shipped via `middleware.ts` with `'strict-dynamic'`; `'unsafe-inline'` dropped from script-src. Theme bootstrap inline `<script>` consumes `x-csp-nonce` via `headers()`; Next.js 15 auto-stamps framework chunks from the same header. `/_site/csp-report` endpoint persists violations to `csp_reports` (mig 209), drops `chrome-extension://` noise, is CSRF-exempt + rate-limited 120/min/IP. Soak in Report-Only completed with zero violations across login → dashboard → portfolio-items → topology → theme switcher; flipped to enforce same day. SRI on third-party scripts is N/A (no CDN scripts shipped). `style-src 'unsafe-inline'` retained as transitional measure — see TD-SEC-CSP-STYLE-INLINE for the cosmetic follow-up (S3, blocked on retiring 96 inline `style={{}}` props + handling React Flow / styled-jsx dynamic styles). |
| ~~TD-SEC-CSP-STYLE-INLINE~~ | 2026-05-18 | ~~S3~~ | ~~Frontend / CSP style-src~~ | ~~`style-src 'unsafe-inline'` was the last `'unsafe-inline'` directive in the Content-Security-Policy.~~ | n/a — closed. | n/a — closed. | **Resolved 2026-05-18 (commit cb4c301).** Audit found only 3 real `style-src` blockers (3 `<style jsx>` blocks); the 96 inline `style={{}}` JSX props compile to inline `style=""` attributes which `style-src-attr` gates (default permissive), not `style-src` — they were never an XSS surface. React Flow ships CSS via a normal stylesheet (allowed under `'self'`), no special handling needed. The three `<style jsx>` blocks (redesign/page, flow-states-v2, circularAdditor) migrated to globals.css; `${transitionMs}` interpolation in circularAdditor became CSS custom property `--aftd-transition-ms` set via inline `style={{}}` on the wrapper (still `style-src-attr` surface). Production `style-src` is now `'self' 'nonce-{n}' fonts.googleapis.com`. Dev keeps `'unsafe-inline'` because Next.js dev devtools inject inline styles with no nonce hook — same conditional pattern as `'unsafe-eval'`. Also fixed `report-uri` to be an absolute backend URL (browsers fire CSP reports directly, no Next.js proxy hop). Inline `style={{}}` cleanup remains a separate CLAUDE.md cleanup (CSS discipline, not security). |
| ~~TD-SEC-SESSION-ANOMALY~~ | 2026-05-18 | ~~S3~~ | ~~Auth / session telemetry~~ | ~~No backend session-anomaly detection. IP / ASN / country / UA change between refreshes is invisible.~~ | n/a — closed. | n/a — closed. | **Resolved 2026-05-18 — Stage 1 telemetry + Stage 2 enforcement shipped together.** Migration 214 adds `users_sessions_first_ip / _asn / _country / _ua_fp` columns; every login + MFA-verify + trusted-device + workspace-switch stamps a fresh fingerprint baseline and emits an audit_logs row in `vector_artefacts` carrying `{ip, ua, ua_fp (base64url SHA-256), country, asn, session_id}`. Refresh-token rotation inherits the parent's first_country / first_asn so the drift baseline stays anchored at the original login, NOT the rotation point. New `backend/internal/geo` package wraps MaxMind GeoLite2-City + GeoLite2-ASN lookups via `oschwald/geoip2-golang`; nil-safe on missing .mmdb (dev fail-open). **Stage 2 enforcement (TD entry's "step-up" rewording):** silent /auth/refresh can't pop a modal, so drift triggers a HARD logout instead — revoke session family, audit `auth.refresh_session_anomaly` with both bound + incoming fingerprints, return 401 + `Problem.Code=session_anomaly`. Frontend recognises the new terminal code, hardLogout cascade redirects to /login with "We detected a change in your network location" banner. Procurement-ready user flow: unusual location → re-authenticate from new location (the re-auth itself proves credentials), audit trail captures the event. **Audit narrative:** NIST 800-53 AU-12 (audit generation), FFIEC 2021 § III.B (layered behavioural controls), SOC 2 CC6.1 (logical-access detection commensurate with risk), PCI-DSS 4.0 § 10.2 (event logging with sufficient detail). **Operational notes:** dev defaults to fail-open (no .mmdb files = no drift signal); production deploys must provision GeoLite2-City.mmdb + GeoLite2-ASN.mmdb per [backend/data/geoip/README.md](../backend/data/geoip/README.md) and set GEOIP_CITY_DB + GEOIP_ASN_DB env. UA fingerprint is currently logged but not enforced — added to the audit row for future drift-rule expansion once we observe baseline UA-change patterns in soak data. TLS-fingerprint (JA3/JA4) explicitly out of scope — that would require unwrapping the TLS handshake before chi routing, which is a much bigger surface than this TD entry covers. |
| TD-SEC-WEBAUTHN-STEPUP | 2026-05-18 | S3 | Auth / step-up | No phishing-resistant step-up auth for privileged actions (delete workspace, change permissions, export data, rotate API keys). Defence customers expect FIDO2/YubiKey support (NIST 800-63B AAL3); finance expects WebAuthn for high-value flows. | First defence customer in pre-sales asks about hardware token support. Or first audit finding flags step-up auth missing on privileged paths. | This register entry. RBAC catalogue at `internal/permissions/catalogue.go` is the gate for which actions count as "privileged". | Add WebAuthn registration + verification flow; gate every permission tagged `requires_step_up` behind a fresh-WebAuthn challenge (TTL ~5 min, scoped to the action). Existing TOTP MFA covers login; this layer covers sensitive in-session actions. ~2 weeks. |
| ~~TD-SEC-RESET-TOKEN-FRAGMENT~~ | 2026-05-18 | ~~S2~~ | ~~Auth / reset flow~~ | ~~Password-reset email links carried `?token=<raw>` in the query string.~~ | n/a — closed. | n/a — closed. | **Resolved 2026-05-18 (cookie handoff — gold-standard path).** Email link now emits `:5100/_site/auth/password-reset/redeem?t=<raw>` — a Go endpoint that validates the raw token server-side, mints a 5-min HttpOnly JWT cookie carrying only the `reset_id`, and 302s to `/login/reset/confirm`. The user-visible URL after the redirect is the clean path; raw token never lands in JS, address bar, history, Referer, or proxy logs past the single inbound GET. Frontend `/state` probe checks cookie liveness on mount; `/confirm` POST reads the cookie (back-compat raw-token-in-body retained for any non-browser caller). ResetHandoffClaims JWT `kind: "reset_handoff"` discriminator prevents access-token replay. Cookie: `Path=/_site/auth/password-reset`, `HttpOnly`, `Secure` in prod, `SameSite=Lax` (must survive cross-port redirect from :5100 to :5101). End-to-end test 2026-05-18: redeem 302+cookie → /state 200 → /confirm 204 → row marked used → replay 400; claude_3_test password restored post-test. |
| ~~TD-SEC-LOGIN-REDIRECT-COOKIE~~ | 2026-05-18 | ~~S3~~ | ~~Auth / redirect handling~~ | ~~`/login?redirect=<path>` lets an unauthenticated user be sent back to their requested route after sign-in.~~ | n/a — closed. | n/a — closed. | **Resolved 2026-05-18 (cookie handoff — mirrors TD-SEC-RESET-TOKEN-FRAGMENT pattern).** Next.js middleware no longer writes `url.searchParams.set("redirect", ...)`. Instead it 302s the unauthenticated request to `:5100/_site/auth/login-required?p=<path>`. The Go endpoint validates `p` (same-origin relative, no `//` / `\\` prefix, no `/v2/`), mints a 10-min HttpOnly `LoginContinuationClaims` JWT cookie carrying only the signed path, and 302s to a plain `/login`. After successful auth, the frontend probes `GET /_site/auth/login-continuation` — the handler verifies the JWT, returns `{ path }`, and clears the cookie atomically (single-use). 204 means no continuation; fall through to `/nav/start-page`. The path never appears in the user-visible address bar past the inbound middleware hop, JS never touches the cookie (HttpOnly), and a scanner POSTing hostile paths either fails `isSafeContinuationPath` validation or lands a cookie that requires a successful login on the same browser to retrieve. JWT `kind: "login_continuation"` discriminator prevents access/reset-token replay. Cookie: `Path=/`, `HttpOnly`, `Secure` in prod, `SameSite=Lax` (rides the cross-port redirect from :5100 mint to :5101 login). End-to-end test 2026-05-18: hit `/portfolio-items` logged out → 302 to `:5100/_site/auth/login-required?p=/portfolio-items` → 302 to `/login` with cookie set → login → probe returns `{path:"/portfolio-items"}` and clears cookie → navigation lands on portfolio-items. |
| TD-SEC-STEPUP-ROLLOUT | 2026-05-18 | S3 | Auth / step-up reauth coverage | B16.8.10 shipped per-action step-up reauth (`RequireStepUpReauth(actionKey)` middleware + `POST /_site/auth/reauth` issuing single-use HMAC-bound action proofs). Wired onto `DELETE /workspaces/{id}` only — the one sensitive route that lacked any reauth. The other two existing sensitive routes (`POST /auth/change-password`, `DELETE /auth/mfa`) already self-gate by taking a password in the body and re-verifying inline, so the new middleware would be redundant defence-in-depth. They could still benefit from the proof-token model (single-use + per-action binding defends against in-realm extension capture in ways inline password verify does not), and aligning the UX (one modal pattern, one frontend hook) makes the whole product feel consistent. `change-email` was named in the original B16.8.10 prompt but no such endpoint exists today — see sibling TD-SEC-CHANGE-EMAIL-MISSING. | Procurement review questions inconsistent reauth UX, OR an in-realm extension attack on the inline-reauth routes prompts an audit. Inline reauth still protects against pre-stage attacks because the password is checked at the same wall-clock moment as the action, so the gap is narrow — defer until either trigger fires. | This register entry. Inline reauth on change-password and disable-mfa is already in place — the gap is consistency / belt-and-braces, not an unprotected hole. | Wrap both routes in `RequireStepUpReauth` with action keys `change-password` and `disable-mfa`. Frontend lights up the same modal via `useStepUpAction(...)`. Drop the inline password from the request body once the middleware proof is the gate. ~2 hours plus E2E tests. |
| TD-SEC-CHANGE-EMAIL-MISSING | 2026-05-18 | S3 | Auth / change-email endpoint | No backend route lets a user change their email address. `users.email` is set at account creation and never altered through any handler today. The original B16.8.10 prompt named change-email as one of four sensitive routes to gate with step-up reauth, but the route doesn't exist to gate. Email changes today require a DBA — fine for tiny pilot, untenable at any real scale. | First customer asks how to change their email after typoing it at signup, OR LDAP sync introduces directory-driven email updates that need parity with local-auth users. | This register entry. | Add `POST /_site/me/email` taking `{new_email, password (or action_proof if you wire the proof model)}`. Send a verification email to the new address; the verify link flips the row. The proof model is the right gate from day one — see TD-SEC-STEPUP-ROLLOUT. ~1 day backend + frontend + email template. |
| TD-SEC-WORKSPACE-DELETE-UI | 2026-05-18 | S4 | Frontend / workspace lifecycle | The backend `DELETE /_site/workspaces/{id}` route is gated by `RequireStepUpReauth("delete-workspace")` (B16.8.10), so the step-up modal will fire on any frontend call. But there is no UI today that invokes this DELETE — the workspaces admin page does archive/restore only, and full deletion is reserved for the cross-DB cleanup path that gadmin operators trigger out-of-band. When a deletion UI is eventually added, the implementer pairs the call with `useStepUpAction({ actionKey: "delete-workspace", actionLabel: "Delete workspace" })` and renders `<ReauthModal {...modalProps} />` — both shipped 2026-05-18, sitting idle until first consumer arrives. | First product decision that workspace deletion needs a user-facing surface. Until then there is no exposed gap. | This register entry. The hook + modal are exported and tested; pinning them in tests prevents bitrot before they're wired. | Add a "Delete workspace permanently" affordance to the workspaces admin page (probably behind a confirm + step-up). 30 min wiring + smoke test. |
| ~~TD-SEC-COOKIE-SECURE-ENV~~ | 2026-05-18 | ~~S1~~ | ~~Auth / cookie hardening~~ | `backend/.env.staging.locked` and `backend/.env.production.locked` both have `COOKIE_SECURE=false` — the same value as `.env.dev`. Production auth cookies (`rt` refresh token, `csrf_token`, `mfa_remember_*`) therefore ship without the `Secure` flag, meaning a browser will happily send them over plain HTTP (e.g. a downgrade attack on a misconfigured proxy or a public-Wi-Fi MitM). Mitigated 2026-05-18 by TLS auto-detect in `backend/internal/auth/handler.go` `isSecureCookieRequest` and the matching `backend/internal/security/csrf.go` helper — if the backend handler sees `req.TLS != nil`, Secure is set regardless of env. The env override only matters where TLS terminates upstream (CDN/ALB/Cloudflare) and `req.TLS` is nil at the Go layer. `.env.example` updated 2026-05-18 to document the requirement. `.env.staging` and `.env.production` (locked-stub) files are covered by the block-non-dev-env hook and must be edited by Rick directly. | Already firing on any staging/prod deploy where TLS terminates upstream of the Go process and `req.TLS` is nil — auth cookies leak over the internal hop. Procurement security review will flag it. | `.env.example` comment documents the requirement. TLS auto-detect (commit B16.8.7) ensures direct-TLS deployments get Secure cookies without touching env. Frontend `session_alive` cookie also auto-Secure on `window.location.protocol === "https:"`. | **Resolved 2026-05-18.** All four env files flipped to `COOKIE_SECURE=true` by Rick; `APP_ENV=development` → `APP_ENV=production` in `.env.production.locked` fixed in the same pass. All files are gitignored — local disk only. TLS auto-detect in `isSecureCookieRequest` handles direct-TLS deployments; env var handles upstream-TLS-termination (CDN/ALB) where `req.TLS` is nil. |
| ~~TD-URL-FILTER-CHIPS~~ | 2026-05-18 | ~~S2~~ | ~~Frontend / work-items + portfolio-items~~ | ~~Filter + sort state written to address bar via router.replace.~~ | n/a — closed. | n/a — closed. | **Resolved 2026-05-18 (commit 1d5c53f).** Migration 208 adds `users.preferences JSONB`; `/_site/me/preferences/{key}` endpoint via existing users handler. `useWorkItemsFilters` + `useWorkItemsSort` now take `prefKey`, seed from server once, fire-and-forget PUT on change. State persists across reload/tab-close/device. URL stays `/work-items` and `/portfolio-items`. 5 violations cleared. |
| ~~TD-URL-TAB-STATE~~ | 2026-05-18 | ~~S2~~ | ~~Frontend / tab persistence~~ | ~~`useTabState` wrote `?tab=` to the address bar.~~ | n/a — closed. | n/a — closed. | **Resolved 2026-05-18 (commit b26c0d5).** Lifted seed-once-then-PUT plumbing into shared [`app/hooks/useUserPreference.ts`](../app/hooks/useUserPreference.ts); `useTabState` and `work-items-tree-config` both consume it. Hook's third arg shifted from URL param name → `prefKey` namespace. Two call sites migrated (`backlog/page.tsx` → `"tab.backlog"`, `theme/page.tsx` → `"tab.theme"`). 2 violations cleared. |
| ~~TD-URL-VID-VIEW-PICKER~~ | 2026-05-18 | ~~S3~~ | ~~Frontend / custom pages~~ | ~~`/p/<id>?vid=<view-id>` query selector.~~ | n/a — closed. | n/a — closed. | **Resolved 2026-05-18 (commit dced4b2).** Page moved to optional catch-all `/p/<id>/[[...vid]]`. Reads `params.vid?.[0]` instead of `useSearchParams`. Tab click navigates to `/p/<id>/<vid>`. Legacy `?vid=` URLs resolve to default view (no redirect — the form is dead, not aliased). `docs/c_url-routing.md` updated to match. 1 violation cleared. |
| ~~TD-URL-TOPOLOGY-EXPANDED-FOCUS~~ | 2026-05-18 | ~~S3~~ | ~~Frontend / topology~~ | ~~`?expanded=` + `?focus=` + raw `window.history.replaceState`.~~ | n/a — closed. | n/a — closed. | **Resolved 2026-05-18.** Both URL reads were dead — no codepath in the codebase generated `?focus=` or `?expanded=1`. Replaced with plain React state (`focusId = null`, `useState(false)` for expanded); dropped `useSearchParams` import and the `history.replaceState` round-trip. Comment in page.tsx names the right re-introduction path (route segment for focus, prop for expanded) — never URL state. 3 violations cleared. |
| TD-SEC-DOMPURIFY-CLIENT | 2026-05-18 | S3 | Frontend / XSS defense-in-depth | Backend `SanitiseHelpBodyHTML` (`backend/internal/addressables/sanitise.go`) is the load-bearing XSS gate for `body_html` on `page_help` rows — strict allowlist (`p, br, hr, blockquote, strong, b, em, i, u, code, pre, ul, ol, li, h2-h4, a`), `golang.org/x/net/html` tokenizer, drops scripts/styles/event-handlers/data-URLs. Client wraps `Header.tsx` + `HelpDocRenderer.tsx` with `DOMPurify.sanitize(...)` as belt-and-braces (B16.8 Phase 2, 2026-05-18). The two layers are intentional defense-in-depth, not redundant — the client wrap protects against any future write path that bypasses the backend gate (DB-direct edits, schema-direct writes, future writers that forget to call the sanitiser, a sanitiser bug). | Either layer becomes the sole gate (someone removes one), OR a non-help `body_html` surface gets added (n8n payload-rendered cards, library content imports, markdown-rendered comments) without an explicit defense-in-depth pairing. | Two render sites carry the wrap: `app/components/Header.tsx` + `app/components/HelpDocRenderer.tsx` (both via `useMemo(() => DOMPurify.sanitize(...), [raw])`). Backend sanitiser is the primary gate. CSP nonces (TD-SEC-CSP-NONCES-SRI) provide the third layer if both content gates fail. | Add a `useSafeHtml(raw: string)` hook in `app/lib/security/` so future render sites don't reimplement the memo pattern. Remove the per-component DOMPurify import once consolidated. ~30 min when the third consumer appears. |
| TD-SEC-REDIS-DEPENDENCY | 2026-05-18 | S3 | Infra / horizontal scale | Valkey now exists in `infra/swarm/vector-dev-stack.yml` as `vector-dev_valkey` on port 6379, backed by `valkeydata` and Docker secret `valkey_password`; read-only browser inspection is exposed through `vector-dev_valkey_ui` on port 3003. Notifications v2 PendingStore (S12) is the first planned consumer for debounce/digest ZSETs. App code still uses in-process `httprate.LimitByIP` (`backend/cmd/server/main.go`), while account lockout state lives in `users.failed_login_count` + `users.locked_until` (DB). Both work correctly for the single-process deployment; the remaining rate-limit gap begins when multiple Go replicas count attempts independently. Other latent consumers: DPoP JTI replay cache (currently `dpop_jti_cache` Postgres table per TD-SEC-DPOP-BINDING), CSP-report dedup, idempotency keys, websocket-session registry cross-process awareness. | First horizontal scale event or first backend feature expecting shared cache semantics via Valkey/Redis. | Swarm substrate + secret + volume + read-only UI + smoke docs are now in place. The lockout half (`failed_login_count`/`locked_until` DB columns + `LOCKOUT_THRESHOLD=5` / `LOCKOUT_DURATION=15min`) already protects per-account regardless of replica count — that's the load-bearing security gate, not the rate limiter. | Wire `github.com/redis/go-redis/v9` for S12 PendingStore, add `VALKEY_URL`/secret handling, then later wire `github.com/go-redis/redis_rate/v10` and swap `httprate.LimitByIP(...)` for `limiter.Allow(ctx, key, ...)` on every gated route. Pair rate-limit migration with TD-SEC-DPOP-BINDING's JTI cache migration. |
| TD-SEC-HIBP-PROMOTE-TO-ENFORCE | 2026-05-18 | S3 | Auth / breach-password rollout | B16.8 P4 shipped HIBP k-anonymity breach-password check (`backend/internal/auth/hibp.go`) gated by `HIBP_CHECK_MODE={disabled|telemetry|enforce}`. Default in all envs is `disabled` (zero-risk deploy). Telemetry mode hits HIBP and logs every breach hit to `audit_logs` (action=`auth.hibp_breach_hit`) without blocking the user — so we can measure the false-positive rate against existing user behaviour before turning enforcement on. Enforce mode rejects the change with `Problem.Code=breached_password` + the matching frontend prompt. **Step 1 done 2026-05-18:** `HIBP_CHECK_MODE=telemetry` set in `backend/.env.dev`; soak started. | Operator decision to roll out: soak telemetry data confirms acceptable false-positive rate, then flip to enforce. Or external event: defence/finance procurement asks for the control. | This register entry. Step 1 (dev telemetry) is active. Soak query: `SELECT count(*), action_data->>'email' FROM audit_logs WHERE action='auth.hibp_breach_hit' GROUP BY 2 ORDER BY 1 DESC;` | (2) After 1+ week of dev soak: if no false positives, flip `HIBP_CHECK_MODE=enforce` in dev, then staging. (3) Flip to `enforce` in production. (4) Optional: add a `--check-breach` admin CLI. |
| ~~TD-URL-SHAREABLE-VIEWS~~ | 2026-05-18 | ~~S3~~ | ~~Frontend / URL surface policy~~ | ~~The blanket `feedback_url_is_path_only` rule accidentally closed the shareable-link surface.~~ | n/a — closed. | n/a — closed. | **Resolved 2026-05-18.** `app/lib/shareableParams.ts` — allow-list registry (`SHAREABLE_PARAMS` map: `/work-items` + `/portfolio-items` → `{type, status, priority, owner, sort}`), `parseShareableParams` (inbound URL → `WorkItemsFilters` + sort), `buildShareableHref` (filter + sort + current search → new href preserving `?meg=` and other infra params). `useWorkItemsFilters` + `useWorkItemsSort` in `work-items-tree-config.tsx` now dual-write: every filter/sort change fires a preference PUT AND a `router.replace` updating the address bar. On first seed, URL params override stored preference so a shared link lands the recipient on the sender's view, then persists. `p_ObjectTree.tsx` cross-wires `sortRef` ↔ `filtersRef` so URL writes from either hook carry both dimensions. Hook script updated with `SHAREABLE_PARAMS` carve-out guidance. `users.preferences` stays as the primary store; URL is the secondary shareable surface. `// hook-allow-url-query` annotation on `work-items-tree-config.tsx` authorises the write path. |
| TD-UI-PLACEHOLDER-HANDLERS | 2026-05-18 | S4 | Frontend / unimplemented affordances | Six `console.log(...)` calls in two component files are placeholder handlers for affordances that look real in the UI but do nothing: `CustomFieldsTree.tsx:117` "add custom field" button, and the 5-action menu in `ObjectTree/p_ObjectTree.tsx:178-182` (edit/duplicate/move/split/delete). When the user clicks any of these, the only signal is a `console.log` line in the devtools — for a non-dev user the click is silently a no-op. Discovered while auditing production `console.*` calls for B16.8 P5. Not a security issue (no data leak), but it IS a UX trust issue and a discovery surface for users hunting for missing features. | Anyone clicks one of these affordances and reports "the button doesn't work" — and we can't tell whether they hit a bug or just a placeholder, because there's no marker on the button. Compounds if a future polish pass adds tooltips/states without auditing what's behind the click. | This register entry. The handlers exist (they're not crashes); they just don't do anything useful in prod. | Two paths per affordance: (1) Implement the real handler — preferred when the feature is on the imminent roadmap. (2) Remove the affordance from the UI — preferred when the feature is months out. Half-state ("disabled button with tooltip 'coming soon'") is anti-pattern; either build it or hide it. ~30 min per affordance once the decision is made. The seven-row decision matrix lives with the owning feature plan, not here. |
| TD-RF1-DOC-GO-ADOPTION | 2026-05-18 | S3 | Backend / documentation hygiene | RF1's master plan §6 promised every `backend/internal/<package>/` would gain a `doc.go` describing what the package owns, who can call into it, and which DB pool(s) it touches — the file you open first to orient. Today only `internal/artefactitems/doc.go` exists; 41 packages have no `doc.go`. RF1.7.1 (completion test "open any package, find `doc.go + service.go + handler.go + sql.go + tests` in that order") fails on this. Not a runtime issue, but the central RF1 deliverable was "a codebase a fresh engineer can read at first glance" and the missing `doc.go` is the most visible gap against that promise. | First new contributor (or first Claude session that hasn't held the codebase in head for a week) opens `backend/internal/<random>/` and has to reverse-engineer the package's role from import graphs. Or external audit / procurement asks for a per-package responsibility map and the answer is "read the service.go header comments." | This register entry. `c_c_db_routing.md` covers the package→DB map; absent `doc.go` only loses the per-package "what writers can call into me" surface. | Alphabetical sweep, ~10 lines per file: package purpose, public surface (the few exported funcs that constitute the writer boundary), DB pool(s) used (`s.Pool` / `s.VAPool` / `libPools.RO`), cross-cutting partners (audit, alerting, transport). ~3-4 hours for the full 41 packages. Pair with the next round of cross-package refactoring so a fresh `doc.go` always lands alongside the touch. |
| TD-DEV-DATA-ORPHAN-MRP | 2026-05-19 | S3 | Backend / cross-DB canary races parallel tests | `TestCrossDBCanary_WorkspaceReferences` was originally written to detect real cross-DB orphans (master_record_portfolios rows whose workspace_id no longer exists in mmff_vector). Initial 2026-05-19 attempt resolved the static orphans: swept 56 dangling MRP rows, soft-archived 246 orphan artefacts_types + 55,715 orphan artefacts in vector_artefacts. **But the canary still fails** because the `adopt_*_test.go` suite creates ephemeral test workspaces in vector_artefacts that intentionally don't exist in mmff_vector. When `go test ./...` runs the package in parallel, those ephemeral rows are alive while the canary scans — false positives forever. Re-skipped with a clearer rationale. | (a) A production workspace-delete handler ships and skips the cross-DB sweep — real orphans accumulate silently (test won't catch them while parallel-race condition exists); (b) Anyone investigating the skip finds a deeper test-isolation problem. | This register entry + the `t.Skip` with the parallel-race note. The static-orphan sweep already happened (data hygiene chunk done). | Two paths to durable fix: (1) Move the canary to an integration-test build tag (`//go:build integration_canary`) that runs sequentially against a known-quiescent DB. (2) Re-architect `adopt_*_test.go` to use deterministic test subscriptions + add their workspace IDs to a master_record_workspaces row in mmff_vector during seed (mirroring how production would). Option (1) is cheaper; option (2) better mirrors production. Pair with whichever workspace-delete handler ships next so the canary becomes part of that handler's regression net. |
| TD-URL-SCOPE-PARAM-CUTOVER | 2026-05-18 | S2 | Frontend / scope clamp transport | First surgical carveout from `feedback_url_is_path_only` (PLA-0053): the active topology scope node now lives in the address bar as `?meg=<node-id>` instead of `localStorage["vector.scope.activeNodeId"]`. Symptom that forced the carveout: Summary and List on `/portfolio-items` returned inconsistent counts because `withForwardedScope` in [`app/lib/api.ts`](../app/lib/api.ts) read localStorage at call time, and an async `seed()` in [`ScopeContext.tsx`](../app/contexts/ScopeContext.tsx) overwrote that value between the two requests — Summary captured the pre-seed value, List captured the post-seed value. URL is now canonical; localStorage + server profile (`/me/active-scope`) remain as fallbacks. Backend handler ([`backend/internal/artefactitems/handler.go`](../backend/internal/artefactitems/handler.go) `List` + `Summary`) accepts both `?meg=` (preferred) and legacy `?scope=` during the migration window. The param name is intentionally opaque (not `?scope=`) so topology-shape isn't guessable from the URL. Carveout authorised by `// hook-allow-url-query` annotation in ScopeContext.tsx; the rest of the address bar stays path-only. | Two triggers: (1) any caller still sending `?scope=` after 7 days — drop the back-compat fallback. (2) Next time someone hits a similar "two requests on one render disagree because of mutable side-channel state" race — same root cause, same fix shape; this entry is the precedent. | URL writer is centralised in `writeUrlMeg` inside ScopeContext.tsx; reader is `readUrlMeg` + `URLSearchParams` in `withForwardedMeg` (api.ts). Hook annotation in ScopeContext.tsx prevents the rule-enforcer from blocking edits to the canonical writer. Backend accepts both params, prefers `?meg=`. | Three closeout steps: (1) Audit access logs for any `?scope=` after 7 days; if zero, delete the legacy fallback branch in handler.go + api.ts. (2) Delete `localStorage["vector.scope.activeNodeId"]` reads — URL is sufficient now. (3) Update [`docs/c_c_transport_segregation.md`](c_c_transport_segregation.md) and the rule statement in [`block-url-query-state.sh`](../.claude/hooks/block-url-query-state.sh) to name the carveout explicitly (today the only signal is the per-file annotation). The larger shareable-views work is [TD-URL-SHAREABLE-VIEWS](#td-url-shareable-views) — this entry is the proof-of-concept; that one is the generalised allow-list. **2026-05-18 addition:** `?scope_dir=` (scope direction: "ascend"/"descend") is NOT in the URL — direction is not a resource identifier so no PLA-0053 carveout was needed. Direction lives in module-level state (`_scopeDirection` in api.ts) written by `setScopeDirection()` which ScopeContext calls on seed + setDirection. Direction is persisted in user preference `scope.direction` (existing `/_site/me/preferences/{key}` endpoint). Backend handler reads `?scope_dir=` and branches `AncestorNodeIDs` vs `DescendantNodeIDs` in the service. The `parent_artefact_id IS NULL` default is now dropped when any scope clamp is active — the root cause of the "tree empty" bug on `/portfolio-items`. |
| ~~TD-NAV-AUTH-TIER~~ | 2026-05-19 | ~~S1~~ | ~~Auth / nav rail visibility~~ | **Recurring-issue ledger entry — Trust-No-One lapse.** First implementation of "admin tags only show to the right roles" was client-only (`ShellContext.tsx` filtering on `tag.minAuthLevel`). The /nav/catalogue payload still emitted every admin tag enum to every caller, so a tampered client could re-introduce them and at minimum enumerate the admin nav structure (procurement defence/finance bar: information disclosure of admin surfaces is itself a finding). The lapse repeats a pattern I should have caught: optimised for proximity (frontend files already open) over principles (Trust No One — server is source of truth). | Any subsequent nav/auth/scope/visibility change where I (a) build a client-side filter without first writing the server-side one, or (b) tell the user "this is locked by the backend" without proof. If this fires twice more it auto-promotes to a hard rule via `<r>`. | This register entry. `feedback_security_first_server_truth.md` saved as memory so the next session enters with the rule loaded: any visibility/role/scope gate writes the server-side filter first, client-side is defence-in-depth only. | **Resolved 2026-05-19 (same session).** Server-side gate shipped: `nav/registry.go` carries a `roleRankByID` map loaded once per registry refresh; `authLevelFor(roleID)` derives the 1/2/3 tier; `CatalogFor` drops catalogue entries whose tag is gated above the caller's tier; new `TagsFor(roleID)` is the authoritative filter the handler uses (the old `Tags()` is now reserved for internal/test use). Handler ([handler.go:59](../backend/internal/nav/handler.go#L59)) reads `u.RoleID` and routes through `TagsFor`. Two new tests in [service_test.go](../backend/internal/nav/service_test.go) pin the contract: `TestTagsFor_AdminTierFiltering` covers gadmin/padmin/team-member/unknown-role (5 cases); `TestCatalogFor_DropsAdminPagesForLowerTier` asserts admin pages cannot reach a team member. Both green. The migration trio (220 then 221) defines the column and seeds the tier values. Frontend client-side filter retained as defence-in-depth, not the gate. **Superseded 2026-05-19 (later that day) by PLA-0053 (B5.11–B5.16).** The tier gate is gone entirely. `users_roles_pages` is the sole page-access source of truth; tag visibility on the rail is derived from page-grant fan-out (`Registry.TagsFor(roleID, subscriptionID)` returns only tags with ≥1 granted page). `roleRankByID` + `authLevelFor` removed from `nav/registry.go`. `pages_tags_min_auth_level` kept nullable in DB for rollback; not scanned. Tests rewritten to use an isolated tenant-custom role with zero grants — `TestTagsFor_PageGrantDerived` + `TestCatalogFor_PageGrantIsSoleGate`. ADR captured in [`docs/c_c_roles_permissions.md`](c_c_roles_permissions.md). Migration: [`db/mmff_vector/schema/228_collapse_min_auth_level_gate.sql`](../db/mmff_vector/schema/228_collapse_min_auth_level_gate.sql). Audit script: [`dev/scripts/audit_role_page_grants.sh`](../dev/scripts/audit_role_page_grants.sh) — surfaced the wide seed-grant footprint to clean up via the permissions matrix. |
| TD-ITEMTYPE-KILL | 2026-05-19 | S2 | DB + Backend + Frontend / type discriminator | Legacy `item_type` TEXT column on `o_artefacts_execution_work_items` (with CHECK constraint pinned to `epic\|story\|task\|defect\|risk` and `idx_o_wi_type` index) is the legacy string discriminator that the new `vector_artefacts.artefacts.artefact_type_id` UUID FK already replaces in the new schema. Today the two coexist: `o_artefacts_execution_work_items.item_type` is a denormalised TEXT column kept in lockstep via `ResolveArtefactTypeIDByName` ([service.go:493](../backend/internal/artefactitems/service.go#L493)); the wire still emits `item_type` lowercased from `at.artefacts_types_name` ([sql.go:46](../backend/internal/artefactitems/sql.go#L46)); the handler accepts `?item_type_id=<uuid>` (new) but `createWorkItemReq` still REQUIRES `item_type` string ([handler.go:370](../backend/internal/artefactitems/handler.go#L370)); 13 frontend files key on the string (TYPE_TIER maps, CustomFieldManager prefix routing, badge colours); 5+ test files seed by string name. Audit (2026-05-19) catalogued 127 Go hits, 51 TS hits, 60 unique files. Real-world impact: any custom artefact type the user adds via the admin UI (e.g. "Vulnerability") will fail the create path because `validItemTypesByScope` is a hardcoded allow-list of the 5 known names. The kill is blocked on the unified `/artefacts` API (B1.9) — until create accepts `artefact_type_id` UUID natively and the frontend stops needing the string for layout decisions, dropping the column would break the live read path. | (a) User adds a custom artefact type via the new admin UI and clicks "Create" — fails on `validItemTypesByScope` rejection. (b) Anyone adds a sixth standard artefact type and discovers the CHECK constraint needs widening alongside the allow-list, the TYPE_TIER map, the CustomFieldManager key set, and the icon-overrides CHECK. (c) Defence/finance audit asks "how does the type catalogue work" and the answer is "two systems running in parallel." | This register entry + the B1.9 scope sequence (`Vector_Scope.md`). The current path is functional for the 5 known types; nothing breaks until a 6th type or a custom type is created. `ResolveArtefactTypeIDByName` is the single chokepoint that maps string ↔ UUID, so the system stays consistent as long as catalogues don't drift. | B1.9.8 is the closeout story: drop `item_type` TEXT column + CHECK + `idx_o_wi_type` index via migration; backfill `subscription_item_type_icons` to use `artefact_type_id` FK + drop its CHECK too; remove `createWorkItemReq.ItemType`, `validItemTypesByScope`, `canHaveManualPoints(itemType string)` switch; replace TYPE_TIER string keys with sort_order from the type record (already on the type row); remove `useChipTypeOptions` name-based fallbacks; rewire CustomFieldManager to key on `artefact_type_id` UUID. Sequenced AFTER B1.9.1–7 so no live caller reads or writes the legacy column. Estimated 8-12 hr split across 2-3 sessions; the schema change is small, the breadth of code touches is the cost. Audit detail in conversation transcript 2026-05-19. |
| TD-SPECSYNC-HANDLER-AMBIGUITY | 2026-05-19 | S3 | Tooling / OpenAPI spec curator | `dev/scripts/sync_specs.py` + `dev/scripts/extract_handler_shapes.py` resolve a route's handler symbol by parsing `r.Post("/work-items", h.Create)` and matching the symbol literal (`h.Create`) back to a Go method receiver. When a route is mounted inside a closure that takes the handler as a parameter (e.g. `mountArtefactSite = func(r chi.Router, h *artefactitems.Handler)` at [`backend/cmd/server/main.go:1276`](../backend/cmd/server/main.go#L1276)), every route in the closure refers to the parameter as `h.<Method>`. The curator then matches by name alone and picks the first `h.Create` it finds in any package — for `POST /work-items` and `GET /work-items` it lands on `topology.h.Create` / `libraryreleases.h.List` instead of `artefactitems.h.Create` / `artefactitems.h.List`. Result: `siteAPI.yaml` documents the wrong request/response shape for those routes — e.g. POST /work-items appears to take topology-node fields (`workspace_id`, `parent_id`, `name`, `icon`, ...) rather than work-item fields (`item_type`, `title`, `priority_id`, ...). Confirmed 2026-05-19 after the PLA-0043 writer patch landed (the `?meg=` + `X-Act-As` additions never showed up in the regenerated spec because the curator is documenting the wrong handler). Other affected paths: every closure-mounted route group — `/portfolio-items`, `/sprints`, `/releases`, `/topology` subtree probably has the inverse drift. Two `h.Bulk` references exist (artefactitems vs nothing else), so Bulk happens to resolve correctly by uniqueness. | Anyone trusts `siteAPI.yaml` for `POST /work-items` parameters (Scalar IDE autocomplete, integration test stubs, frontend codegen) and writes a request against the wrong shape — request body fails JSON validation; integration tooling silently drifts. Compounds every time a new closure-mounted route group is added. | This register entry. The actual code in `backend/internal/artefactitems/handler.go` is the source of truth; anyone editing the route uses the Go handler, not the yaml. The yaml is wrong but not load-bearing for runtime — the SPA uses typed `app/lib/apiSite/index.ts`, not the spec. | Two paths. **Cheap fix (recommended):** teach `extract_handler_shapes.py` to walk the AST upward from each `r.Post(...)` call site, find the enclosing function literal, read its parameter types, and resolve `h.Create` against the parameter's package. ~30-40 lines of Go AST walking; preserves the rest of the curator. **Architectural fix (heavier):** add an explicit `// @handler:artefactitems.Handler.Create` annotation above each closure-mounted route so the curator has a direct pointer, no AST walking required. Cheap fix is the right starting point — fall back to annotations only if the AST walk hits chi-specific edge cases (route groups inside route groups, helper functions, etc.). Pair with the next `sync_specs.py` touch so the regen-after-rename works. |
| ~~TD-RF1-TEST-COLUMN-RENAME-DRIFT~~ | 2026-05-18 | ~~S2~~ | ~~Backend / test suite reliability~~ | **Retract + re-scope 2026-05-18 evening:** the original framing ("14 packages, mechanical column-rename drift, ~2-4hr sed-style") was wrong. After running an uncached `go test -count=1 ./...` and tracing each failure category, the test-debt is broader and not mechanical. Four distinct failure modes coexist: (1) **column-rename drift** in deferred cleanup helpers — `DELETE FROM users_roles WHERE subscription_id = $1` references a column that was renamed to `users_roles_id_subscription` in RF1.4.4 (this is the original framing, and it IS present, but it's a minority of the noise); (2) **table-renamed-or-dropped drift** — tests query `page_help`, `portfolio_models`, `library_releases_acknowledgements` which were renamed/dropped during the RF1 migration sweep, surfacing as `relation does not exist` rather than column-level errors; (3) **DPoP-fixture drift** — the parallel DPoP rollout added a NOT NULL `users_sessions_dpop_jkt` column (migration 213); tests that bypass `auth.Service.Login` and INSERT directly into `users_sessions` fail on the constraint until they stamp a placeholder thumbprint (partly fixed in realtime/session_sweeper_test.go on 2026-05-18, remaining packages still drift); (4) **fixture collisions** — `TestCreateWorkItem_RiskType_ReturnsRSKKey` hits `duplicate key value violates unique constraint "artefacts_number_unique_per_type"` because test data leaks between runs without proper teardown. Each category needs a different fix; they don't share a regex-replace. Failing packages (post-cache uncached): addressables, artefactitems, dbinvariants, errorsreport, librarydb, libraryreleases, nav, portfoliomodels (1 test), realtime (1 of 3 remaining), roles, timeboxsprints, topology, workspaces. (featuretests + 2 of 3 realtime + portfoliomodels-bad-uuid + 3 unrelated DPoP-test-signature drifts fixed 2026-05-18 evening commit `b60012c`.) RF1.7.4 ("run `go test ./...` and see zero failures") still fails. | Same as original framing: any contributor running `go test ./...` sees noise that masks real regressions; CI hard-fail on `go test` blocked. The original sized this at 2-4 hours which created false confidence — calling it out so the next session doesn't make the same mistake. | This register entry. Production code is unaffected — the live SQL in `sql.go` files uses current column/table names. The failures are entirely in test fixtures, cleanup helpers, and integration-test setup. | **Multi-session pay-down, expect 1-2 full sessions.** Approach: (1) Inventory pass with an Explore agent — categorise each failing test by mode (1/2/3/4 above) before touching code. (2) Per category: column-rename is the easiest (regex per mapping table); table-renamed needs a per-package decision (does the test even still apply post-rename?); DPoP-fixture needs the placeholder stamp added everywhere; fixture-collision needs proper teardown discipline (or use `t.Cleanup` + isolated subscription_ids per test like the working realtime sweeper test does). (3) Per-package commits so progress is reversible. The original "~2-4hr mechanical sed" estimate was wrong; honest re-estimate is 8-12hr split across two sessions, with the second contingent on what the inventory pass surfaces. Pair with TD-RF1-DOC-GO-ADOPTION if both land in the same session (touches the same files at sentence-comment level so the rhythm rhymes). |

| TD-ARTEFACT-INLINE-FORM-FIELDLIB-LEFTOVERS | 2026-05-20 | S3 | DB / dual-storage path | Migration 084 promoted `colour`, `is_blocked`, `blocked_reason`, `story_points` (already existed), `timebox_milestone_id` to first-class columns on `vector_artefacts.artefacts`. Some artefact-type field-library bindings still reference these as custom fields (e.g. the risk-type `blocked_reason` binding seeded by migration 075). Two storage paths now exist for the same logical fact: the row column AND the per-type field-values row. The ArtefactInlineForm writes to the row column; older code that wrote to field-values continues to do so but the UI no longer renders those bindings. | (a) Anyone writes to the field-values row without mirroring to the column — the inline form silently shows stale data. (b) Reports / summaries that aggregate "blocked" counts across types get different answers from the two sources. (c) A data-migration follow-up tries to drop the redundant bindings and discovers historical data lives only in field-values. | This register entry. The inline form writes only the column. The risk-type field-library binding still exists but is no longer surfaced in the UI (the form's right column hard-codes the column-backed Blocked toggle). | Data migration follow-up: for every artefact that has a non-null `blocked_reason` / `story_points` / `colour` in `artefacts_fields_values` AND a null in the corresponding artefacts column, COPY the field-values row to the column. Then DELETE the field-library bindings for these keys (per artefact-type catalogue). Single migration; safe because the form already prefers the column. |
| TD-ATTACHMENTS-WIRING | 2026-05-20 | S3 | Frontend / inline form gap | The ArtefactInlineForm's left column renders a labelled stub for Attachments ("Drop files here (wiring pending)"). No upload surface exists yet — no backend handler, no siteAPI helper, no storage backing. The stub keeps the form's visual layout final so the eventual wiring is a swap-in. | User clicks the drop-zone expecting an upload — nothing happens. Or a future feature ships that depends on artefact attachments and discovers the surface needs full backend + frontend build. | This register entry + the visible stub label in the form. The stub names its own gap. | Build the upload pipeline: storage backing (object store or DB blob), `POST /_site/artefacts/{id}/attachments` handler, siteAPI helper, replace the stub with a real drop-zone component. Cross-cutting work; pair with the next feature that needs attachments. |
| TD-PARENT-CANDIDATES-DYNAMIC | 2026-05-20 | S3 | Frontend / inline form | `app/components/ArtefactInlineForm/useParentCandidates.ts` hard-codes the allowed-parent prefix map: TA → DE/US, US → FE/EP, DE → EP/US. Tenant-added custom artefact types do not flow through — they appear in the type catalogue but cannot be parented to anything via the inline form. | Tenant adds a custom artefact type with a clear parent intent (e.g. "Vulnerability" parented to a Story) and the parent dropdown is empty. Or a sixth standard type ships (Risk, etc.) and the map needs widening alongside the UI. | This register entry + a `// TODO: derive from artefact_types.parent_type_id` comment at the prefix-map declaration site. Hard-coded map keeps the v1 form functional for the 5 known types. | Replace the map with a runtime resolver that walks the `artefact_types.parent_type_id` chain (already in the catalogue). The resolver returns the allowed prefixes for a given child type; the prefix map becomes a computed value rather than a constant. Pair with TD-ITEMTYPE-KILL closeout (B1.9) so the form keys on UUID throughout. |
| TD-COLOUR-PICKER-CSS-RENAME | 2026-05-20 | S3 | Frontend / CSS naming hygiene | The promoted `app/components/ColourPicker.tsx` still emits `at-colour-picker` / `at-colour-popover` / `at-colour-cell` CSS classes. The `at-` prefix was scoped to artefact-types page; the class names survive the component promotion so the existing CSS rules at `globals.css:13562-13638` continue working without a parallel rewrite. Doesn't match the project's `root-block__Container_Child_leaf` convention. | A theme refresh that touches the colour-picker visual surface has to find rules under the wrong prefix. Or anyone reads the new shared component and assumes the CSS lives under `colour-picker__*`. | This register entry. The component file's docstring names the class-name carryover; the existing CSS rules continue working unchanged. | Rename pass: `at-colour-picker` → `colour-picker`, `at-colour-popover` → `colour-picker__Popover`, etc. Sync the rule names in `globals.css` and the className strings in the component. Mechanical sed-style edit; ~30 min including the visual diff against the two catalogue pages. |
| TD-FIELDS-WRITER-PERMS | 2026-05-20 | S2 | Auth / RBAC catalogue | The rebuilt Custom Fields write surface ([`backend/internal/fields/service.go`](../backend/internal/fields/service.go) `AssertCallerMayWrite`) gates on role-tier UUIDs (`SystemGrpGlobalID` / `SystemGrpPortfolioID`) + the legacy `users_roles_workspaces_role = 'admin'` string for the workspace-admin tier. PLA-0007 ([`docs/c_c_roles_permissions.md`](c_c_roles_permissions.md)) is mid-cutover to permission-driven gates (`useHasPermission(<code>)` on the frontend, `permissions.Has(ctx, code)` on the backend). The new writers ship in the role-string style for consistency with the existing `AssertCallerMayRead` in the same file — but every other recent admin surface (cost-centres, admin-roles) gates on permission codes. Two systems running in parallel for the same package. | (a) PLA-0007 G3 closes out and the codebase audit flags `fields.AssertCallerMayWrite` as the last role-tier holdout — the flip becomes mandatory not optional. (b) Anyone adds a custom role with "manage workspace fields but not tenant fields" granularity — the role-string gate can't express it; the permission catalogue can. (c) The workspace-roles enum (`admin`/`editor`/`viewer`) is itself slated to retire in favour of role-FKs ([TD-ITEMTYPE-KILL](#td-itemtype-kill) sibling), and this gate hard-codes the literal `'admin'` string. | This register entry. The `AssertCallerMayWrite` rule signature already takes a target scope, so the body is the only thing that changes — call-sites stay stable. The legacy workspace-roles enum hasn't moved yet, so `users_roles_workspaces_role = 'admin'` is still the correct probe today. | Two-step flip when PLA-0007 G3 lands: (1) Add permission codes `fields.write.workspace` + `fields.write.tenant` to `internal/permissions/catalogue.go`; seed grants to `grp_global`+`grp_portfolio` (tenant) and `grp_global`+workspace-admin (workspace) in a migration. (2) Replace the role-UUID + role-string branches in `AssertCallerMayWrite` with `permissions.Has(ctx, "fields.write.<scope>")`. Drop `sqlExistsActiveWorkspaceAdminGrant` once nothing else reads the legacy workspace-roles enum. ~2 hours including handler-test updates and the migration. |
| TD-RICHTEXT-HTML-RENDER | 2026-05-20 | S2 | Frontend / read-side rendering | The new `app/components/RichTextField/` stores rich descriptions as TipTap JSON in `vector_artefacts.artefacts.description_doc` (JSONB, migration 088). Edit-side is wired (form → JSON → backend → JSONB). **Read-only surfaces that need to render the rich content as HTML are NOT wired yet** — list cells, summary tooltips, search hit cards, etc. still show the plain `description` TEXT fallback. The plain-text fallback is correct (no XSS surface, no rendering bug) but loses formatting. | First product surface that wants formatted description rendering outside the edit form (e.g. a hover preview, a print-friendly view, a notifications digest). At that point the rendering helper must exist and must be sanitised. | This register entry. The plain-text fallback handles every existing read surface; nothing breaks today. The `description` TEXT column is still populated for fallback. | Build a small `<RichTextRenderer>` companion: takes a JSON doc, generates HTML via TipTap's `generateHTML` with the same extension set, runs the result through DOMPurify (already installed) with a strict allowlist, renders via `dangerouslySetInnerHTML`. Wire to read surfaces as they need it. Same JSON source-of-truth as the editor — no schema drift. Pair with: a backend-side renderer too if any non-React consumer (PDF export, email digest, search snippet) needs HTML. |
| TD-FLOWSTATE-CASCADE-STRATEGY | 2026-05-21 | S2 | Backend / domain logic | The flow-state cascade engine ([`backend/internal/artefactitems/recalc.go`](../backend/internal/artefactitems/recalc.go)) derives a parent artefact's `flow_state_id` from its direct live children (work flows up) — but ONLY within the execution scope. Strategy-zone rows (Theme / Business Objective / Feature) are exempt: the recalc checks `scope != s.scope` and returns. Rationale: strategy items are user-edited "intent" rows whose state shouldn't be jerked around by execution-zone churn (Story going `doing` shouldn't auto-flip the parent Feature). However, this means a Feature parented onto a Theme can sit "Backlog" forever while its Epic children are all "Done" — the strategic surface won't reflect execution reality. Buyer (defence/finance) procurement review may flag the inconsistency as a reporting gap. | (a) First product complaint that "all my Epics are Done but the Feature still says Backlog." (b) A reporting / roll-up dashboard ships that depends on strategic state being a function of execution state. (c) An audit reviewer asks "what triggers a Feature to move?" and the answer is "manual only" — looks awkward against an otherwise heavily-automated cascade. | This register entry + the explicit `scope != s.scope` guard in `recalcParentFlowState` (recalc.go ~L95) names the boundary. Strategic items remain manually editable end-to-end. The `--derived` lock on the pill row only applies to execution-zone parents (it's keyed on `children_count > 0`, which is true for strategy items too — but they have no recalc engine writing on their behalf so the lock is a small UX wart, see below). | Decide whether strategy items should auto-derive from execution children. If yes: relax the `scope != s.scope` guard to also walk the strategy-execution boundary, but pick a narrower rule (probably "all children done → parent done" only — strategic items shouldn't track `in_progress` from a single Epic moving). If no: tighten the frontend pill-row `--derived` logic to skip strategy items so they don't render locked when they actually CAN be edited. ~1-2 hours either way. |
| TD-FLOWSTATE-CASCADE-PILL-STRATEGY-LOCK | 2026-05-21 | S3 | Frontend / inline form gating | The `children_count > 0` check on the pill row + inline form's Flow State select locks editing whenever the row has live children. Backend only enforces the same lock for execution-zone rows ([`ErrParentFlowStateDerived`](../backend/internal/artefactitems/types.go) is only raised by the cascade-aware PatchWorkItem path, and the cascade itself doesn't fire on strategy). Effect: a strategy-zone row with children (e.g. a Feature with Epic children) renders as `--derived` and won't let the user click pills, but the BACKEND would actually accept the manual write. So the frontend lock is stricter than the server contract in the strategy zone. | First user tries to manually edit a Feature's state via the inline form and finds the pill row locked. Or QA asks "why can't I move this Feature?" | This register entry. The lock errs on the safe side — better to confuse a user with "can't edit" than to let them set state that the cascade will silently auto-revert later (which would happen if TD-FLOWSTATE-CASCADE-STRATEGY ever closes by extending the cascade upward). | Pair with TD-FLOWSTATE-CASCADE-STRATEGY decision. If cascade extends to strategy → lock is correct, drop this entry. If cascade stays execution-only → gate the frontend lock on the row's scope too: `derived = children_count > 0 && scope === 'work'`. Adds `scope` to ArtefactDetail + the WorkItem row shape. One-line frontend tweak + a wire-shape extension. |
| TD-REPARENT-BACKEND-PARENT-TYPE | 2026-05-21 | S2 | Backend / domain logic | The drag-to-reparent gate (`canReparent` in [`p_ObjectTree.tsx`](../app/components/ObjectTree/p_ObjectTree.tsx)) and the inline-form Parent dropdown ([`useParentCandidates.ts`](../app/components/ArtefactInlineForm/useParentCandidates.ts)) both read the hard-coded `PARENT_PREFIX_MAP` (TA→DE/US, US→FE/EP, DE→EP/US, EP→FE) to decide which parent types are legal for a given child. Backend `PatchWorkItem` accepts ANY `parent_artefact_id` that exists in the subscription — no type-check, no scope-boundary check, no strict execution-vs-strategy gate. A malicious or buggy client could PATCH a Task to be parented directly under a Theme. | Buyer (defence/finance) procurement audit: "show me how unauthorized parent-type combinations are prevented." Today the answer is "frontend gate" — not good enough. Or: a non-React tool (CLI, automation) writes a bad parent_id and corrupts the strategic hierarchy. SOC 2 server-is-the-gate principle says this enforcement belongs in the backend. | Hard-coded prefix map names the contract; this register entry; existing `useParentCandidates` already enforces on the form path. Drag-to-reparent enforces on the DnD hover gate (`canReparent`) and silently ignores illegal drops. | Add allowed-parent enforcement to `PatchWorkItem` when `ParentArtefactID` is in the body: fetch the prospective parent's type, walk the catalogue's prefix rule (or, when [TD-PARENT-CANDIDATES-DYNAMIC](#td-parent-candidates-dynamic) lands, the proper resolver), return a new `ErrInvalidParent` sentinel → 409 with `{"error":"parent_type_not_allowed",...}`. Pair with TD-REPARENT-CYCLE-DETECTION-BACKEND for the same PR — both gates are the same risk class. ~1 hour including a regression test (try parenting a Task under a Theme, expect 409). |
| TD-REPARENT-CYCLE-DETECTION-BACKEND | 2026-05-21 | S2 | Backend / domain logic | Same shape as TD-REPARENT-BACKEND-PARENT-TYPE: cycle prevention lives on the frontend only. `useResourceRank.getDescendants` walks the visible subtree and blocks drops onto descendants on the DnD path; `useParentCandidates` excludes self from the dropdown. Backend `PatchWorkItem` happily accepts a `parent_artefact_id` that would create a cycle (e.g. set Story A's parent to one of A's own Tasks). | Same triggers as above — procurement audit, non-React writer, or even a stale frontend that misses a descendant load. A cycle in `parent_artefact_id` breaks the recursive CTE in `sqlSelectAncestors` (infinite loop), breaks the cascade engine (would recurse forever), and corrupts the tree topology. | This register entry. Frontend gates on DnD hover; the existing recursive CTE uses `WHERE a.id != ...` which provides a degree of protection but isn't the right gate. | Add a single SELECT inside `PatchWorkItem`'s `ParentArtefactID` path: walk up from the prospective new parent via parent_artefact_id; if the chain reaches the row being patched, reject with `ErrInvalidParent` (same sentinel as TD-REPARENT-BACKEND-PARENT-TYPE — same 409 path). Recursive CTE with subscription_id clamp, bails fast on hit. Pair with the parent-type gate in one PR — shared cost. ~1 hour including a cycle-attempt test. |
| TD-FIELD-LIBRARY-NAME-COLUMN-DRIFT | 2026-05-21 | S2 | Backend / SQL constants | `sqlListFieldValuesForArtefact` in [`backend/internal/artefactitems/sql.go:619`](../backend/internal/artefactitems/sql.go#L619) selects `fl.name, fl.label, fl.field_type, fl.options_json` from `artefacts_fields_library`. The live `dev` schema's column is `field_name`, not `name` (migration [006_field_library.sql:24](../db/vector_artefacts/schema/006_field_library.sql#L24) — never renamed). Any call to `GET /api/v2/work-items/{id}/field-values` against the live dev DB returns `column "name" does not exist (SQLSTATE 42703)`. Discovered while wiring the rule-engine hook for custom-field writes (my new `sqlSelectFieldLibraryNameAndType` initially had the same bug; corrected to `field_name` with a NOTE comment pointing here). | Anyone opens the work-item detail panel's custom-fields section and clicks anything that hits the GET — would return 500. Today the panel only PUTs (via `UpsertFieldValues`) and reads the field_values back from the PUT response, NOT from this GET, which is why nobody's noticed. Trigger: a new feature reads field-values via the GET endpoint, OR an automated test exercises `ListFieldValues`. | This register entry. The wider hook-wiring PR ships with the correct column name; the broken constant ships alongside, isolated. | Two-line fix: change `fl.name` → `fl.field_name` in both the SELECT and ORDER BY clauses of `sqlListFieldValuesForArtefact`. Add a `GET /work-items/{id}/field-values` integration test (~10 lines) to lock the contract. ~10 min total. |
| TD-CHIP-SCOPE-MISMATCH | 2026-05-23 | S2 | Frontend / ObjectTreeV2 filter chips | Filter chips (Type, Priority) on `/work-items` were loading options from the workspace-clamped catalogues (`useArtefactTypeCatalogue` + `useArtefactPriorityCatalogue`), but the grid clamps by **topology scope** on top of workspace. Visible rows under the active scope can be tagged with artefact-type UUIDs that don't appear in the workspace catalogue (federated scopes), so clicking a chip filtered against a UUID nothing in the grid carried → 0 results despite the rows being visible (2026-05-23 incident: 27 Tasks in the grid, Task chip returned empty). Intermediate fix on disk derives `typeOptions` / `priorityOptions` from `windowRoots` directly in [`p_ObjectTree.tsx`](../app/components/ObjectTreeV2/p_ObjectTree.tsx), with workspace catalogue used only for label + colour metadata. Eliminates the UUID mismatch but introduces a coverage gap: `windowRoots` only contains current-page roots, so types that exist only at child depth or only on later pages don't surface as wedges. `WorkItemsFilterChipsProps.typeOptions` + `.priorityOptions` carry a `= []` default for V1 compatibility — temporary. Plan: PLA057 on /dev/reporting. | (a) User can't filter to a type whose rows are below the visible page or below the expanded-child threshold (e.g. all Tasks are children of an Epic on page 1, but the Task wedge never appears unless rows are expanded). (b) Same gap on Priority for any priority not represented in the current page roots. (c) On a federated scope where root rows are tagged with workspace-B types, the chip can offer those types but workspace-A admin tools (action-bar create flow) still need the workspace-A catalogue — divergence makes the chip and the create button feel inconsistent. | This register entry. The `windowRoots`-derivation in `p_ObjectTree.tsx:372-411` is the active fallback. Empty-state guard in `NavigationPie.handleChipClick` refuses to open the pie when `options.length === 0`, so the symptom is a click that does nothing rather than a broken pie. | Implement OBJ1 (PLA057): backend `ListFacets` on `artefactitems.Service` (workspace + topology clamp via existing `CanReadScope` + `DescendantNodeIDs`), `GET /work-items/facets` + `/portfolio-items/facets` handlers under the same WorkspaceClampMiddleware as list, frontend `useObjectTreeFacets` hook (cache key `(workspace, meg, resource)`, refetches on scope change). ObjectTreeV2 swaps `windowRoots`-derivation → hook output. Drop `?` + `= []` fallback on `WorkItemsFilterChipsProps` once V1 ObjectTree retires. ~8 stories OBJ1.0.1–OBJ1.3.1. Closes when the facets path lands. |
| ~~TD-AUTH-KEYS-001~~ | 2026-05-23 | ~~S2~~ | ~~Backend / auth — credential issuance gate~~ | ~~/admin/api-keys/{issue,list,revoke} at [`backend/cmd/server/main.go:1328`](../backend/cmd/server/main.go#L1328) was gated by `permissions.UsersList` — the same code that reads the user directory. Wrong threat boundary: any role with users-list could issue/list/revoke programmatic credentials, with no path to grant "user admin without key issuance." Compounding: `auth.RequirePermission` has a deliberate pass-through for api-key auth (`UserFromCtx==nil && api_key_subscription_id!=nil → next`), so an API key bound to a subscription could mint or revoke another API key on its own subscription — accidental key-chaining. No fresh-password gate; no step-up; no audit narrative for procurement.~~ | ~~Defence/finance procurement audit: "who can mint credentials, how often is the human re-verified, can a service identity escalate to another service identity." Today's answer (users.list + api-key pass-through) doesn't meet NIST 800-63B AAL2/AAL3 or SOC 2 expectations.~~ | ~~This register entry (added at resolution).~~ | **Resolved 2026-05-23 (PLA059).** Migration [242_api_keys_manage_permission.sql](../db/mmff_vector/schema/242_api_keys_manage_permission.sql) seeds dedicated `api_keys.manage` permission (granted to `grp_global` + `grp_portfolio`, mirroring prior `users.list` holders so no operator loses capability). [`backend/internal/permissions/catalogue.go`](../backend/internal/permissions/catalogue.go) carries the matching `ApiKeysManage` constant. New [`auth.RequireUserAuth`](../backend/internal/auth/middleware.go) middleware denies API-key-only callers explicitly (403 + `Problem.Code: "user_auth_required"`). Route group rewritten with `RequireFreshPassword` → `RequireUserAuth` → `RequirePermission(ApiKeysManage)` → `RequireStepUpReauth("manage-api-keys")` on Issue + Revoke (List skips step-up — read-only audit data). Action-key registered in `sqlInsertReauthNonce` comment block. Seven middleware contract tests added in [`auth/middleware_test.go`](../backend/internal/auth/middleware_test.go). `go build ./...` clean. `go test ./internal/auth/... ./internal/apikeys/...` green. Docs: [`c_security.md`](c_security.md) "Privileged-action gates" + [`c_c_roles_permissions.md`](c_c_roles_permissions.md) permission table. |
| ~~TD-SQL-DRIFT~~ | 2026-05-23 | ~~S3~~ | ~~Backend / SQL placement hygiene~~ | ~~Two packages still carried inline backtick SQL in non-`sql.go` files at PLA060 B16.13 ship time: `internal/ranking` (6 sites — `fmt.Sprintf` table-name templated queries across `service.go` + `position.go`) and `internal/polymorphicrefs` (4 sites — 2 printf-format + 2 plain SQL across `service.go`). Both used `fmt.Sprintf("… %s …", table)` to splice a table name from the resource config, which is why the SQL stayed inline rather than landing as plain consts. Allowlisted in [`backend/internal/lintchecks/sql_placement_test.go`](../backend/internal/lintchecks/sql_placement_test.go) `sqlPlacementAllowlist`.~~ | ~~Next edit of either package.~~ | ~~The lint test froze current state — neither package could grow new inline SQL.~~ | **Resolved 2026-05-23 (PLA060 follow-up pay-down).** Both packages now carry their own `sql.go`: [`backend/internal/ranking/sql.go`](../backend/internal/ranking/sql.go) with 6 printf-format constants (`sqlUpdateRankPositionFmt`, `sqlSelectRankPositionFmt`, `sqlSelectRankRowForUpdateFmt`, `sqlSelectRankCohortBacklogFmt`, `sqlSelectRankCohortScopedFmt`, `sqlRebalanceRanksFmt`) and [`backend/internal/polymorphicrefs/sql.go`](../backend/internal/polymorphicrefs/sql.go) with 2 plain SQL consts + 2 printf-format consts (`sqlSelectPolymorphicParentForUpdateFmt`, `sqlInsertEntityStakeholder`, `sqlInsertPageEntityRef`, `sqlDeletePolymorphicChildFmt`). Service callers compose final SQL via `fmt.Sprintf(sqlFooFmt, table)` at call time — same behaviour, no signature changes. `sqlPlacementAllowlist` is now **empty**: every package under `backend/internal/` routes SQL through `sql.go`. `go test ./internal/{ranking,polymorphicrefs,lintchecks}/...` green. Canary-verified post-conversion that the ratchet still fires on a fresh inline SQL literal in a non-exempt file. |
| TD-HTTPERR-LINT-COVERAGE | 2026-05-23 | S3 | Backend / error-response wire shape | The new lint test [`backend/internal/lintchecks/http_error_placement_test.go`](../backend/internal/lintchecks/http_error_placement_test.go) (PLA060 B16.12.6) found 5 additional product files that still call `http.Error` for product-path errors instead of going through `httperr.Write` / `httperr.WriteCoded`: `nav/entities.go`, `portfoliomodels/adopt.go`, `portfoliomodels/adopt_stream.go`, `portfoliomodels/adoption_state.go`, `portfoliomodels/handler_workspace_layers.go`. These were out of scope for PLA060's named-file conversions but are visible to the new gate. | Next edit of any allowlisted file ships in the same PR as its httperr conversion + an entry removal from the allowlist. Or: a procurement audit asks "what does a 500 body look like across all endpoints?" — heterogeneous text/plain vs problem+json answers fail the question. | The lint's allowlist (5 entries today) freezes current state and fails on any NEW offender, so the surface cannot grow. Existing rows still leak plain-text errors but the population is closed. | When a story touches one of the listed files: convert each `http.Error` call to the appropriate `httperr.Write` / `httperr.WriteCoded` with `usermessages.*` user-facing strings + `log.Printf` for the raw err. Remove the file from `httpErrorAllowlist`. Re-run `go test ./internal/lintchecks/...` (still green). ~10 min per file. |
| TD-SCOPES-DEFAULT | 2026-05-23 | S2 | Backend / auth — API key default privilege | `apikeys.HasScope` returns true when the key's `admin_api_keys_scopes` array is empty (back-compat for legacy seeded keys including the dev key, all of which currently have `'{}'`). New keys issued post-PLA060 SHOULD default to least-privilege, but until the issuance UI ships there is no UX gate to enforce "give the user a list of scopes to tick before minting." | Issuance UI for API keys ships (deferred — see [`backend/internal/apikeys/dev.go`](../backend/internal/apikeys/dev.go) header note). At that point an empty `scopes` array means "user clicked through with no scopes selected" rather than "legacy key from before scope enforcement," and the back-compat branch becomes wrong. | This register entry + a doc-comment on `apikeys.HasScope` naming the back-compat behaviour + the existing `RequireScope` middleware on data-plane routes (denies any key whose scopes don't contain the required scope, but lets empty-scopes through). | Flip the empty-scopes branch in `HasScope` from `return true` to `return false`. Issuance UI enforces "must select at least one scope" at the form level. Update the legacy dev key (and any pre-PLA060 seeded keys) by `UPDATE admin_api_keys SET admin_api_keys_scopes = ARRAY['work_items:read', 'work_items:write', 'portfolio_items:read', 'portfolio_items:write']` in the same PR so nothing breaks. ~30 min including the migration. |
| TD-SCOPES-COVERAGE | 2026-05-23 | S3 | Backend / auth — scope enforcement coverage | PLA060 B16.11 wires `RequireScope` on `/samantha/v2/work_items*` + `/samantha/v2/portfolio_items*` only (4 named scopes: read/write per resource). The wider `/samantha/v2` surface (~260 endpoints across artefacts, types, flows, fields, timeboxes, etc.) is NOT scope-gated yet. An API key with `work_items:read` only can today still hit `/samantha/v2/flows` or `/samantha/v2/artefact_types` because those routes have no `RequireScope` wrap. | First buyer audit asks "can a read-only key fetch our flow definitions?" or "can a write key for work items mutate type catalogues?" Today's answer is "yes" because the gate is opt-in per route. | This register entry. The two flagship resources (work_items + portfolio_items) carry the gate; everything else falls back to "any valid key authenticates." The cross-tenant filter on subscription_id (PLA060 B16.10) still applies, so the failure mode is intra-tenant over-privilege, not cross-tenant leakage. | Define the full scope vocabulary (one read + one write per resource family — flows, artefact_types, fields, timeboxes, etc.) and stamp `RequireScope` on each route group in `main.go`. Same pattern as B16.11.4/.5. Pair with the issuance UI work so newly minted keys ship with the right defaults. ~1 day for the wider sweep including handler tests per resource. |
| ~~TD-AUTH-KEYS-002~~ | 2026-05-23 | ~~S1~~ | ~~Backend / auth — tenant isolation on credential revoke~~ | ~~[`apikeys.Service.Revoke`](../backend/internal/apikeys/apikeys.go) issued `UPDATE admin_api_keys SET admin_api_keys_revoked_at = now() WHERE admin_api_keys_id = $1` with NO subscription clause. Any authenticated caller who learnt or guessed another tenant's key UUID could POST `/admin/api-keys/revoke` and disable that tenant's programmatic access. Combined with the dual-auth handler (which never read SubscriptionID from auth ctx on the revoke path), this was a clean cross-tenant credentials-revocation bug — exactly the class of failure procurement audits look for first.~~ | ~~Defence/finance procurement: "show me cross-tenant isolation on every credential mutation." Today's answer (none on the revoke path) fails NIST 800-53 AC-3 + SOC 2 CC6.1. Active exploit path: a caller with subscription A revokes subscription B's key; B loses programmatic access mid-flight; A leaves no fingerprint without the audit row.~~ | ~~This register entry (added at resolution).~~ | **Resolved 2026-05-23 (PLA060 B16.10).** `Service.Revoke` signature is now `Revoke(ctx, keyID, subscriptionID string) error`; SQL adds `AND admin_api_keys_id_subscription = $2 AND admin_api_keys_revoked_at IS NULL`; 0 rows affected → `ErrKeyNotFound` sentinel. `Handler.Revoke` resolves the actor's subscription from `auth.UserFromCtx`, maps `ErrKeyNotFound` to RFC 9457 404 (`code: "key_not_found"`) — never confirms cross-tenant existence to an enumerating caller. Audit-log row per attempt (action `api_key.revoke`, outcome `success|denied`; denials record reason `cross_tenant_or_missing` and BOTH subscription IDs for forensics — via new `Service.LookupOwningSubscription` helper that keeps the inline SQL out of the handler). Tests `TestRevoke_SameTenant_204` + `TestRevoke_CrossTenant_404` ([backend/internal/apikeys/handler_revoke_test.go](../backend/internal/apikeys/handler_revoke_test.go)) seed two subscriptions, exercise both paths against the live dev DB + `audit_logs` in vector_artefacts, and pin: 204 + revoked_at IS NOT NULL same-tenant; 404 + problem+json + revoked_at IS NULL cross-tenant; one audit row per call with the right outcome/reason/subscription ids. `go build ./...` + `go vet ./...` clean. |
| ~~TD-CREATE-CUSTOM-FIELDS-AT-MOUNT~~ | 2026-05-23 | ~~S3~~ | ~~Frontend + Backend / create flow~~ | ~~V2 create flyout had no custom-field section; per-type bindings were reachable only post-create via the inline edit form (which itself didn't render them).~~ | ~~n/a — closed.~~ | ~~n/a — closed.~~ | **Resolved 2026-05-23.** Backend: new `GET /_site/{work-items|portfolio-items}/types/{typeId}/fields` returns the schema (`field_library_id` / `field_name` / `label` / `field_type` / `options_json` / `position` / `required` / `default_value`) ordered by position; subscription-clamped via `artefacts_types_id_subscription` JOIN. `POST /{resource}` extended with `custom_fields: [{ field_library_id, string_value?/number_value?/text_value?/date_value? }]`; values land inside the create transaction (atomic — invalid value rolls the whole insert back). Frontend: `useFieldsForType` hook + `<CreateCustomFields>` component render per-type inputs in the V2 create flyout; `customFieldsToWire` routes values to the correct *_value column by field_type. The inline EDIT form (`ArtefactInlineForm`) ALSO gained a custom-fields section via `<EditCustomFields>` — same primitives, on-blur PUT to `/{resource}/{id}/field-values`. Used today by Risk (risk_score / risk_impact / risk_probability + 9 optional bindings per migration 075). |
| TD-OBJECTTREE-V1-RETIRE | 2026-05-23 | S3 | Frontend / dead component | `app/components/ObjectTree/` (V1) + `app/components/ObjectTree/configs/p_wizard_*.json` are now unused. The three user-facing pages that consumed V1 (`app/(user)/work-items/page.tsx`, `app/(user)/portfolio-items/page.tsx`, `app/(user)/risk/page.tsx`) were swapped to `app/components/ObjectTreeV2/` this session along with a scope/createableTypeIds-aware Create-New picker. V1 stays on disk as a one-sprint rollback fallback. The V1 sidecar JSONs are byte-identical to V2's (verified pre-swap), so the duplication is harmless but it does mean any sidecar edit must be made in both directories until V1 is deleted. | (a) V2 has run hot for ≥1 sprint without rollback; OR (b) a developer edits a V1 sidecar by mistake and the V2 page silently doesn't pick up the change (different file paths). | This register entry. The three pages no longer import from `@/app/components/ObjectTree/` — grep is the canary. | Delete `app/components/ObjectTree/` entirely (component + sidecar JSON dir + index.tsx); grep for any stragglers importing from that path; if `useArtefactItemsWindow` or related work-items-tree-config helpers are V1-only, delete those too (V2 uses `useObjectTreeWindow` instead). Single PR, ~10 min. |
| TD-DEPS-IMPORT-CYCLES | 2026-05-23 | S3 | Frontend / module graph hygiene | The TS module graph carried three import cycles before this session's pay-down: (1) **runtime** — `AuthContext` → `Sentinel` → `ScopeContext` → `Sentinel` (Sentinel held a module-level `_scopeReload` registry while also importing `useAuth`+`useScope` for its composed view); (2) **type-only** — `app/(user)/portfolio-model/page.tsx` ↔ `LayersPreviewTable.tsx` (table imported `LayerDTO` from its own parent page); (3) **type-only** — `app/components/work-items-tree-config.tsx` ↔ `app/lib/shareableParams.ts` (shareableParams imported `WorkItemsFilters`/`SortKey`/`SortDir` from the config module that imported it back). Type cycles don't crash but they pin TS performance, blur ownership ("which file defines this type — the one with the value, or the importer?"), and disguise runtime cycles in tools that don't distinguish. Runtime cycles (#1) are the dangerous class: under hot-reload or test-mode imports, one of the two modules sees a partially-initialised export from the other and silently no-ops. The fix shape — extract module-level shared state to a neutral leaf, move shared types to a sibling `.types.ts` — is the playbook for any future cycle. | `npx madge --circular --extensions ts,tsx --ts-config tsconfig.json app/ dev/` returns ≥1 cycle. Run quarterly OR after any large refactor that moves module-level state (registries, event buses, shared refs) into a context file. | This register entry + the three fixes shipped this session: `app/contexts/scopeReloadRegistry.ts` (neutral leaf for the scope-reload registry; Sentinel re-exports for back-compat), `app/(user)/portfolio-model/types.ts` (`LayerDTO`), `app/components/work-items-tree-config.types.ts` (`SortKey`/`SortDir`/`WorkItemsFilters`). Madge confirms 0 cycles across 412 files post-fix. | When trigger fires (madge reports a new cycle): identify the cycle class first — if module-level shared state (a `let _registry = …`, an event emitter, a module-scoped ref) bridges two contexts, extract it to a `*Registry.ts` / `*Bus.ts` neutral leaf with no React or context imports. If a child module needs a type from its parent, the type belongs in a sibling `.types.ts` that both can import. Re-exports on the original module preserve back-compat. Add the cycle scan to CI (`madge --circular --no-color --exit-code` in a workflow step) once the pattern is in place, so the next cycle fails the build instead of the audit. |
| TD-RANK-PARTITION-PARENT-NOT-SPRINT | 2026-05-21 | S2 | Backend / domain logic + frontend wire shape | The `work_item` ranking resource (`backend/internal/ranking/service_integration_test.go:65`) currently partitions on `ScopeColumn: artefacts_id_timebox_sprint`. Result: `/rank/move` reorders rows against sprint peers, not parent peers. That's the wrong shape per the design decision logged this session: an artefact's CANONICAL position is its position in the parent's child list (where it lives in the work tree). The sprint is a TEMPORARY allocation; sprint-local ordering should be a separate concern. Today, dragging a Story to the top of a sprint backlog mutates `artefacts.position` partitioned by sprint — when the Story is later removed from the sprint, that position number is meaningless against its parent peers and the row can land anywhere. | (a) User drags a Story to top of Sprint 5's backlog, removes it from the sprint, finds it in a confusing spot in the overall backlog. (b) The cross-parent drop UX (drag-to-reparent + position) needs sibling-position semantics that work against the new parent's children, not the new parent's sprint allocation. Today's cross-parent drops can only "append to end" because sprint partitioning would give nonsense positions. (c) Any reporting view that wants "top N items in the Epic" against the canonical work tree. | This register entry. Cross-parent drops currently land at the end of the new parent's child list (the create-time `MAX + 100` default fires when no position is explicitly set). Sprint planning still works because sprint-local position is whatever the user last set. | Re-partition `work_item` ranking by `parent_artefact_id`. Steps: (1) Change the `ResourceConfig.ScopeColumn` from `artefacts_id_timebox_sprint` to `parent_artefact_id`. (2) One-shot data migration: group by `parent_artefact_id` (NULL = root) and renumber `position` in current sort order, gap-100 stepping. (3) Introduce a separate sprint-local ordering surface — `sprint_artefact_ranks(sprint_id, artefact_id, position)` join table OR a `sprint_position` column on `artefacts` partitioned by sprint. Read only when the user is planning that sprint; the Story's parent-position survives sprint removal untouched. (4) Same pattern for `release_artefact_ranks` if releases need ordered planning. (5) Cross-parent drag-drop drops gain proper position semantics (before/after target's CHILDREN, not just append). One backend story with a migration; medium-sized. Touches: ranking config + a migration + reads on the sprint/release planning surfaces. The drag-to-reparent feature shipped this session works fine without it — only the position fine-tuning under a new parent is degraded. |
| TD-SCOPE-GHOST-LS-READS | 2026-05-24 | S1 | Frontend / wire-layer scope clamp | `app/lib/api.ts::withForwardedMeg` used to fall back to `localStorage.getItem("vector.scope.activeNodeId")` when the page URL had no `?meg=`. The writer for that key was deleted with `ScopeContext` in PLA062 S22 (commit `d14bcc70`), but the reader survived. So the localStorage value was a ghost — only ever containing stale residue from pre-S22 sessions. When `ObjectTreeV2` mounted on any page without `?meg=` in the URL (e.g. `/value-sprint`, `/value-status`, or `/dashboard` post-fresh-login), the function injected that stale UUID into wire requests under `(work-items\|portfolio-items)/*`. Backend handler `artefactitems.Facets` read the `?meg=` param, called `topology.CanReadScope` against the stale node, found no active grant for the current user, returned `ErrScopeForbidden` → 403 `scope_read_denied`. ObjectTreeV2 lost its facets + flow-states + types catalogue and rendered as an empty grid. The bug was invisible on `/work-items` itself because SentinelProvider mirrors `sentinel_focus_node` into `?meg=` on that route, so the URL value won over localStorage. Compounded by [TD-SENT-URL-MIRROR-PATHNAME] below: the URL-mirror effect didn't re-fire on client-side navigation, so `?meg=` was bare on every fresh route. | Anyone writes new code that reads a retired ScopeContext localStorage key. The retired key list (extracted from pre-S22 git history): `vector.scope.activeNodeId`, `vector.scope.direction`, `vector.scope.activeWorkspaceId`, `vector.scope.tenantId`. | **Fix shipped 2026-05-24** — `app/lib/api.ts:153` no longer reads localStorage; URL is sole source. SentinelProvider URL-mirror effect (`app/sentinel/SentinelProvider.tsx:357`) now depends on `pathname` and re-stamps `?meg=` after every nav. Regression test pinned in `app/sentinel/__tests__/sentinel_provider.test.tsx` Case 20. | Add a lint rule banning `localStorage.{get,set,remove}Item("vector.scope.*")` outside an exempt registry (start the registry empty — no production code should read these). Pattern matches the existing `lint:no-direct-workspace-id` + `lint:no-old-context-imports` registry-with-allowlist shape (see `dev/scripts/lint_no_old_context_imports.py`). Single Python lint script + a small registry JSON; ~30 min. |
| TD-SENT-URL-MIRROR-PATHNAME | 2026-05-24 | S2 | Frontend / Sentinel URL contract | The Sentinel URL-mirror effect (`app/sentinel/SentinelProvider.tsx:357`) writes `?meg=<focus>` to the address bar via `replaceState` after boot, but originally depended only on internal state (`state.loading`, `state.focus_override`, `state.url_focus`, `state.user`, `state.tenant_root`) — not on `pathname`. Next.js client-side `router.push("/somewhere")` drops query params; the effect didn't re-fire on the new route, so `?meg=` was missing on every navigation target until Sentinel state next changed (often never). The downstream cost — combined with TD-SCOPE-GHOST-LS-READS above — was wire-layer scope clamps degrading to either nothing (post-fix) or stale ghost values (pre-fix). The "bookmarkable scope" promise of `?meg=` was effectively broken: refreshing a page mid-session preserved scope, but clicking a nav link silently dropped it. | Any new client-side nav lands on a page where `?meg=` should appear in the URL and doesn't. Hard refresh would then show the page differently — the "share-this-URL works" guarantee falls over. | **Fix shipped 2026-05-24** — added `usePathname()` to SentinelProvider; `pathname` included in the URL-mirror effect dep array. Regression test pinned in `app/sentinel/__tests__/sentinel_provider.test.tsx` Case 20. | Add a Playwright spec under `dev/tests/playwright/` that logs in, navigates through 3+ pages (Dashboard → Value Status → Work Items), and asserts `?meg=` is present in the URL at each landing. Catches regressions from any future refactor that drops the `pathname` dep or moves URL-mirror logic out of SentinelProvider. ~30 min once a Playwright auth fixture exists for `claude_3_test@mmffdev.com`. |
| ~~TD-AUTH-JWT-WORKSPACE-CLAIM-REFRESH~~ | 2026-05-25 | ~~S2~~ | ~~Backend / auth — refresh-path workspace claim loss~~ | ~~`auth.Login` resolved the user's workspace via `resolveDefaultWorkspace` and stamped `u.WorkspaceID` before `SignAccessToken` so the first JWT carried the `workspace_id` claim. `auth.Refresh` (and the grace-window `refreshFromSuccessor`) called `FindUserByID` to rehydrate the user, then `SignAccessToken(u, ...)` — but `FindUserByID` does NOT scan `WorkspaceID` (no DB column; it's a transient per-session selection on `roletypes.User`). So `u.WorkspaceID == uuid.Nil` on every refresh, `SignAccessToken` omitted the claim per the `omitempty` contract, and `sentinel.Middleware` fell back to `FirstLiveWorkspace` — which returned whichever workspace was created first in the tenant, NOT the workspace the user picked. Compounded by the pre-existing `sentinel.sqlFirstLiveWorkspace` referencing a placeholder `workspace` table that ships empty — every legacy-token request 403'd until 2026-05-25.~~ | ~~Defence/finance procurement audit: "show me the user's picked workspace survives the refresh-token cycle." Today's answer (it doesn't — silently reverts to tenant's earliest) fails SOC 2 CC6.1 + the threat model in `context/USER.md`. Active user-facing bite: any user with grants on >1 workspace who picked a non-default. Padmin's dev account had exactly 1 grant so the bug was invisible to single-grant smoke testing.~~ | ~~This register entry (added at resolution).~~ | **Resolved 2026-05-25 (commits `1bdbfebc`..`1649231f`).** Two-pronged fix: (1) **Sentinel SQL tightened** — `sqlFirstLiveWorkspace` now JOINs `users_roles_workspaces` so the fallback can only return workspaces the actor holds an active grant on. Closes the bug class for any code path that still hits the fallback. `Resolver.FirstLiveWorkspace` signature gained `userID uuid.UUID`. (2) **`auth.Refresh` re-derives `WorkspaceID`** via new `deriveWorkspaceForRefresh` helper inserted between `FindUserByID` and `SignAccessToken` in both `Refresh` AND `refreshFromSuccessor`. Three-tier resolution: Path A = `users.default_focus_node_id → topology_nodes.workspace_id` validated against `users_roles_workspaces` grant; Path B = first granted workspace fallback; Path C = leave nil (JWT signs without claim; sentinel 403s `no-workspace`). Fail-open on resolver errors (refresh is hot-path; sentinel.HasActiveRole remains the authoritative downstream gate). Auth depends on a small `WorkspaceResolver` interface (mirrors `PermissionResolver` precedent); concrete impl in new package `backend/internal/workspaceresolver/` wired in main.go onto `authSvc.WorkspaceResolver`, guarded on `vaPool != nil` for pre-cutover env degradation. Verified live: padmin login → navigate `/value-*` → refresh → dev-debug panel still shows ✓ match with `user.workspace_id (JWT) = a4df2e21-...`, NOT zeros. Spec + plan: [docs/superpowers/specs/2026-05-25-jwt-workspace-claim-refresh-design.md](superpowers/specs/2026-05-25-jwt-workspace-claim-refresh-design.md). |
| TD-NAV-MAXPINNED-SEED-OVERCAP | 2026-05-25 | S2 | Backend / nav prefs validator | **HACK in production.** `MaxPinned` was bumped from 50 → 100 in `backend/internal/nav/service.go:20` (mirrored in `app/user/navigation/page.tsx:49`) to clear `ErrTooManyPinned` on the new account-settings homepage dropdown's read-modify-write PUT. The actual bug is that the seed (`sqlBackfillDefaultPinnedPages` running on first read after the lazy-seed in `Service.ResolveProfile`) inserts every `pages.default_pinned=TRUE` row the user's role permits — for padmin/gadmin that's currently 51, which exceeded the prior cap of 50. The seed has no guardrail against `MaxPinned`; it just dumps everything in. Bumping the cap moved the tripwire, didn't fix the cause. Next time `pages.default_pinned=TRUE` grows past 100, the same bug returns and whoever inherits it spends two days the way I just did. | (a) Someone adds the 101st `default_pinned=TRUE` page to `pages` (very likely within months — `value-*` and `dev-*` buckets keep growing). (b) Any feature that does read-modify-write of the pinned list (the new homepage dropdown, `/user/navigation` save) starts failing for higher-role users with `ErrTooManyPinned`. (c) Lower-role users (regular `user@`) are insulated only because `users_roles_pages` filters out admin pages — their seed stays well under cap, masking the bug. | Cap bumped 50 → 100. Comments in `service.go:16-23` name the prior cause + this TD. New hard rule in `.claude/CLAUDE.md` forbids hacks disguised as fixes so the next person doesn't bump 100 → 200 without thinking. | Three possible proper fixes, in increasing order of correctness: **(A) Seed respects the cap.** Add `LIMIT (SELECT MaxPinned)` or equivalent priority-ranking to `sqlBackfillDefaultPinnedPages` so the seed never exceeds the cap; document the prioritisation rule on `pages` (likely a new `pages_default_pinned_priority` int column). Small backend change + 1 migration. **(B) Cap counts only user-added pins, not seeded defaults.** Add `users_nav_prefs_is_seeded boolean NOT NULL DEFAULT FALSE` column; seed sets it TRUE; `validatePinned`'s size check excludes `is_seeded=TRUE` rows from the count. The cap then protects against runaway USER pinning (the actual threat model), not the seed. Medium change: migration + service.go + a guard in `ReplacePrefsForProfile` to preserve `is_seeded` across PUT cycles (the wire payload doesn't carry it; needs server-side merge). **(C) Remove the cap entirely.** The cap is arbitrary — there's no UI ceiling at 50 or 100 that would justify it; the rail scrolls. If 500 pinned items is fine performance-wise (it is: O(N) renders, all client-side, no SQL fanout), drop the validator entirely. Smallest code change but removes a guard against pathological user behaviour. **Recommendation: (B).** Matches the threat model, is a one-PR change, and makes the seed cap-immune by design. Trigger to start the work: anyone touches `MaxPinned` again, OR `count(pages.default_pinned=TRUE) > 80`. |

| TD-CUT1.1.1-BOOKMARK-SURFACE | 2026-05-25 | S2 | Backend+Frontend / entity bookmark surface | CUT1.1.1 (PLA064) dropped `portfolio` and `product` tables (0 rows). The bookmark surface (`nav/bookmarks.go` `Pin/Unpin`, `polymorphicrefs.InsertPageEntityRef`) serviced those entity kinds. After the drop: `Bookmarks.Valid()` returns false for all kinds → `Pin` rejects at the gate; `InsertPageEntityRef` returns `ErrUnknownEntityKind` for all kinds. The `page_entity_refs` table retains its `CHECK entity_kind IN ('portfolio','product')` constraint but no new rows can land there (dispatch trigger dropped + Go-layer rejection). The frontend `NavPrefsContext.bookmark(kind, id)` call-site, `apiSite.nav.addBookmark`, and the `EntityKind` type are all live code that now always returns a 422/unknown-kind response. `app/(user)/product/[id]/page.tsx` was deleted (pure placeholder). `listEntities` API method removed from `apiSite`. | Someone tries to add a real Portfolio or Value-Product entity type in the future and wires it up as a bookmarkable entity — they'll find the service returns `ErrUnknownEntityKind` and the frontend bookmark button does nothing. Alternatively, if entity bookmarking is never needed (likely given no portfolio concept shipped), the frontend dead code (EntityKind type, bookmark/unbookmark callbacks) should be removed. | This register entry. Comments in `bookmarks.go`, `service.go`, `NavPrefsContext.tsx` (EntityKind type) point here. | **Pay-down path A (no entity bookmarks ever):** Delete `EntityKind` type + `bookmark/unbookmark` from `NavPrefsContext`, remove `addBookmark`/`removeBookmark`/`checkBookmark` from `apiSite`, drop the `page_entity_refs` table's `entity_kind` CHECK constraint (now vacuously enforced by 0 rows + no Go writer), delete `KindPortfolio`/`KindProduct`/`KindWorkspace` etc. constants from the service. ~60 min cleanup; no migration needed beyond a comment. **Pay-down path B (new entity type needs bookmarking):** Introduce a new `KindArtefactType EntityKind = "artefact_type"` or whatever maps to `vector_artefacts` rows, wire `parentTableFor` to the new table, update `page_entity_refs` CHECK to include the new kind, write the Go handler. The current stubbed-out surface makes this straightforward. **Recommendation:** Wait 2 sprints. If no entity bookmark feature is on the backlog by then, execute Path A. |
| TD-NOTIF-V2-OUTBOX-NOTIFY-TRIGGER | 2026-05-27 | S3 | Backend / notifications-v2 relay wakeup latency | `notifications_outbox_v2` (mig 122) lacks a `pg_notify('notifications_outbox_v2_inserted', ...)` trigger on INSERT, so the v2 relay's LISTEN/NOTIFY wakeup path (`backend/internal/notifications/v2/relay/relay.go` `listen`/`listenLoop`, const `listenChannel = "notifications_outbox_v2_inserted"`) is effectively tick-only — the relay drains on the 5s safety-net ticker. The LISTEN goroutine binds the channel and waits, but no NOTIFY ever arrives, so the wakeup channel only fires on `<-ticker.C`. End-to-end latency from outbox INSERT to dispatcher publish is therefore bounded by the tick interval (5s default), not the broker round-trip. | (a) v2 ships to staging and the 5s wakeup latency becomes user-observable for in-app notifications (toast/badge "felt slow"); OR (b) outbox spike rate exceeds what one 5s tick can drain in a single batch (50 rows/tick = 10/s sustained — fine for current load, a known ceiling). Performance-only — no correctness impact. The drain still happens within 5s; SKIP LOCKED prevents double-delivery; failed publishes un-claim correctly. | This register entry + the relay's `listenChannel` const carries an inline TD comment pointing here. The ticker safety-net guarantees drain within `DefaultTickInterval` (5s) regardless. | Add a migration creating an AFTER INSERT trigger on `notifications_outbox_v2` that calls `pg_notify('notifications_outbox_v2_inserted', NEW.notifications_outbox_v2_id::text)`. The relay already binds the LISTEN and coalesces wakeups; no Go-side change needed. ~10 min: one schema migration + apply + verify relay logs show LISTEN-driven drains instead of only ticker-driven. Pin a regression test that inserts a row and asserts the LISTEN channel fires within 100ms. |
| TD-NOTIF-V2-TEMPLATES-CHANNEL-TYPING | 2026-05-27 | S3 | Backend / notifications-v2 templates | `backend/internal/notifications/v2/templates/service.go` carries `Channel string` on `Template`, `ListFilter`, and on the `Render(ctx, eventType, channel, locale, data)` / `lookup` / Upsert signatures instead of `domain.Channel`. This is a residue of dispatch order: S08 was cut from main (`1c81202e`) before S02 (`feature/notifications-v2`+S02 = `ada34a4a`) landed, so the worker had no `domain.Channel` type to import. The string values used in seed mig 131 (`'in_app'`, `'email'`) match `domain.Channel` constant values byte-for-byte, so the wire contract is correct — only the Go type loses the compile-time guarantee. Render callers (S06 pipeline, future S09 dispatchers) will receive `string` from the outbox row and pass it straight in, also losing the type. | (a) S09 dispatchers introduce a new channel value that diverges from `domain.Channel` and no compiler error catches it because the column is `text` and the templates service signature is `string`. (b) Templates service ships to staging and a typo'd channel (`"e-mail"` vs `"email"`) hits production via the seed editor (S11) without a typecheck. (c) A future refactor swaps `domain.Channel` constants for typed enums — `templates` won't get the rename treatment because the compiler can't find the usages. | This register entry. Inline TD comment in `templates/service.go` (lines 38–40) already points here ("Until S02 lands and the validator merges this story, we accept Channel/EventType as string"). | One commit: change `Channel string` → `Channel domain.Channel` in `Template`, `ListFilter`, `Render`, `lookup`, `Upsert`, `GetByID`, `List`, `SetActive`. Touch ~20 call-sites and pass through the existing string values (they round-trip through `domain.Channel(s)` losslessly). One package import in service.go: `"github.com/mmffdev/vector-backend/internal/notifications/v2/domain"`. No DB migration needed — the column stays `text`; the marshalling on the way out is `string(c)`. ~15 min, low risk, all tests should pass unchanged. Trigger to start: S06 pipeline lands (it will be the first big consumer of `Render`), OR any time a developer touches `templates/service.go` for any reason. |
| TD-FLOWBOARD-EXIT-RULES | 2026-05-27 | S2 | FlowBoard / transitions | Exit-rule predicates defined in mig 045 (`flows_transitions.flows_transitions_exit_rules JSONB`) are not yet evaluated by the FlowBoard drag path. `useFlowStateTransitions` gates hard-blocked transitions (no `flows_transitions` row) but does not evaluate JSON-predicate exit rules on the source card (e.g. "can't leave Doing until `story_points` is set"). The backend PATCH (`/_site/artefacts/:id/flow-state`) also does not validate exit rules — the predicate evaluation engine was out of scope for the v1 ship slice (spec §11.3). | First tenant reports "I expected this transition to be blocked by a precondition I configured." Likely during a demo or early-access run once exit-rule authoring ships in the workflow editor. | This register entry + inline comment at `useFlowStateTransitions.ts` pointing here. | Implement predicate evaluation in two passes: (1) frontend — `useFlowStateTransitions` fetches the outbound `flows_transitions` row for the source state, parses `exit_rules` JSONB, evaluates against the current card's field values; disallowed transitions are added to the hard-blocked set so the drag visually dims them. (2) backend — the PATCH handler loads the transition row and re-evaluates exit rules server-side before committing the state change (the server is the authoritative gate per the `HARD RULE — SERVER IS THE GATE`). Predicate language is already in the DB schema; the evaluator is the new work. ~2 days backend + frontend. |
| TD-FLOWBOARD-CARD-PREFS-UI | 2026-05-27 | S3 | FlowBoard / card field selection | Per-user card-field preferences are stored in `users_flowboard_prefs` (mig 134) and surfaced via the existing `GET/PUT /_site/artefacts/card-prefs` endpoints (FB1.2.3). The v1 board uses `resolvedConfig.card.default_fields` (from the sidecar JSON) for every user — the DB-stored prefs are read by the backend but no UI lets the user edit which fields appear on their cards. The read path is wired; the editor UI is the gap. | A user asks "can I change what shows on my cards?" — the answer is "yes in the backend, no UI yet." Likely at first stakeholder demo. | This register entry. The API endpoints exist and are exercised by tests. A user who knows the API contract can `PUT /_site/artefacts/card-prefs` directly. | Build a card-prefs editor: a small popover or settings sheet accessible from the FlowBoard toolbar (or the WipGearButton area) that renders a checkbox list of available fields from the artefact type's field catalogue, checked against the user's current prefs, and PUTs on save. Frontend-only once the API exists. ~1 day. |
| TD-FLOWBOARD-WIP-AUDIT | 2026-05-27 | S2 | FlowBoard / audit trail | `topology_nodes_wip_limits` (mig 133) records `topology_nodes_wip_limits_updated_at` and `topology_nodes_wip_limits_updated_by` on the row itself, but there is no history table — each UPSERT overwrites the prior value. A SOC 2 audit that asks "who changed the Doing WIP limit from 3 to 5 and when?" cannot be answered from the database; only the most-recent value survives. | SOC 2 / defence / finance audit asks for a history of WIP-limit changes. Also fires if an accidental WIP change goes undetected (no rollback path without history). | This register entry. The `updated_at` / `updated_by` columns on the current row provide the last-write answer only. | Add a `topology_nodes_wip_limits_history` table: `(id UUID PK, node_id UUID, flow_state_id UUID, old_limit INT, new_limit INT, changed_by UUID, changed_at TIMESTAMPTZ)`. Populate it via a trigger on `topology_nodes_wip_limits` on UPDATE (compare OLD.limit ≠ NEW.limit before inserting). Expose via `GET /_site/topology/:id/wip-limits/history` (admin-only, sentinel-clamped). ~half day: one migration + one endpoint + one test. |

| TD-ERD-LARGE-GRAPH-PERF | 2026-05-27 | S3 | Frontend / Dev ERD page | Cytoscape default canvas renderer + dagre layout may struggle once table count crosses ~150 (vector_artefacts is currently ~71 + mmff_library ~12). Symptom: dagre layout time visibly slow, pan/zoom janks at low zoom levels. | dagre layout > 500ms on `/dev/erd` load (timeable via DevTools performance panel) OR table count crosses 150. | This register entry; the page already filters via the rail so users can sub-divide the graph today. | Switch Cytoscape to the WebGL renderer (`cytoscape-cose-bilkent` + canvas WebGL backend or migrate to `react-flow` which is already a project dep for the Visualiser). ~half-day refactor isolated to `dev/pages/DevErdCanvas.tsx`. |
| TD-ERD-SOFT-REF-EVIDENCE | 2026-05-27 | S2 | DevOps / ERD snapshot freshness | The ERD payload's `soft_ref` edges are injected via `erd.Service.SetSoftRefs([]Edge)` from SY003 (Vector substrate paper). The handler currently calls `SetSoftRefs(nil)` — no soft refs land on the diagram until somebody wires the SY003 parser. When the wire is built, the soft-ref set will go stale every time SY003 regenerates (after migrations, after cross-DB writers ship). | The first soft-ref writer ships and ERD's soft_ref edges disagree with SY003 evidence pointers. | This register entry. `erd.Service.SetSoftRefs` exists as the injection point so future SY003 integration is one-line. | Add a small helper `internal/erd/softrefs.go` that parses `dev_reports.SY003` (via `devreports.Service.GetByID("SY003")`), extracts the soft-ref markdown table or fenced JSON, builds `[]Edge`, and calls `erdSvc.SetSoftRefs(...)` from `main.go` after dev_reports is wired. Then chain `<report> -sy` to POST `/_site/admin/dev/erd` so the ERD snapshot regenerates whenever SY003 does. |
| TD-VALUE-SPRINT-RANK-PARTITION | 2026-05-28 | S2 | Backend / domain logic | Child of [TD-RANK-PARTITION-PARENT-NOT-SPRINT](#td-rank-partition-parent-not-sprint) (TD-0185). The value-sprint planning surface (`app/(user)/value-sprint/page.tsx`) now lets users move work items between sprints via per-row "Add to Sprint" / "Target Sprint" / "Move to Sprint" buttons and the bulk equivalents — every move calls `PATCH /work-items/{id}` with `sprint_id`. Because `work_item` ranking still partitions on `artefacts_id_timebox_sprint` (per TD-0185), each row carries a `position` that's meaningful only against its current sprint peers. Moving Story A from Sprint 4 → Sprint 5 leaves the OLD partition's positions intact (Story A's old row in Sprint 4 simply vanishes; the remaining Sprint 4 rows now have a gap) and the NEW partition gets a fresh "MAX + 100" slot for Story A. Cross-sprint move is functionally fine but ranking gets noisier on every move: gap accumulation in source sprints, position drift relative to where the user dropped it, and "MAX + 100" never reflects the user's actual intent ("put this at the top"). | First user-visible mis-ordering after a sprint move that the user notices and complains about. Most likely scenario: bulk "Move 10 items to Sprint" — the 10 items all get `MAX + 100, MAX + 200, …` slots in arrival order, which doesn't match the order they appeared in the source sprint. Or: user drags Story A to top of Sprint 5, then later moves it back to Sprint 4 and finds it at the bottom. | This register entry. The cross-sprint move PATCH path is intentionally vanilla — it does NOT attempt to compute a position in the target sprint; the backend's `MAX + 100` create-time default fires by virtue of the position column being unset on PATCH. Comments in `app/(user)/value-sprint/page.tsx` `assignToSprint` / `assignManyToSprint` document the deferral and point here. | Same pay-down as parent TD-0185 — re-partition the canonical `work_item` ranking by `parent_artefact_id` and introduce a separate `sprint_artefact_ranks` (or `artefacts.sprint_position`) surface for sprint-local ordering. The value-sprint planning UX should additionally accept an optional "place at top" / "place at bottom" hint in the PATCH body so a bulk move can preserve source order in the target sprint instead of arrival-order shuffling. ~Same medium-size effort as the parent TD; the value-sprint surface is the first consumer that makes the symptom user-visible, so the trigger for pay-down is "first complaint about sprint-move ordering." |
| TD-WORKITEMS-SPRINT-ID-UUID-PARSE | 2026-05-28 | S3 | Backend / artefactitems handler | `GET /work-items?sprint_id=<value>` 500s with `{"error":"internal"}` when the value isn't a parseable UUID. `backend/internal/artefactitems/handler.go:325-326` stores the raw string into `f.SprintID` and passes it through to the service / SQL layer, which throws on the UUID cast. No `httperr.BadRequest` gate. Value-sprint hit this with a `__none__` sentinel value (used as a "match-nothing" filter when no sprint was loaded into the panel) — the frontend was patched to gate the panel ObjectTree on a real sprint id (`app/(user)/value-sprint/page.tsx` commit `<pending>`) so the bad request no longer fires from this caller. The backend remains exposed to any future caller that sends an unparseable string. | Any new caller (manual API exploration, a fuzzer, a wired-wrong frontend) sends `sprint_id=<non-uuid>`. Same trip exists on every other UUID filter parsed via the same pattern in this handler — `item_type_id`, `flow_state_id`, `priority_id`, `owner_id`, `parent_id`. Audit them in passing. | This register entry. Frontend mitigation pinned at `app/(user)/value-sprint/page.tsx::panelWizardConfig` (the panel tree only mounts when `panelSprintId` is a real UUID). | Wrap each UUID-shaped query param parse in `uuid.Parse(v)`; on `err`, return `httperr.BadRequest(w, "invalid sprint_id: must be a UUID")`. Apply the same pattern to the other UUID filters listed above. ~30 min: one helper `parseUUIDQueryParam(q url.Values, key string) (*string, *httperr.Problem)` and ~10 call-sites refactored. Add a single unit test that asserts 400 (not 500) for each filter with a malformed value. |
| TD-SENT-CACHE-EXPAND-COVERAGE | 2026-05-28 | RESOLVED | Backend / sentinel cache | **CLOSED 2026-05-28** (cycle 3, commits `2761d762` + `9f5f4038`). Final scope of the cache rollout: (a) sentinel middleware — `FocusWorkspace`, `GrantOnNode`, `ResolveSubtree` cached; (b) `/sentinel/boot` sub-reads — `auth.Service.LoadRoleAndPermissions` (3 sequential SQL → 1 cache GET) and `topology.Service.ListMyGrants` (+ gadmin variant). Invalidation: existing `subtreeCacheInvalidator` adapter extended to also wipe `topology:mygrants:{tenant}:*` on every topology structure write (16 callsites already in place — zero new code in topology); new `permCacheInvalidator` adapter wires 4 new callsites in `roles.Service` (AssignPermissions / RevokePermissions / Archive — namespace-wide wipe) and `users.Service.Update` on role change (per-user surgical DEL). `HasActiveRole` remains UNCACHED (~30ms doesn't justify the invalidation surface). Backend curl warm /sentinel/boot dropped from 0.309s (cycle 2 baseline) → 0.222s (cycle 3 post-commit, 10-run median); cold→warm shows ~350ms saved per boot (0.544s → 0.18s with cache populated). | N/A — closed. Re-open if `/value-sprint` warm regresses above 700ms or a perm-revoke leaks a stale YES (would be caught by RBAC test surface; lint:no-direct-workspace-id covers the read side). | This register entry. Cycle 3 audit found 4 write callsites total — well below cycle 2's >10 stop-gate. | N/A. |
| TD-FLOWBOARD-STALE-FILTER-PARAM | 2026-05-28 | S3 | Frontend / FlowBoard hook | `app/(user)/value-flow/hooks/useFlowBoardData.ts:157` passes `?artefact_type_id=<uuid>` to `GET /_site/work-items`. The v2 handler at `backend/internal/artefactitems/handler.go:290` reads `item_type_id=` per the PLA-0054 rename — the old param name is silently ignored, so the filter never applies. The FlowBoard then receives all work-scope types (TA/EP/US/DE/RSK) and filter-out-EPs client-side. The current dev seed is small enough that this is not user-visible, but the wire is dead. | First production FlowBoard with > 200 work items of mixed types — board renders rows the FlowBoard wasn't meant to show (or perf suffers from client-side filtering of a much larger response). | This register entry. Per cycle 2's diagnosis (cycle 1 had hypothesised this as a perf hotspot; cycle 2's EXPLAIN ANALYZE showed the SQL is 7ms total so the silent filter-drop is a correctness issue, not a perf issue). | Single-line frontend fix in `useFlowBoardData.ts:157` — change `artefact_type_id` → `item_type_id`. Also audit other hooks that hit `/work-items` for the same rename gap: `grep -rn "artefact_type_id=" app/ --include="*.ts" --include="*.tsx" \| grep work-items`. ~15 min including the audit pass. |
| TD-CACHE-NO-HEALTHCHECK-ENDPOINT | 2026-05-28 | S3 | Backend / cache ops visibility | The cache.Client wrapper has a `Health()` method that PING-PONGs Valkey and reports `"available"` or `"unavailable: <reason>"` (returns string). No HTTP handler exposes it. Operationally invisible: if Valkey is down, the only signal is grep'ing the backend log for the `breaker opened` line. Procurement narrative for SOC 2: "how do you monitor the cache layer?" → today the honest answer is "by tailing logs." | First time ops needs to verify cache health without shell access. Or first time the cache silently degrades (breaker stuck open after a transient blip) and stays degraded because nobody noticed. Likely fires when a non-Claude operator (junior dev, contractor) joins. | This register entry. `cache.Client.Health(ctx)` is the implementation hook; the wrapper logs every break + recovery so a log-tailing operator has signal today. | Add `GET /_site/admin/dev/cache-health` (or similar admin-gated path) returning `{"status":"available"}` or `{"status":"unavailable","reason":"<err>"}` plus the Valkey INFO hits/misses counters. ~30 min: one handler + one route mount in main.go. Out of scope for this session because we deliberately stayed in the "wrapper + ResolveSubtree only" envelope and didn't introduce any new admin route surface. |
| TD-OBJECTTREE-PICKER-CUSTOM-FIELDS | 2026-05-28 | S2 | Frontend / ObjectTreeV2 column picker | `<ColumnPicker>` (Slice 4.5) is wired to a server-driven `ColumnCatalogue` via `useColumnCatalogue()` hook. Today the catalogue ships ONLY the built-in `artefactitems.ColumnSpec` allow-list (30+ columns enriched 2026-05-28 with Label / Group / DefaultVisible / Addable). Workspace-scoped custom fields (rows in `artefacts_fields_library`) are NOT in the catalogue, so the picker can't list or toggle them. The picker UI is otherwise complete (draft/commit, search, Apply/Cancel, maxColumns cap, localStorage prefs). | (a) First buyer demo where the slide says "users can pick any field, including their custom ones" and the screen disagrees. (b) Defence/finance procurement question: "can the user customise the grid columns including their tenant's custom fields?" — answer today is "built-ins yes, custom no." (c) First user creates a custom field via `/workspace-admin/custom-fields` and expects it in the grid picker. | This register entry. `app/components/ObjectTreeV2/hooks/useColumnCatalogue.ts` header comments name the deferral. `app/components/ObjectTreeV2/plugins/ColumnPicker.tsx` is shape-complete — picker accepts any `ColumnCatalogue` shape; nothing on the picker side blocks the feature. | Three-layer cross-stack feature, designed as a spec before any code (brainstorming → writing-plans → execution): **Backend** — (1) extend `artefactitems.IsKnownArtefactItemColumn` to accept `field:<uuid>` wire keys, validating each against the per-workspace `artefacts_fields_library` admitted set (NOT a static allow-list — workspace-scoped); (2) extend the List projection to bulk-JOIN `artefacts_fields_values` on the requested field IDs, returning values keyed by `field:<uuid>` in each WorkItem JSON (single round-trip, NOT N+1 per-row); (3) extend `/work-items/columns` response to include custom-field entries for the active workspace (so the picker sees them without a second fetch). **Frontend** — (4) extend `useColumnCatalogue` to consume the enriched `/columns` response (no merge — backend already merged); (5) build a `<CustomFieldCell>` renderer that handles all 11 `field_type` variants (textbox, richtext, integer, decimal, date, boolean, select, multiselect, radio, user, url) in dense-grid context (display only, edit via existing detail flyout). **Estimate** 4–6 hours focused work; non-trivial enough to warrant a spec in `docs/superpowers/specs/` before code lands. |
| TD-SAVEDVIEWS-WORKSPACE-SHARE-PERM-CODE | 2026-05-28 | S3 | RBAC / permission catalogue | The saved-views substrate uses `workspace.archive` as the proxy for "this user can share a view to workspace-scope" — both the frontend (`SavedViewsControl` prop `canShareToWorkspace: sentinel_can("workspace.archive")`) and the backend (`savedViewsWSAdminAdapter` in `backend/cmd/server/main.go:2596+` calls `permissions.Resolver.Has(WorkspaceArchive)`). The proxy works because every role currently granted `workspace.archive` is the role you'd also want to grant share-views to. But naming-wise it's wrong: a procurement audit narrative ("who can publish saved views to the whole workspace?") shouldn't have to explain that the answer is gated by a permission named "archive." | A buyer asks the RBAC question in an audit, or somebody legitimately wants the two permissions to diverge (e.g. allow team leads to share views without giving them the workspace-archive grant). Also: anyone reading the SavedViews codepath today is going to ask "why is the share-to-workspace gate called workspace.archive?" — that question recurs on every read of the file. | This register entry. `backend/internal/savedviews/handler.go` + `app/components/ObjectTreeV2/p_ObjectTree.tsx` both have inline comments documenting the proxy choice and pointing at this TD. The permission code constant `permissions.WorkspaceArchive` in Go is the single source of truth — swap-in is one line on each side. | Three-step migration, **do not apply piecemeal** (VerifyParity at server boot will fail if Go-side All and DB users_permissions diverge): (1) **Go catalogue** — add `WorkspaceShareViews Code = "workspace.share_views"` to `backend/internal/permissions/catalogue.go` constants block (near `WorkspaceArchive`) and to the `All` slice. (2) **DB migration** (new mig 146 or higher) — `INSERT INTO users_permissions (users_permissions_code, users_permissions_description) VALUES ('workspace.share_views', 'Share saved views to workspace scope')` + grant onto the same role-set currently holding `workspace.archive` (`SELECT users_roles_id FROM users_roles WHERE users_roles_code IN ('grp_global','grp_portfolio')` — confirm exact role-set with user before applying; today's `workspace.archive` grantees per live query). The pattern to follow: mig `100_workspace_permissions_seed.sql` for insert shape, mig 197 (PLA-0049) for the rename precedent. (3) **Swap proxy at call sites** — replace `permissions.WorkspaceArchive` → `permissions.WorkspaceShareViews` in `savedViewsWSAdminAdapter` (backend) and `sentinel_can("workspace.archive")` → `sentinel_can("workspace.share_views")` in `app/components/ObjectTreeV2/p_ObjectTree.tsx` (frontend). Then regenerate SY003 (perm catalogue is substrate). Total: ~45 min including the SY003 regen and a server-boot sanity check; gated on the user's decision about which roles should hold the new code (default: identical to current workspace.archive grantees). |
| ~~TD-MIG-137-DEFERRED-DRIFT~~ | 2026-05-28 | ~~S2~~ | ~~DB / migration ledger drift~~ | `db/vector_artefacts/schema/137_users_statusbar_prefs.sql` exists in the repo (committed in `10475cca`, "feat(redesign): StatusBarBottom shell component + users_statusbar_prefs migration 137") but is NOT recorded in `schema_migrations` and `users_statusbar_prefs` does NOT exist live on the dev DB. The intent was to ship `StatusBarBottom` with the prefs table; the apply step never ran because of an auth issue at the original session, and the file has been sitting between migrations 136 and 138 ever since. No code references `users_statusbar_prefs` today (grep across `backend/internal/` + `app/` returns zero hits), so nothing relies on it being live — but the file's mere presence in the `db/vector_artefacts/schema/` ordered set will trip the next session that runs the migrate tool against a fresh DB (137 will be applied automatically) AND it leaks into SY003 substrate inventory regenerations as a phantom "expected next table." | Two paths to bite: (a) the next time anyone runs the migrate tool against a fresh DB (e.g. CI seeding, a new dev environment), 137 will be applied silently — fine if the schema is still right, latent footgun if the schema needs updating. (b) The next time `StatusBarBottom` user-prefs writes are wired and the writer attempts to write the table on the existing dev DB, the call will fail because the table doesn't exist. (c) SY003 regeneration after any further mig drifts further if 137 is silently re-recorded without an apply on a different env. | This register entry. The file is unaltered (still describes the intended shape — single per-user JSONB row of pinned data points, no workspace scope). The 138-145 migrations have skipped over it cleanly; nothing currently depends on its absence. | Two options, user's call: (1) **Apply now** — `psql -f db/vector_artefacts/schema/137_users_statusbar_prefs.sql vector_artefacts` + insert the `schema_migrations` row + regenerate SY003. ~5 min. Right answer if `StatusBarBottom` prefs are about to be wired up. (2) **Stash the file** — move `137_users_statusbar_prefs.sql` (and its DOWN sibling) out of the active schema dir to e.g. `db/vector_artefacts/schema/_deferred/` until the writer is ready, then renumber when re-introduced. Right answer if the prefs feature is genuinely deferred. **Do not silently insert the schema_migrations row without applying** — that hides the drift without fixing it. Origin: 2026-05-28 — surfaced by SY003 regeneration during the saved-views build verification gate. **Resolved 2026-05-29.** Path 1 taken: ran the migration via the migrator (`go run ./cmd/migrate -db vector_artefacts -env .env.dev`), `users_statusbar_prefs` table now live on dev, `schema_migrations` row recorded. Did NOT regenerate SY003 — out of scope for the saved-views build; the next session that touches substrate inventory should run `<report> -sy` to pick up 137 + 149. |
| TD-SCHEMA-MIGRATIONS-DRIFT-ROOT-CAUSE | 2026-05-29 | S2 | DB / migration tooling | On 2026-05-29, attempting to apply migration 149 surfaced that 37 migration files (093–130) existed on disk + had fully landed in the `vector_artefacts` schema but were NOT recorded in `schema_migrations`. The drift covered the entire Pillar 1 column-prefix sweep (094–108), the Pillar 2 cross-DB merge (109–118), the Pillar 3 FDW drop (119), and the notifications v2 cluster (120–130). Verified via `/tmp/verify_migrations.sh` (38 files: every CREATE TABLE / column rename / DROP / trigger function claim matched live `information_schema`); rows backfilled in `schema_migrations` 2026-05-29. The drift was bookkeeping-only — but the ROOT CAUSE is unknown: someone applied 37 migrations without the runner inserting their rows. Plausible: the Pillar 1/2/3 refactor used `db/vector_artefacts/schema/merge_plan/*.sql` raw psql scripts to run an end-to-end pipeline that landed the same SQL the canonical 093–130 files describe, but never round-tripped through the migrator. If the same pattern recurs (next big refactor wave runs merge_plan-style scripts), the next 30+ migrations will silently drift again. | Either: (a) the next time anyone runs `db/vector_artefacts/schema/merge_plan/*.sql` style raw-psql pipelines without backfilling `schema_migrations`, this debt repeats; (b) a CI seeder spins up a fresh DB, applies all migrations 001–N, and the post-merge_plan migrations (which assume the merge_plan landed) fail because the merge_plan was never run as canonical migrations. | This register entry + the 2026-05-29 backfill closes today's drift. Verifier script at `/tmp/verify_migrations.sh` is reusable — re-run after any suspected drift to compare disk vs DB structurally. | Two-step fix: (1) **Investigate the merge_plan** — read `db/vector_artefacts/schema/merge_plan/README.md` (if it exists) or the Pillar 2/3 retro to confirm whether merge_plan was the application vector, and document it as either "merge_plan is the canonical apply path for 109–119" (then add merge_plan to the migrator's manifest) OR "merge_plan was a one-shot, 109–119 should have been runner-applied" (then ratchet against repeat). (2) **Tooling guard** — add a CI check that compares `ls db/vector_artefacts/schema/*.sql` against `SELECT filename FROM schema_migrations` and fails the build if any file is on disk but missing a row. Catches future drift at PR time, not migration-runtime. ~30 min for the CI check; merge_plan investigation is a separate session. |
| TD-SAVEDVIEWS-PAGE-KIND-OTHER-PAGES | 2026-05-29 | S3 | Frontend / saved views | The new `KindPage` saved-views surface (introduced 2026-05-29 for `/value-sprint`) is only wired on one page. Other multi-grid surfaces still use per-grid `KindObjectTree` dropdowns inside their ActionBars (`/work-items`, `/portfolio-items`, `/custom-fields`, `/risk`). Today's design is right: those pages are single-grid, so a per-grid dropdown IS a page-level dropdown by accident. But if any of those pages grow a second grid (e.g. a `/work-items` with a side panel summarising selected rows), the per-grid pattern starts to drift toward "two dropdowns for one page" again. | Either: (a) any of the currently-single-grid pages grows a second grid; (b) a new multi-grid page is built and the author copies a `/work-items`-style mount without consulting this TD. | This register entry. `app/components/SavedViews/PageSavedViewsControl.tsx` documents the design + partial-body rule. The cross-cutting cap is the lint rule `lint:no-direct-workspace-id` doesn't apply here — saved-views has no equivalent yet. | When the trigger fires: lift the page's grid(s) onto `PageSavedViewsControl` (one hook call at the page top, `bind(gridKey)` spread on each ObjectTree mount, drop the per-grid `savedViews` prop). Each migration is mechanical (~10 min/page). Optional cap: add a lint rule that flags pages with > 1 `<ObjectTree>` mount that both pass `savedViews=` props (i.e. the dual-dropdown pattern this entry warns about). |
| TD-MILESTONES-JUNCTION | 2026-05-29 | S2 | DB / artefacts substrate | Rally exposes `Milestones` as a multi-value Object Selector on Defect, User Story, Task, Risk, and Portfolio Item. Vector currently has a single-FK approximation (`artefacts.artefacts_id_timebox_milestone`), which can't represent the Rally many-to-many model. Deferred from the 2026-05-29 Rally-screenshots batch (migs 150-158) per Decision H to keep the batch focused on the easy substrate wins. Today: any user importing Rally data with multi-milestone artefacts will lose all but the first milestone link. | (a) First user requests cross-artefact milestone querying ("show me every artefact under Milestone X"); (b) First Rally-import surface needs to round-trip multi-milestones; (c) A milestone surface page is built that expects a back-link query. | This register entry. The spec (docs/superpowers/specs/2026-05-29-rally-screenshots-fields-design.md §3 Decision H) names the deferral and the rationale. Existing single-FK column does not block (NULL is the default). | Add junction table `artefacts_milestones (artefacts_milestones_id, artefacts_milestones_artefact_id FK, artefacts_milestones_milestone_id FK, artefacts_milestones_position, artefacts_milestones_created_at)`. Service methods: AddMilestone / RemoveMilestone / ListByArtefact / ListByMilestone. Migration ~30 min + service ~1h + handler/FE wire ~1h. Order of operations: junction first → backfill from single-FK column → deprecate single-FK in subsequent migration. |
| TD-INVESTMENTS-JUNCTION | 2026-05-29 | S2 | DB / artefacts substrate | Rally exposes `Investments` as a multi-value Object Selector on Portfolio Item (strategic-artefact in Vector terms). No Vector column today. Deferred from the 2026-05-29 Rally-screenshots batch per Decision H — same many-to-many shape as Milestones; both deferred together so a single junction-table pattern can be designed once and applied twice. | First Rally PI import that carries Investments references → silent data loss. Also: first strategic-artefact surface that expects an Investments-back-link query. | This register entry. The 2026-05-29 spec §3 Decision H names the deferral. | Same shape as TD-MILESTONES-JUNCTION — `artefacts_investments (artefact_id, investment_id, position, created_at)` junction with strategy-slot gate on the artefact FK. Best landed in the same session as TD-MILESTONES-JUNCTION so the junction-table service abstraction can be reused. |
| TD-TAGS-REGISTRY | 2026-05-29 | S3 | DB / artefacts substrate | The 2026-05-29 Rally-screenshots batch (mig 150) shipped `artefacts_tags TEXT[]` with a GIN partial index — the simplest correct shape per Decision D. Works fine for free-text tagging. Does NOT support: (a) per-workspace tag vocab control (e.g. typeahead with allowed-list enforcement), (b) per-tag metadata (color, description, owner), (c) tag rename across all referencing artefacts in O(1) instead of O(N). | (a) First user requests "lock tags to a controlled vocabulary" feature; (b) Workspace admins want to manage tag taxonomies (color, group); (c) Tag rename becomes a feature request and N-row UPDATE on `artefacts_tags` is too expensive at scale. | This register entry. mig 150 + the FE customFieldsAdapter document the TEXT[] choice. No production blocker today — free-text is the canonical Rally behaviour too. | Add `artefacts_tags_registry (workspace_id, tag, color, description)` registry table; promote `artefacts_tags` from TEXT[] to a junction `artefacts_tags_links (artefact_id, registry_id)`. Migration is a TEXT[] flatten + dedup into the registry per workspace, then rewrite the junction. ~2-3h including UI tag-manager. Cheap relative to the value. |
| TD-PLAN-ESTIMATE-DECIMAL | 2026-05-29 | S3 | DB / type mismatch with Rally | Vector keeps `artefacts_story_points` as INTEGER (the long-standing story-points convention). Rally's `PlanEstimate` is decimal. Decision A locked: keep INTEGER, accept rounding on Rally imports, rename the UI label to "Plan Estimate". An importer that round-trips a Rally PlanEstimate=1.5 will land 1 (or 2 if rounding). | First user import of fractional plan estimates from Rally → silent data loss at the row level (1.5 → 1 or 2). Also: any reporting query that aggregates plan estimates will be slightly off vs Rally's source. | This register entry + the Rally-screenshots batch spec §3 Decision A. UI label change is the only user-visible touch this batch — the column shape is unchanged. | One-line migration: `ALTER TABLE artefacts ALTER COLUMN artefacts_story_points TYPE NUMERIC(8,2);` Backend `WorkItem.StoryPoints` and the patch DTO change to `float64` (or `*decimal.Decimal`). FE detail forms accept fractional input. ~30 min including tests. Cheap. |
| TD-CORE-ROW-BINDING-RENDER | 2026-05-29 | S3 | Frontend / custom fields admin | The customFieldsAdapter CORE-row synthesis (extended 2026-05-29 to cover migs 150-158 + 14 retroactive mig-147 cleanups) carries `boundTo: "wrk_defect"\|"wrk_risk"\|"strategy"\|"universal"` metadata on each CORE override entry. `WorkspaceField` (the row shape consumed by ObjectTree) has no `boundTo` field today, so the metadata is staged but not rendered as a slot pill in the Type / Source cells. | First time a workspace admin tries to scan the Custom Fields admin grid and figure out "which artefact types is this CORE field bound to?". Today the answer requires opening the column in psql or reading the spec doc. | This register entry. `customFieldsAdapter.tsx` carries the override map with `boundTo` populated; no schema change blocks adding the render. | Add `boundTo?: string[]` to the `WorkspaceField` type (currently in `app/lib/fieldsApi.ts`). Have synthesiseCoreRow() populate it from the override map. Extend the Type / Source cell renderers to show a slot pill ("defect", "risk", "strategy", "all artefact types") when the row has `boundTo`. ~45 min including a typeahead chip for the read-only mode. Mirror of the existing `TypeBindingsPicker` chip styling so the grid reads coherently across CUSTOM (with binding chips) and CORE (with the same chip shape, but read-only). |
| TD-COLUMNSTORE-ANALYTICAL-TABLES | 2026-05-28 | S3 | DB / storage shape | Three tables in this project are append-only and queried with aggregations — the canonical columnstore workload, not the canonical row-store workload Postgres defaults to. **`audit_logs`** (every user action, queried by user/time-range/event-type aggregations for compliance + investigations), **`error_events`** (every error report, queried by code/time-range/subscription aggregations for dashboards), plus any future **`artefacts_history`** or telemetry/metrics table. Today all three are row-store Postgres tables with B-tree indexes. At low row counts (millions) this is fine — Postgres scans cheaply. At hundreds of millions to billions, aggregations over them will start dominating query time and the row-store layout will cost ~10× what a columnstore would. Procurement narrative for SOC 2 / defence-finance also benefits: "we retain N years of audit history and can answer compliance queries in sub-second" reads better when the aggregation engine is built for it. | First of: (a) `audit_logs` crosses 100M rows AND a compliance query (filter by user + date range + event type) takes > 1s on dev. (b) `error_events` powers a live dashboard and the dashboard query times out or shows > 2s spinner. (c) A buyer asks for multi-year audit retention with sub-second compliance queries. (d) Any new table being designed where the access pattern is "aggregate over millions of rows by time-range." | This register entry — names the future migration path before scale forces a hurried decision. Today's row-store + indexes is correct for current scale; nothing wasted by waiting. | Two viable paths, both Postgres-compatible (avoid leaving the SOC 2 audit perimeter unless forced): (1) **`pg_columnar`** (Citus / Hydra Open Source — Apache-2 licensed) — adds columnstore tables to Postgres directly as a foreign-data-wrapper-style storage option. Mixed row + column tables in the same DB. Minimal operational overhead. (2) **TimescaleDB hypertables with compression** — row-store partitions that auto-compress to columnstore-like layout after a configurable TTL (e.g. partitions older than 30 days). Best fit for `audit_logs` / `error_events` because they're already time-series-shaped. (3) **External columnstore** (ClickHouse / DuckDB) — best raw performance but adds a second backup story, second auth surface, second tenant-isolation story. Reserve for the case where (1) and (2) don't keep up. **Decision tree at trigger time**: prefer TimescaleDB if the table is time-series natured (audit_logs ✅, error_events ✅, artefacts_history ✅); prefer pg_columnar if it isn't; consider external only if both fail at the workload. Each path has a Go-interface boundary precedent in this codebase (the `cache.Client` wrapper that lets Valkey be swapped out), so the swap pattern is established. |
| TD-MILESTONES-NOTES | 2026-05-29 | S2 | Artefacts / milestones detail surface | Rally's milestone detail surface has a long-form `Notes` field. The 2026-05-29 milestones page build skipped it per brainstorm Decision 4 ("Skip Notes for now"); the DB column `timeboxes_milestones_notes` does not exist and the inline form does not render a Notes input. Description (a separate short-form field) is present. | First user request for Notes on a milestone, OR before any external demo that walks through the milestone detail flyout. | This register entry + the brainstorm decision documented. No schema change, no form field, no user-facing surface. | One migration adding `timeboxes_milestones_notes TEXT`. Backend types/service/handler updates to include the field in the wire shape. A form field in `TimeboxInlineForm`'s `MilestoneBody` section. ~2h including tests. |
| TD-MILESTONES-BACKEND-TESTS | 2026-05-29 | S2 | Backend / test coverage | `backend/internal/timeboxmilestones/` shipped without `service_test.go` or `handler_test.go`. Pre-existed the milestones page build but is now on the user-visible critical path; any defect in milestone CRUD is user-facing. | Before any change to the service, OR before this surface is shown to a buyer/investor. | This register entry. The milestones service is not marked with a NOCOV comment; no linting enforces test coverage, so the gap is invisible without inspection. | Mirror the test files in `backend/internal/timeboxsprints/` (`service_test.go` + `handler_test.go`), adapted to the milestone wire shape (simpler than sprints: no points, no rank-order, just name + description + milestone_on + is_active). Tests pin CRUD contract, list/filter ops, and role-gated access. ~4h including edge-case coverage. |
| TD-MILESTONES-SINGLE-CREATE | 2026-05-29 | S3 | Frontend / single-create flow | The "Create Milestone" button opens `ObjectTreeBulkCreateSheet` with `defaultCount: 1, maxCount: 1` instead of a dedicated single-row inline form. This matches the stopgap sprints page uses (`TimeboxObjectTree/index.tsx` `handleSingleClick`). The UX works but the bulk-sheet shell carries unused affordances (the count-stepper is disabled but present), and the milestone branch in `buildBulkConfig` leaves `endpoint: ${cfg.apiBase}/bulk-create` as dead config because no bulk-create endpoint exists for milestones. | When adding any other artefact kind that doesn't have a bulk-create path, OR when single-create UX becomes a usability complaint (modal weight, slow render, confusing layout). | This register entry. The `buildBulkConfig` dead endpoint is documented in an inline comment; the disabled-stepper affordance is noted as "stopgap." No lint enforces single-kind bulk sheets, so future maintainers may assume the pattern is intentional. | Extract `<TimeboxInlineSingleCreate>` from the bulk sheet and route all three kinds (sprint, release, milestone) through it for the single-create case. The bulk sheet stays for sprint/release multi-create only. This is a refactor with no API change — the three endpoints remain at the same paths, the form binding stays the same, only the modal container changes. ~3h including tests. |
| TD-STRATEGIC-INVESTMENT-WEIGHT-VOCAB | 2026-05-29 (updated 2026-05-30) | S2 | DB / artefacts substrate | Mig 162 (fourth-wave demotion, 2026-05-29) added `artefacts_strategic_investment_weight` as a free-text `TEXT` column. Rick locked it as a "dropdown enum" semantically (2026-05-29) but the vocab + value-store mechanism are not yet defined — to be decided in a follow-up session. **Update 2026-05-30:** Rick promoted SIW to a full core field (mig 164), so the catalogue row `c_artefacts_pi_strategic_investment_weight` is now ARCHIVED — it can no longer hold draft vocab in `options_json`. The column still accepts ANY non-empty TEXT with no CHECK and no scratch-space fallback. Slot-gate trigger (mig 162) enforces strategy-scope writeability; the handler-side validator does no vocab check. | Either (a) the first strategic artefact is created with a value typed by a user (free-text path → inconsistent data); (b) the value-store + dropdown UI is built before this entry is paid down (then the deferral is moot); (c) Rick locks the vocab in a follow-up session. | This register entry + the SQL comments in `db/vector_artefacts/schema/162_artefacts_fourth_wave_core_columns.sql` and `164_artefacts_flow_state_change_owner_core.sql`. The column accepts free text until the CHECK lands — drift risk is now higher since the catalogue scratch-row is gone. | Two steps: (1) lock the vocab + the value-store table (likely a small `strategic_investment_weight_options` lookup with `(value, label, position)` shape — mirrors the existing `artefact_priorities` pattern). (2) `ALTER artefacts ADD CONSTRAINT artefacts_strategic_investment_weight_chk CHECK ...` with the locked vocab, regenerate SY003. ~1h once the vocab is decided. Cheap. |
| TD-ARTEFACT-TYPES-DUP-SEED | 2026-05-29 | S2 | DB / artefact_types seed history | The `artefacts_types` table has 5 active "Feature" rows for the same subscription (and probably other duplicate names — confirm). Seed history left these — possibly multiple seed-replays during the Pillar refactors. Today's fix (FE dedup in `app/components/CustomFields/TypeBindingsPicker.tsx` + backend `DISTINCT ON (scope, name)` in `backend/internal/artefacttypes/service.go::List` + `ListByWorkspace`) hides them at the read layer, but writes against the wrong UUID would still drift to a random duplicate — e.g. if a future caller uses an older list path or constructs the artefact write against a stale id from a different render. | First user creates an artefact of a duplicated-name type and the row's `artefacts_id_artefact_type` points to a different UUID than the next user's same-name choice; cross-row aggregation reports show "two Features" instead of one. | This register entry; the picker query's `DISTINCT ON` masks the issue at read time; the FE picker dedups again belt-and-braces in `TypeBindingsPicker.tsx`. | Inventory all duplicate names: `SELECT artefacts_types_name, COUNT(*) FROM artefacts_types WHERE artefacts_types_archived_at IS NULL GROUP BY artefacts_types_name HAVING COUNT(*) > 1`. For each name, pick the canonical row (lowest UUID or one chosen by Rick), UPDATE every artefact pointing at the non-canonical rows to point at the canonical one, then soft-archive the non-canonical rows. ~1h depending on row counts. Verification: artefact count per type stays constant. |
| TD-FORMLAYOUT-READSIDE-REF | 2026-05-30 | S2 | Artefacts / form-builder read side | The write side already stamps `artefacts_id_form_layout` on artefact create (the exact layout version in force at creation), but the read DTO does NOT expose it, so `FormLayoutRuntime` resolves the layout by `(node, type)` current-version instead of pinning the stamped version. For carry-through this is the difference between "shows the layout the story was authored under" and "shows whatever the node's layout is NOW" — they diverge the moment padmin saves a new version (verified: v1→is_current=f, v2→is_current=t on save). PoC demo is correct because no re-save happened mid-demo; the gap bites the instant authoring and viewing straddle a version bump. | First time a story is viewed AFTER its node's layout is re-saved to a new version — the runtime renders the new layout, not the one the story was created under. Also bites carry-through across nodes (Insurance→Retail) where the receiver's current version ≠ the sender's stamped version. | This register entry + the explicit PoC-scope note in `FormLayoutRuntime.tsx` header (lines 8–14). The runtime degrades gracefully (current-version is a sane fallback), so no crash — just wrong-version drift. | Expose `artefacts_id_form_layout` (and the resolved layout doc, or a `getLayoutById` fetch) on the artefact read DTO in `backend/internal/artefactitems`. Change `FormLayoutRuntime` to prefer the stamped ref via `getLayoutById(stampedId)` and fall back to `getCurrentLayout(node,type)` only when the ref is NULL (legacy rows pre-stamp). ~3h including a backend test pinning "stamped version wins over current version." |
| TD-FORMLAYOUT-RUNTIME-ADOPTION | 2026-05-30 | S2 | Artefacts / inline-form integration | `FormLayoutRuntime` is built and verified standalone but is NOT spliced into the live `ArtefactInlineForm` (the actual create/edit surface on `/work-items`). Per foundation-mode the overnight build kept it decoupled rather than risk the critical-path form mid-session. So today: padmin can author + save a layout (full builder works end-to-end, 201 persist confirmed), but a user creating a story still sees the hardcoded inline form, not the authored layout. The headline "the editor IS the form" loop is closed on the builder half, open on the consumer half. | The moment this PoC is shown as "users get the custom form" — it is not wired to the user-facing create/edit path yet. Also any demo that walks create-story-on-node expecting the authored layout to appear. | This register entry + the standalone-component boundary (FormLayoutRuntime is imported nowhere on the critical path — grep confirms zero live consumers). Deliberate: decoupling protected the live form from an unreviewed overnight splice. | Splice `FormLayoutRuntime` into `ArtefactInlineForm` behind the layout-exists check: when `getCurrentLayout(node,type)` returns a layout, render the runtime; else render today's hardcoded form (the `fallback` prop already wires this). Host owns core-field PATCH via `onCoreChange`; custom-field write-back per TD-FORMLAYOUT-CUSTOM-WRITEBACK. ~4–6h including create + edit paths and a Playwright round-trip (author layout → create story → assert the authored grid renders). User review gate before merge. |
| TD-FORMLAYOUT-CUSTOM-WRITEBACK | 2026-05-30 | S3 | Artefacts / form-builder runtime | In `FormLayoutRuntime`, custom-field edits are display+local only (`setValue` updates React state but does not PUT). Core fields write back via the host's `onCoreChange`; custom (EAV) fields do not commit. For the PoC this is intentional (read-render is the demo), but a user typing into a custom field on the runtime form would lose the edit on reload. | First time the runtime is used as a real EDIT surface (not just a render) — i.e. coupled to TD-FORMLAYOUT-RUNTIME-ADOPTION. Standalone, this never bites; adopted without this fix, custom-field edits silently vanish. | This register entry + the inline PoC note in `FormLayoutRuntime.tsx` `setValue` (lines 133–142). | Reuse the existing EAV PUT path (`EditCustomFields` already owns `PUT /{resource}/{id}/field-values`); on custom-field blur/commit, call it with `{field_library_id, value}`. Debounce per the 250ms server-of-truth convention. ~2h, gated on the adoption splice landing first. |
| TD-FORMLAYOUT-VERSION-RETENTION | 2026-05-30 | S3 | DB / form-layout versioning | Every save mints a new `topology_node_form_layouts` row and flips the prior `is_current` to false; old versions are retained forever (no pruning). This is correct for carry-through (a stamped ref must resolve even to an old version), but unbounded: a node whose padmin saves the layout 200 times accrues 200 rows, 199 stale. Demo data already has 2 versions on the focus node from verification. | A node's layout is edited heavily (hundreds of saves) AND storage/scan cost on `topology_node_form_layouts` becomes measurable; OR a compliance review asks "why are there 200 versions of one form." Neither is near-term. | This register entry. The unique-current invariant (exactly one `is_current=t` per node+type) is enforced and verified; retention is the only unbounded axis. | Retention policy: keep all versions that are EITHER `is_current=t` OR referenced by at least one `artefacts_id_form_layout` stamp (a stamped version must never be pruned). Prune the rest beyond a keep-N-recent window. A periodic job or an on-save prune. ~3h; only worth building once a real node shows version bloat. Verification: no stamped ref ever dangles post-prune. |
| TD-FLB-DRAFT-DUP-IDS-ONDISK | 2026-05-31 | S2 | DB / form-layout draft integrity | The FLB ID minter (`useFormBuilderState._seq`) is a module-level counter that restarted at 1 on every page load, so a draft saved, closed, and reopened re-minted COLLIDING row/cell IDs into the same document (verified: draft `d1c76b35` for artefact-type `6ae170a7` carried 3× `cell-2`, 3× `cell-3`, 2× `row-5`, etc.). Duplicate IDs make `absorbedBy` ambiguous → `ownerOf`/`hOwnerOf`/`vOwnerRow` resolve to the wrong cell → seam handles land wrong / go missing / merges corrupt. **Fixed forward** (2026-05-31): `bumpSeqPast` advances the counter past any loaded document; `repairDuplicateIds` re-keys a corrupt document on `reset()`, repointing `absorbedBy` by POSITION. So load-time is now self-healing. BUT the **persisted JSON in the DB is still corrupt on disk** until each draft is re-opened AND re-saved — repair happens in memory, not written back automatically. | A consumer reads `topology_node_form_layouts_layout_json` for a pre-2026-05-31 draft DIRECTLY (not through the builder's `reset`) — e.g. the runtime, an ETL, or a report — and trips over the duplicate IDs without the repair pass. Currently only the builder reads drafts (`is_draft` filtered out of `GetCurrent`), so the blast radius is small, but any new direct reader inherits the corruption. | This register entry + the `repairDuplicateIds`/`bumpSeqPast` self-heal on load (`useFormBuilderState.ts`) + the two regression tests in `undoRedo.test.ts`. The corruption can no longer be CREATED; only legacy on-disk rows remain affected, and only the builder reads them today. | One-shot migration or dev-script: for every `topology_node_form_layouts` row, run the JSON through the same positional re-key (`repairDuplicateIds` logic, ported to Go or a node script) and UPDATE the column in place; regenerate SY003. ~2h. OR cheaper: let it heal organically (every reopened+resaved draft self-cleans) and only run the batch fix if a direct non-builder reader is added. Verification: `SELECT` no row whose `layout_json` has a repeated id (a small jq/plpgsql check). |
| TD-GRID-CHILDREN-GET-DEPRECATE | 2026-05-30 | S2 | API / read-gateway uniformity | `GET /work-items/{id}/children` (+ the `/portfolio-items` variant) still exists after the audited POST read-gateway (`POST /{resource}/query`) landed. The new Grid primitive on `/scope` reads exclusively through the POST gateway (no identifiers in any URL — SOC 2 audit-trail uniformity); the GET routes survive only because ObjectTreeV2 still calls them. Two read paths for the same data is the debt: one audited-POST, one GET-with-id-in-URL. | ObjectTreeV2 is migrated onto the POST gateway (or retired). At that point the GET children routes have zero callers and should be removed so the single audited POST is the only child-read path. | This register entry + the route is unchanged/working. The POST gateway is the canonical path for all new code; the GET is legacy-consumer-only. | Migrate OTV2's `fetchChildren` to `workItems.query({parentId})` / `portfolioItems.query({parentId})`, then delete `GET /{id}/children` (handler + route on both mounts) and its frontend `apiSite` wrapper. Verification: grep shows zero `/children` GET callers; OTV2 tree still loads. Standard: SOC 2 — every read uniformly logged through one body-driven endpoint. |
| TD-GRID-FORM-MODES | 2026-05-30 | S2 | Grid primitive / inline-form modes | `Grid__Forms` (the expandable-flyout payload) renders only the always-editable, auto-saving `ArtefactInlineForm`. The `view` / `edit` / `create` / `duplicate` mode distinctions are unmodelled — matching today's reality (the old DataGrid sidecar ignored its `mode` arg; `duplicate` was an amber header tint; `create` has no form at all). The reference extension was built edit-only on purpose; modes were deferred when the user redirected mid-design. | The user returns to the deferred form-mode question, OR a consumer needs a genuine create surface (inline create-row) or a read-only view that suppresses auto-save. | This register entry. `Grid__Forms` is a thin host with a stable prop surface (`artefactId`/`resourceUrl`/`scope`/`onClose` + optional `onSaved`/`onDuplicate`/`onDelete`), so adding a `mode` enum later is additive, not a rewrite. | Settle the `FlyoutMode` enum on `Grid__Forms`; build the create surface (OTV2's inline create-row is the reference); wire `view` to a read-only render that suppresses the auto-save PATCH; keep `duplicate`'s visual treatment. ~4–6h, user-driven. Verification: each mode renders + behaves distinctly; create persists a new artefact. |
| TD-GRID-LAZY-EXPANDALL | 2026-05-30 | S2 | Grid primitive / expand-all scale | `useTree.expandAll` is an eager latch cascade: it expands every currently-known expandable row and fetches every level until a fixed point. Correct + simple at `/scope` scale (~58 rows, shallow tree), but it issues one `POST /query` per expandable node with no virtualization or depth cap — a deep/wide clamp would fan out into hundreds of concurrent child fetches on a single "expand all" click. | A workspace clamp exceeds ~300 rows in scope, OR expand-all shows visible latency / a fetch storm in the network tab, OR a tree deeper than ~4 levels becomes common. | This register entry. The cascade is bounded by `children_count` (a leaf never fetches) and the `inFlightRef` double-fetch guard, so it's correct — just not scale-tuned. | Add a depth cap + "load more" beyond N levels, OR virtualize the row render and lazy-drive the cascade off the viewport, OR batch the child fetches behind one bulk endpoint. Pick once a real clamp shows the latency. Verification: expand-all on a 500-row clamp stays responsive with bounded concurrent fetches. |
| TD-GRID-PRIMITIVE-GENERICITY | 2026-05-30 | S3 | Grid primitive / abstraction proof | The Grid primitive (`useTree` headless core + `Grid__Tree` skin + the connector CSS) is proven by exactly ONE extension (expandable-rows-with-flyout-below) on ONE consumer (`<GridExecution>` at `/scope`). The hook contract (`UseTreeOptions`) and the skin's capability props (`loadingStyle`/`dnd`/`renderRowDetail`/`accentOf`) are therefore designed against a single use case — the seams may be slightly wrong for a second consumer. | A second extension or consumer is built (e.g. `<GridStrategic>` / `<GridRisk>`, or a non-expandable flat-list variant via `useTree({expandable:false})`). | This register entry. The boundaries are deliberately clean (headless core holds zero JSX; skin holds zero fetch; barber-pole is a prop not a component), so a second consumer is the cheap validation, not a rewrite. | When the second consumer lands, confirm the hook contract + skin props are the right seams BEFORE hardening; refactor any prop that turned out single-use-shaped. No action until then — generalising on one example is speculative. Verification: two consumers share the primitive with no divergent forks of the skin or hook. |
| ~~TD-DEPENDENCY-MAP-PERSISTENCE~~ | 2026-06-03 | ~~RESOLVED 2026-06-03~~ | ~~Dependencies / critical-path substrate~~ | **CLOSED 2026-06-03** via PLA074 / B23.1.1–.8 + B23.2.1–.3 + B23.3.1. Migrations 173–175 ship the three tables (`artefact_dependency_maps`, `artefact_dependency_edges`, `artefact_dependency_edge_events`). `backend/internal/dependencies/` is the sole Sentinel-gated writer; edge inserts are tx-wrapped with `FOR UPDATE` on the parent map for cycle + uniqueness; three partial unique indexes (directed, parallel, cross-kind canonical) enforce no double-add per pair per map. `GET /work-items/{id}/dependency-impact` + 409 `dependency_impact` on `DELETE /work-items/{id}` wired via the `depsArchivePreflight` adapter (handler-level, fail-closed on preflight error). Audit table is append-only with Sentinel-clamp snapshot per row. Live-DB Tier-B tests pin the cycle CTE, the three partial unique indexes, three-bucket projection, cross-bucket candidate exclusion, cross-clamp reachability redaction. System note: [`docs/c_c_dependencies.md`](c_c_dependencies.md). | n/a — closed. | n/a — closed. | n/a — closed. Composer's full bucket-render integration (UI switch from ephemeral → persisted state) is tracked as `TD-DEP-COMPOSER-PERSISTENCE-RENDER`. CPM duration deferral: `TD-DEP-CPM-DURATION`. |
| ~~TD-DEP-COMPOSER-PERSISTENCE-RENDER~~ | 2026-06-03 | ~~RESOLVED 2026-06-03~~ | ~~Dependencies / composer UI~~ | **CLOSED 2026-06-03** — composer drives off persistence end-to-end. (1) Hook auto-resolves a default map per topology node (`maps.list` → first; `maps.create({name: "Default dependencies"})` on miss); `?mid=` URL param remains an explicit override. (2) `effectiveBuckets` reads `persisted.buckets` when `isPersisted`, hydrating each `PersistedBucketRow` via `workItems.get(artefact_id)` into the existing `DependencyCandidate` render shape; rendered items carry `__edgeId` so remove targets the right edge. (3) `addCandidatesToBucket` + `removeSelectedFromBucket` branch on `isPersisted` and delegate to `persisted.addToBucket` / `removeFromBucket` (optimistic update + revert on backend error); errors surface via `notify.error()` toast — cycle, 409-duplicate, 403-out-of-scope all reach the user. Round-trip works: close overlay → reopen → edges re-hydrate. | n/a — closed. | n/a — closed. | n/a — closed. Remaining v2 polish (additive, not blocking): (a) candidate dropdown could call `/dependencies/candidates` for server-side cross-bucket exclusion (today the partial unique indexes catch dupes at insert time → 409 toast; UX is correct but does one extra round-trip per dup attempt), (b) Playwright `dependency_map_persistence.spec.ts` e2e author, (c) named-map picker UX for the multi-map use case. |
| TD-DEP-CPM-DURATION | 2026-06-03 | S2 | Dependencies / critical path | CPM (critical path method) is deferred until duration semantics are agreed. The substrate ships reachability (`/dependencies/{id}/transitive-impact`) — "if Story 3 slips, these artefacts can't start" — but does NOT compute earliest/latest start/finish or float. `artefacts.artefacts_story_points` exists but has no calibrated tenant-level points-to-days factor; `artefacts.artefacts_due_date` is sparse/optional. A CPM forward-pass over sparse durations would produce a misleading "critical path" with most artefacts at zero float (no input). | Either (a) `artefacts_story_points` gains a calibrated per-tenant velocity mapping (so points → days is honest), OR (b) a new `artefacts_estimate_days` field lands as an explicit duration source. Until one of these holds, CPM is intentionally absent. | This register entry. Reachability ships the CPM-shaped value the product needs ("what unblocks / what's blocked transitively?") without leaning on dishonest durations. | (1) Decide between calibrated-points OR explicit-duration with the product owner. (2) If calibrated-points: add per-tenant `velocity_points_per_day` (or similar) to a tenant-settings table; resolver picks it up at CPM compute time. If explicit-duration: migration to add `artefacts_estimate_days NUMERIC` to `vector_artefacts.artefacts`; UI field on the inline form. (3) Implement forward/backward pass + float in `dependencies.Service.CPM(mapID)`; gate on `len(without_duration) == 0` or surface as a warning. (4) Author golden-graph tests. ~2–3 days once the duration source is decided. |

## Anti-patterns (don't do these)

- **Silent fix.** Resolving debt without removing the register entry — future-Claude will assume it's still latent and add a duplicate cap.
- **Over-capping.** Building elaborate guards for S3 debt that has no trigger in sight. Caps are for S1/S2; S3 gets a register entry and that's it.
- **Pay-down at first sight.** Refactoring a polymorphic relationship "while you're in there" on an unrelated task. Pay-downs are scheduled, not opportunistic, unless the trigger has fired.
- **TODO comments without a register entry.** A TODO in code is debt that isn't measured. Either add the register entry or do the work.
- **"We'll come back to it."** Either it's S1 (do it now), S2 (cap it now, schedule pay-down), or S3 (register it). There's no fourth option.

## How to surface to the user

When you add or escalate a register entry during a task, end your reply with a one-line debt note:

> **Debt:** added TD-007 (S2) — bookmark cache lacks invalidation on tenant rename. Capped with doc; pay-down deferred until tenant rename ships.

When a trigger fires:

> **Debt trigger:** TD-001's pay-down condition met (second polymorphic writer landing in this PR). Recommend migrating `entity_stakeholders` to per-kind tables before merge.

Short, factual, no ceremony. The user decides whether to act.
