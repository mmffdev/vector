openapi: 3.1.0
info:
  title: Vector Public API (v2)
  version: 2.0.0
  description: |
    Vector's public data plane — vector_artefacts-backed endpoints
    served at `/samantha/v2/*`. Work items, portfolio items, ranking,
    timeboxes, fields. Designed for external API consumers (procurement
    audience) with API keys + Bearer JWT.

    **Companion spec**: `openapi.yaml` (the Site BFF surface at `/_site`).

    Routes here use vaPool (the `vector_artefacts` Postgres database).
    Legacy `/samantha/v1` was retired 2026-05-19 — no v1 data plane today.
servers:
- url: http://localhost:5100/samantha/v2
  description: Local development
- url: https://api.mmff.dev/samantha/v2
  description: Production
tags:
- name: work-items
  description: Work items (tasks, epics, bugs, stories) in vector_artefacts
- name: ranking
  description: Generic position management across orderable resources
- name: timeboxes
  description: Timebox management — sprints and releases
- name: workspace-fields
  description: Workspace-scoped admitted field set from artefact_field_library
- name: portfolio
  description: Portfolio layers from artefact_types (scope=strategy)
- name: topology
  description: Federated organisational canvas — topology_nodes in vector_artefacts (M6.1)
paths:
  /flows:
    get:
      tags:
      - flows
      summary: List flows for the caller's subscription
      operationId: v2_listFlows
      description: |
        Returns every flow for the caller's subscription, grouped by artefact
        type scope (work / strategy). Each group contains the flow's states
        ordered by sort_order. Archived flows and states are excluded.
        Requires the flows.manage permission.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Flow list grouped by scope
          content:
            application/json:
              schema:
                type: object
                properties:
                  work:
                    type: array
                    items:
                      $ref: '#/components/schemas/FlowGroup'
                  strategy:
                    type: array
                    items:
                      $ref: '#/components/schemas/FlowGroup'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '503':
          description: vector_artefacts unavailable
  /portfolio-items:
    get:
      tags:
      - portfolio
      summary: List portfolio items
      operationId: v2_listPortfolioItems
      security:
      - bearerAuth: []
      parameters:
      - name: skip
        in: query
        schema:
          type: integer
          default: 0
      - name: limit
        in: query
        schema:
          type: integer
          default: 50
      - name: sort
        in: query
        schema:
          type: string
      - name: dir
        in: query
        schema:
          type: string
          enum:
          - asc
          - desc
      - name: scope
        in: query
        description: Topology scope clamp (UUID) — see /work-items for details.
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: Portfolio items
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      type: object
                  total:
                    type: integer
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          description: Scope read denied.
        '404':
          description: Scope node not found in this tenant.
    post:
      tags:
      - portfolio-items
      summary: POST /portfolio-items
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: postPortfolioItems
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '201':
          description: 201 (stub response shape — schema not yet curated)
          content:
            application/json:
              schema:
                type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
  /portfolio-items/bulk:
    post:
      tags:
      - portfolio-items
      summary: POST /portfolio-items/bulk
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: postPortfolioItemsBulk
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '201':
          description: 201 (stub response shape — schema not yet curated)
          content:
            application/json:
              schema:
                type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
  /portfolio-items/flow-states:
    get:
      tags:
      - portfolio-items
      summary: GET /portfolio-items/flow-states
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: getPortfolioItemsFlowStates
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '200':
          description: 200 (stub response shape — schema not yet curated)
          content:
            application/json:
              schema:
                type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
  /portfolio-items/summary:
    get:
      tags:
      - portfolio-items
      summary: GET /portfolio-items/summary
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: getPortfolioItemsSummary
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '200':
          description: 200 (stub response shape — schema not yet curated)
          content:
            application/json:
              schema:
                type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
  /portfolio-items/{id}:
    delete:
      tags:
      - portfolio-items
      summary: DELETE /portfolio-items/{id}
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: deletePortfolioItemsId
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '204':
          description: 204 (stub response shape — schema not yet curated)
        '401':
          $ref: '#/components/responses/Unauthorized'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '404':
          $ref: '#/components/responses/NotFound'
    get:
      tags:
      - portfolio-items
      summary: GET /portfolio-items/{id}
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: getPortfolioItemsId
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '200':
          description: 200 (stub response shape — schema not yet curated)
          content:
            application/json:
              schema:
                type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      tags:
      - portfolio-items
      summary: PATCH /portfolio-items/{id}
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: patchPortfolioItemsId
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '200':
          description: 200 (stub response shape — schema not yet curated)
          content:
            application/json:
              schema:
                type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '404':
          $ref: '#/components/responses/NotFound'
  /portfolio-items/{id}/children:
    get:
      tags:
      - portfolio-items
      summary: GET /portfolio-items/{id}/children
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: getPortfolioItemsIdChildren
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '200':
          description: 200 (stub response shape — schema not yet curated)
          content:
            application/json:
              schema:
                type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
  /portfolio-items/{id}/field-values:
    get:
      tags:
      - portfolio-items
      summary: GET /portfolio-items/{id}/field-values
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: getPortfolioItemsIdFieldValues
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '200':
          description: 200 (stub response shape — schema not yet curated)
          content:
            application/json:
              schema:
                type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      tags:
      - portfolio-items
      summary: PUT /portfolio-items/{id}/field-values
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: putPortfolioItemsIdFieldValues
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '200':
          description: 200 (stub response shape — schema not yet curated)
          content:
            application/json:
              schema:
                type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '404':
          $ref: '#/components/responses/NotFound'
  /portfolio-items/{id}/field-values/{field_library_id}:
    delete:
      tags:
      - portfolio-items
      summary: DELETE /portfolio-items/{id}/field-values/{field_library_id}
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: deletePortfolioItemsIdFieldValuesFieldLibraryId
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '204':
          description: 204 (stub response shape — schema not yet curated)
        '401':
          $ref: '#/components/responses/Unauthorized'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '404':
          $ref: '#/components/responses/NotFound'
  /rank/move:
    post:
      tags:
      - ranking
      summary: Move item in ranking
      operationId: v2_moveRank
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                resource_type:
                  type: string
                  description: e.g. "work_item"
                row_id:
                  type: string
                  format: uuid
                before:
                  type: string
                  format: uuid
                  description: Place row immediately before this ID
                after:
                  type: string
                  format: uuid
                  description: Place row immediately after this ID
                to_top:
                  type: boolean
                  description: Move row to top of list
                to_bottom:
                  type: boolean
                  description: Move row to bottom of list
              required:
              - resource_type
              - row_id
      responses:
        '200':
          description: Rank updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  row_id:
                    type: string
                    format: uuid
                  scope:
                    type: string
                    enum:
                    - backlog
                    - sprint
                  new_position:
                    type: number
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /search:
    post:
      tags:
      - search
      summary: Fulltext search over artefacts
      operationId: v2_search
      description: |
        Fulltext search via tsvector (plainto_tsquery) over vector_artefacts
        search columns. Returns 503 when vector_artefacts is unavailable
        (`searchH` was not constructed at boot).
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                q:
                  type: string
                  description: Query text
      responses:
        '200':
          description: Search results
          content:
            application/json:
              schema:
                type: object
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '503':
          description: search not available
  /timeboxes/releases:
    get:
      tags:
      - timeboxes
      summary: List releases
      operationId: v2_listReleases
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Release list
          content:
            application/json:
              schema:
                type: object
                properties:
                  releases:
                    type: array
                    items:
                      type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
        '503':
          description: Timebox releases not enabled
    post:
      tags:
      - timeboxes
      summary: POST /timeboxes/releases
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: postTimeboxesReleases
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '201':
          description: 201 (stub response shape — schema not yet curated)
          content:
            application/json:
              schema:
                type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
  /timeboxes/releases/bulk-create:
    post:
      tags:
      - timeboxes
      summary: POST /timeboxes/releases/bulk-create
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: postTimeboxesReleasesBulkCreate
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '201':
          description: 201 (stub response shape — schema not yet curated)
          content:
            application/json:
              schema:
                type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
  /timeboxes/releases/{id}:
    get:
      tags:
      - timeboxes
      summary: Get a release
      operationId: v2_getRelease
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: Release
          content:
            application/json:
              schema:
                type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags:
      - timeboxes
      summary: DELETE /timeboxes/releases/{id}
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: deleteTimeboxesReleasesId
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '204':
          description: 204 (stub response shape — schema not yet curated)
        '401':
          $ref: '#/components/responses/Unauthorized'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      tags:
      - timeboxes
      summary: PUT /timeboxes/releases/{id}
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: putTimeboxesReleasesId
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '200':
          description: 200 (stub response shape — schema not yet curated)
          content:
            application/json:
              schema:
                type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '404':
          $ref: '#/components/responses/NotFound'
  /timeboxes/sprints:
    get:
      tags:
      - timeboxes
      summary: List sprints
      operationId: v2_listSprints
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Sprint list
          content:
            application/json:
              schema:
                type: object
                properties:
                  sprints:
                    type: array
                    items:
                      type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
        '503':
          description: Timebox sprints not enabled
    post:
      tags:
      - timeboxes
      summary: POST /timeboxes/sprints
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: postTimeboxesSprints
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '201':
          description: 201 (stub response shape — schema not yet curated)
          content:
            application/json:
              schema:
                type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
  /timeboxes/sprints/bulk-create:
    post:
      tags:
      - timeboxes
      summary: POST /timeboxes/sprints/bulk-create
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: postTimeboxesSprintsBulkCreate
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '201':
          description: 201 (stub response shape — schema not yet curated)
          content:
            application/json:
              schema:
                type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
  /timeboxes/sprints/{id}:
    get:
      tags:
      - timeboxes
      summary: Get a sprint
      operationId: v2_getSprint
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: Sprint
          content:
            application/json:
              schema:
                type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags:
      - timeboxes
      summary: DELETE /timeboxes/sprints/{id}
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: deleteTimeboxesSprintsId
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '204':
          description: 204 (stub response shape — schema not yet curated)
        '401':
          $ref: '#/components/responses/Unauthorized'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      tags:
      - timeboxes
      summary: PUT /timeboxes/sprints/{id}
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: putTimeboxesSprintsId
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '200':
          description: 200 (stub response shape — schema not yet curated)
          content:
            application/json:
              schema:
                type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '404':
          $ref: '#/components/responses/NotFound'
  /timeboxes/sprints/{id}/close:
    post:
      tags:
      - timeboxes
      summary: POST /timeboxes/sprints/{id}/close
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: postTimeboxesSprintsIdClose
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '200':
          description: 200 (stub response shape — schema not yet curated)
          content:
            application/json:
              schema:
                type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '404':
          $ref: '#/components/responses/NotFound'
  /timeboxes/sprints/{id}/start:
    post:
      tags:
      - timeboxes
      summary: POST /timeboxes/sprints/{id}/start
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: postTimeboxesSprintsIdStart
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '200':
          description: 200 (stub response shape — schema not yet curated)
          content:
            application/json:
              schema:
                type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '404':
          $ref: '#/components/responses/NotFound'
  /topology/commit:
    get:
      tags:
      - topology
      summary: Get commit status
      operationId: v2_getTopologyCommitStatus
      security:
      - bearerAuth: []
      parameters:
      - name: ws
        in: query
        schema:
          type: string
      responses:
        '200':
          description: Commit status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TopologyCommitStatus'
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      tags:
      - topology
      summary: Commit topology changes (gadmin only)
      operationId: v2_commitTopology
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Committed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TopologyCommitStatus'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /topology/disconnected:
    get:
      tags:
      - topology
      summary: List disconnected nodes
      operationId: v2_listDisconnectedTopologyNodes
      security:
      - bearerAuth: []
      parameters:
      - name: ws
        in: query
        schema:
          type: string
      responses:
        '200':
          description: Disconnected nodes (no parent, not the tenant root)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/TopologyNode'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /topology/grants/me:
    get:
      tags:
      - topology
      summary: List caller's topology grants
      operationId: v2_getMyTopologyGrants
      description: |
        PLA-0042 — scope picker source. Returns the live grants held by the
        calling user across the topology. Not workspace-clamped: a user's
        grants may span workspaces inside their subscription.
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Caller's grants
          content:
            application/json:
              schema:
                type: object
                properties:
                  grants:
                    type: array
                    items:
                      type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
  /topology/nodes:
    post:
      tags:
      - topology
      summary: Create topology node
      operationId: v2_createTopologyNode
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTopologyNodeInput'
      responses:
        '201':
          description: Node created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TopologyNode'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /topology/nodes/bulk-position:
    post:
      tags:
      - topology
      summary: Bulk update node positions
      operationId: v2_bulkPositionTopologyNodes
      security:
      - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - updates
              properties:
                updates:
                  type: array
                  items:
                    type: object
                    required:
                    - node_id
                    - position
                    properties:
                      node_id:
                        type: string
                        format: uuid
                      position:
                        type: integer
                      layout_mode:
                        type: string
                      manual_x:
                        type: number
                        nullable: true
                      manual_y:
                        type: number
                        nullable: true
      responses:
        '204':
          description: Positions updated
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /topology/nodes/{id}:
    patch:
      tags:
      - topology
      summary: Update topology node (rename / move / field patch)
      operationId: v2_patchTopologyNode
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        '200':
          description: Node updated
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags:
      - topology
      summary: Archive topology node
      operationId: v2_archiveTopologyNode
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '204':
          description: Node archived
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /topology/nodes/{id}/ancestors:
    get:
      tags:
      - topology
      summary: Get node ancestors
      operationId: v2_getTopologyAncestors
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: Ancestor list (root first)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/TopologyNode'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
  /topology/nodes/{id}/archived-descendants:
    get:
      tags:
      - topology
      summary: List archived descendants
      operationId: v2_getArchivedDescendants
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: ws
        in: query
        schema:
          type: string
      responses:
        '200':
          description: Archived descendants in tree order
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ArchivedDescendant'
        '401':
          $ref: '#/components/responses/Unauthorized'
  /topology/nodes/{id}/disconnect:
    post:
      tags:
      - topology
      summary: Disconnect node from parent
      operationId: v2_disconnectTopologyNode
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '204':
          description: Node disconnected
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /topology/nodes/{id}/duplicate:
    post:
      tags:
      - topology
      summary: Duplicate node subtree
      operationId: v2_duplicateTopologyNode
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '201':
          description: Duplicated root node
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TopologyNode'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /topology/nodes/{id}/restore:
    post:
      tags:
      - topology
      summary: Restore archived node
      operationId: v2_restoreTopologyNode
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                new_parent_id:
                  type: string
                  format: uuid
      responses:
        '204':
          description: Node restored
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          description: Parent archived or missing
  /topology/nodes/{id}/roles:
    post:
      tags:
      - topology
      summary: Grant role on node
      operationId: v2_grantTopologyRole
      security:
      - bearerAuth: []
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - user_id
              - role
              properties:
                user_id:
                  type: string
                  format: uuid
                role:
                  type: string
                  enum:
                  - admin
                  - editor
                  - viewer
                can_redelegate:
                  type: boolean
      responses:
        '201':
          description: Role granted
          content:
            application/json:
              schema:
                type: object
                properties:
                  grant_id:
                    type: string
                    format: uuid
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /topology/preview-move:
    get:
      tags:
      - topology
      summary: Preview node move (cycle check)
      operationId: v2_previewTopologyMove
      security:
      - bearerAuth: []
      parameters:
      - name: node
        in: query
        required: true
        schema:
          type: string
          format: uuid
      - name: new_parent
        in: query
        schema:
          type: string
          format: uuid
      - name: ws
        in: query
        schema:
          type: string
      responses:
        '200':
          description: Move preview result
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  reason:
                    type: string
                    enum:
                    - cycle
        '401':
          $ref: '#/components/responses/Unauthorized'
  /topology/reset:
    post:
      tags:
      - topology
      summary: Reset topology canvas (gadmin only — mass-archives all live nodes)
      operationId: v2_resetTopology
      security:
      - bearerAuth: []
      responses:
        '200':
          description: Reset complete
          content:
            application/json:
              schema:
                type: object
                properties:
                  archived:
                    type: integer
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
  /topology/roles/{grant_id}:
    delete:
      tags:
      - topology
      summary: Revoke role grant
      operationId: v2_revokeTopologyRole
      security:
      - bearerAuth: []
      parameters:
      - name: grant_id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '204':
          description: Grant revoked
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
  /topology/tree:
    get:
      tags:
      - topology
      summary: Get org topology tree
      operationId: v2_getTopologyTree
      description: |
        Returns the tree of live topology nodes for the workspace resolved
        from ?ws=<slug|uuid>. If ws is absent the backend falls back to the
        actor's first live workspace. Archived nodes are excluded from the
        main tree (see /topology/nodes/{id}/archived-descendants).
      security:
      - bearerAuth: []
      parameters:
      - name: ws
        in: query
        description: Workspace slug or UUID (absent → actor's first workspace)
        schema:
          type: string
      - name: root
        in: query
        description: Root node UUID (absent → tenant root)
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: Topology node tree
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/TopologyNode'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
  /topology/users/{userId}/grants:
    get:
      tags:
      - topology
      summary: GET /topology/users/{userId}/grants
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: getTopologyUsersUseridGrants
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '200':
          description: 200 (stub response shape — schema not yet curated)
          content:
            application/json:
              schema:
                type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
  /topology/view-state:
    put:
      tags:
      - topology
      summary: Upsert canvas viewport state for the caller
      operationId: v2_setTopologyViewState
      description: |
        Stores the canvas pan/zoom viewport for the (workspace, user) pair.
        workspace_id is resolved from the ?ws query param (WorkspaceClampMiddleware).
        Replaces the legacy per-node collapsed flag.
      security:
      - bearerAuth: []
      parameters:
      - name: ws
        in: query
        schema:
          type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - viewport_x
              - viewport_y
              - viewport_zoom
              properties:
                viewport_x:
                  type: number
                viewport_y:
                  type: number
                viewport_zoom:
                  type: number
      responses:
        '204':
          description: View state saved
        '401':
          $ref: '#/components/responses/Unauthorized'
  /work-items:
    get:
      tags:
      - work-items
      summary: List work items
      operationId: v2_listWorkItems
      security:
      - bearerAuth: []
      parameters:
      - name: skip
        in: query
        schema:
          type: integer
          default: 0
      - name: limit
        in: query
        schema:
          type: integer
          default: 50
      - name: sort
        in: query
        schema:
          type: string
      - name: dir
        in: query
        schema:
          type: string
          enum:
          - asc
          - desc
      - name: scope
        in: query
        description: |
          PLA-0043 — Topology scope clamp. When set to a topology_node UUID,
          the list is filtered to artefacts owned by that node OR any live
          descendant in the topology tree. Artefacts with no
          topology_node_id are excluded when scope is active. Grant
          inheritance: a grant on a node reaches the node and every
          descendant; gadmin bypasses the check entirely.
          Errors: 400 if not a UUID; 403 if the caller has no grant
          covering the node; 404 if the node does not exist in this
          tenant.
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: Work items
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      type: object
                  total:
                    type: integer
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          description: Scope read denied — caller has no grant on the requested topology node.
        '404':
          description: Scope node not found in this tenant.
    post:
      tags:
      - work-items
      summary: POST /work-items
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: postWorkItems
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '201':
          description: 201 (stub response shape — schema not yet curated)
          content:
            application/json:
              schema:
                type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
  /work-items/bulk:
    post:
      tags:
      - work-items
      summary: POST /work-items/bulk
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: postWorkItemsBulk
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '201':
          description: 201 (stub response shape — schema not yet curated)
          content:
            application/json:
              schema:
                type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
  /work-items/flow-states:
    get:
      tags:
      - work-items
      summary: GET /work-items/flow-states
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: getWorkItemsFlowStates
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '200':
          description: 200 (stub response shape — schema not yet curated)
          content:
            application/json:
              schema:
                type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
  /work-items/summary:
    get:
      tags:
      - work-items
      summary: GET /work-items/summary
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: getWorkItemsSummary
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '200':
          description: 200 (stub response shape — schema not yet curated)
          content:
            application/json:
              schema:
                type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
  /work-items/{id}:
    delete:
      tags:
      - work-items
      summary: DELETE /work-items/{id}
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: deleteWorkItemsId
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '204':
          description: 204 (stub response shape — schema not yet curated)
        '401':
          $ref: '#/components/responses/Unauthorized'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '404':
          $ref: '#/components/responses/NotFound'
    get:
      tags:
      - work-items
      summary: GET /work-items/{id}
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: getWorkItemsId
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '200':
          description: 200 (stub response shape — schema not yet curated)
          content:
            application/json:
              schema:
                type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      tags:
      - work-items
      summary: PATCH /work-items/{id}
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: patchWorkItemsId
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '200':
          description: 200 (stub response shape — schema not yet curated)
          content:
            application/json:
              schema:
                type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '404':
          $ref: '#/components/responses/NotFound'
  /work-items/{id}/children:
    get:
      tags:
      - work-items
      summary: GET /work-items/{id}/children
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: getWorkItemsIdChildren
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '200':
          description: 200 (stub response shape — schema not yet curated)
          content:
            application/json:
              schema:
                type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
  /work-items/{id}/field-values:
    get:
      tags:
      - work-items
      summary: GET /work-items/{id}/field-values
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: getWorkItemsIdFieldValues
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '200':
          description: 200 (stub response shape — schema not yet curated)
          content:
            application/json:
              schema:
                type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      tags:
      - work-items
      summary: PUT /work-items/{id}/field-values
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: putWorkItemsIdFieldValues
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '200':
          description: 200 (stub response shape — schema not yet curated)
          content:
            application/json:
              schema:
                type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '404':
          $ref: '#/components/responses/NotFound'
  /work-items/{id}/field-values/{field_library_id}:
    delete:
      tags:
      - work-items
      summary: DELETE /work-items/{id}/field-values/{field_library_id}
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: deleteWorkItemsIdFieldValuesFieldLibraryId
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '204':
          description: 204 (stub response shape — schema not yet curated)
        '401':
          $ref: '#/components/responses/Unauthorized'
        '400':
          $ref: '#/components/responses/BadRequest'
        '422':
          $ref: '#/components/responses/UnprocessableEntity'
        '404':
          $ref: '#/components/responses/NotFound'
  /workspaces/{id}/fields:
    get:
      tags:
      - workspaces
      summary: GET /workspaces/{id}/fields
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: getWorkspacesIdFields
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '200':
          description: 200 (stub response shape — schema not yet curated)
          content:
            application/json:
              schema:
                type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
  /workspaces/{id}/portfolio/layers:
    get:
      tags:
      - workspaces
      summary: GET /workspaces/{id}/portfolio/layers
      description: |-
        AUTO-GENERATED STUB — schema not yet curated.
        Path exists in main.go; request body, response shape, and error
        cases need hand-curation. Search for `x-stub: true` to find all
        stubs awaiting review. See dev/scripts/sync_specs.py.
      operationId: getWorkspacesIdPortfolioLayers
      x-stub: true
      security:
      - bearerAuth: []
      responses:
        '200':
          description: 200 (stub response shape — schema not yet curated)
          content:
            application/json:
              schema:
                type: object
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
components:
  schemas:
    TopologyNode:
      type: object
      properties:
        id:
          type: string
          format: uuid
        subscription_id:
          type: string
          format: uuid
        workspace_id:
          type: string
          format: uuid
        parent_id:
          type: string
          format: uuid
          nullable: true
        name:
          type: string
        description:
          type: string
        label_override:
          type: string
          nullable: true
        icon:
          type: string
          nullable: true
        colour:
          type: string
          nullable: true
        avatar_url:
          type: string
          nullable: true
        layout_mode:
          type: string
          enum:
          - auto-horizontal
          - auto-vertical
          - auto-radial
          - manual
        manual_x:
          type: number
          nullable: true
        manual_y:
          type: number
          nullable: true
        collapsed_default:
          type: boolean
        sort_order:
          type: integer
        archived_at:
          type: string
          format: date-time
          nullable: true
        archived_descendant_count:
          type: integer
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    ArchivedDescendant:
      type: object
      properties:
        id:
          type: string
          format: uuid
        parent_id:
          type: string
          format: uuid
          nullable: true
        name:
          type: string
        archived_at:
          type: string
          format: date-time
        parent_is_archived:
          type: boolean
    CreateTopologyNodeInput:
      type: object
      required:
      - name
      properties:
        parent_id:
          type: string
          format: uuid
          nullable: true
        name:
          type: string
        description:
          type: string
          nullable: true
        label_override:
          type: string
          nullable: true
        icon:
          type: string
          nullable: true
        colour:
          type: string
          nullable: true
        avatar_url:
          type: string
          nullable: true
        layout_mode:
          type: string
          enum:
          - auto-horizontal
          - auto-vertical
          - auto-radial
          - manual
        manual_x:
          type: number
          nullable: true
        manual_y:
          type: number
          nullable: true
        collapsed_default:
          type: boolean
        sort_order:
          type: integer
    TopologyCommitStatus:
      type: object
      properties:
        committed_at:
          type: string
          format: date-time
          nullable: true
        committed_by:
          type: string
          format: uuid
          nullable: true
        last_node_update:
          type: string
          format: date-time
          nullable: true
        dirty_since_commit:
          type: boolean
    FlowState:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        kind:
          type: string
          enum:
          - todo
          - in_progress
          - done
          - cancelled
        sort_order:
          type: integer
        is_initial:
          type: boolean
        colour:
          type: string
          nullable: true
    FlowGroup:
      type: object
      properties:
        flow_id:
          type: string
          format: uuid
        type_id:
          type: string
          format: uuid
        type_name:
          type: string
        type_scope:
          type: string
          enum:
          - work
          - strategy
        states:
          type: array
          items:
            $ref: '#/components/schemas/FlowState'
    Problem:
      type: object
      description: RFC 9457 Problem Details
      properties:
        type:
          type: string
          default: about:blank
        title:
          type: string
        status:
          type: integer
        detail:
          type: string
        instance:
          type: string
        violations:
          type: array
          items:
            type: object
            properties:
              field:
                type: string
              message:
                type: string
  responses:
    BadRequest:
      description: Bad Request (400)
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    Unauthorized:
      description: Unauthorized (401)
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    Forbidden:
      description: Forbidden (403)
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    NotFound:
      description: Not Found (404)
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    UnprocessableEntity:
      description: Unprocessable Entity (422)
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
