# Claude Test Accounts

**Claude-owned accounts** (stay OFF user@/padmin@/gadmin@ — Rick's). Default: `claude_2_test@mmffdev.com` (padmin, `8bcff3cb-…`, editor on Insurance via mig 090). Also `claude_1_test@` (user), `claude_3_test@` (gadmin). Passwords `password123!`. Fixture sub `…001`, dev `mmff_vector` via :5435.
