apiVersion: 1

providers:
  - name: Vector server health
    orgId: 1
    folder: Server
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
