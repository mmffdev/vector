<!-- DEPRECATED 2026-05-18 — see context/MEMORY.md -->

# MEMORY.md — Retired

**This file is no longer auto-loaded.** Vector's memory canonical home moved to [`context/MEMORY.md`](../../context/MEMORY.md) (working memory) + [`context/USER.md`](../../context/USER.md) (user profile) on 2026-05-18.

The 76 files in this directory remain on disk as a **searchable archive**, indexed by the [`<index>`](../skills/index/SKILL.md) skill (memsearch over `context/memory/` + `context/transcripts/` + `.claude/memory/`). Use `<index> -q "<query>"` for Tier-1 semantic recall.

Boot snapshots (`boot1.md`, `boot3.md`, etc.) are independent of this index — managed by the `<b>` skill, not auto-loaded, not auto-indexed at session start.

See [`.claude/CLAUDE.md`](../CLAUDE.md) § Session Startup for the current frozen-snapshot load order.
