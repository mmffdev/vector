{
  "id": "R002",
  "title": "Navigation Profiles — Feature Design (Phase 5)",
  "category": "Design",
  "topic": "dev/planning/feature_nav_profiles.md",
  "date": "2026-04-27",
  "summary": "Per-user multi-profile navigation: shared custom-pages/groups pool, server-side active profile, edit-mode lockdown UX, and the 14-story breakdown that replaces cards 00118–00123.",
  "content": "<h2>Status</h2><p><strong>Design approved 2026-04-27.</strong> Implementation cards: 00118 (SQL), 00119 (Profiles API), 00120 (Prefs API extension), 00121 (Context), 00122 (Bar UI), 00123 (Editor reactivity). Replaces the original 6 coarse cards with a 14-story breakdown — see checklist at the bottom.</p><p>Related docs: <code>plan_nav_registry_split.md</code> (Phases 1–4 + 8 shipped), <code>plan_nav_phase3_entities.md</code> (entity bookmarks shipped). This is <strong>Phase 5</strong>.</p><h2>Goal</h2><p>Each user gets multiple named navigation layouts (\"profiles\") they can swap between. Default is seeded; user creates more as needed. Each profile is a complete, independent layout: its own pinned items, its own group placements, its own start page.</p><p><strong>Hard rule — shared pool:</strong> custom pages <strong>and</strong> custom groups belong to the user, not to any one profile. They cannot be \"claimed\" by a profile and become unavailable to others. Every profile's Available pool exposes the full set of the user's pages and groups; the profile only decides which ones it displays and where.</p><h2>Current State</h2><p>The schema was forward-designed for this. <code>user_nav_prefs.profile_id UUID</code> exists at <code>db/schema/008_user_nav_prefs.sql:27</code> and every index includes it, but production rows are always NULL. <code>user_nav_groups</code> and <code>user_custom_pages</code> have no <code>profile_id</code>. The 5 buttons at <code>app/(user)/preferences/navigation/page.tsx:1494</code> are pure UI scaffolding with no backend wiring. <code>NavPrefsContext</code> assumes a single profile per user. App-layer (<code>user_id</code>, <code>subscription_id</code>) tenancy enforcement throughout — no RLS.</p><h2>Data Model</h2><h3>Per-profile vs. shared</h3><table><thead><tr><th>Resource</th><th>Scope</th><th>Why</th></tr></thead><tbody><tr><td>Pinned items (<code>user_nav_prefs</code>)</td><td>Per profile</td><td>The whole point — different layouts per profile</td></tr><tr><td>Custom groups (<code>user_nav_groups</code>)</td><td><strong>Shared</strong> per user</td><td>Hard rule — group definition lives at user level and is never \"claimed\"</td></tr><tr><td>Group placement (which profile shows which group, in what position)</td><td>Per profile (NEW <code>user_nav_profile_groups</code>)</td><td>Lets each profile pick which shared groups appear without claiming the group</td></tr><tr><td>Custom pages (<code>user_custom_pages</code>)</td><td><strong>Shared</strong> per user</td><td>Same shared-pool rule</td></tr><tr><td>Bookmarks (entity pins)</td><td>Per profile</td><td>Lives in <code>user_nav_prefs</code> already, automatic</td></tr><tr><td>Active profile selection</td><td><strong>Server-side</strong> on <code>users.active_nav_profile_id</code></td><td>Follows the user across devices (hot-desking)</td></tr></tbody></table><h3>New table: <code>user_nav_profiles</code></h3><pre><code>CREATE TABLE user_nav_profiles (\n  id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n  user_id         UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,\n  subscription_id UUID NOT NULL,\n  label           TEXT NOT NULL CHECK (length(trim(label)) BETWEEN 1 AND 32),\n  position        INTEGER NOT NULL,\n  is_default      BOOLEAN NOT NULL DEFAULT FALSE,\n  start_page_key  TEXT,\n  created_at      TIMESTAMPTZ NOT NULL DEFAULT now(),\n  updated_at      TIMESTAMPTZ NOT NULL DEFAULT now()\n);\n\nCREATE UNIQUE INDEX uniq_default_profile_per_user\n  ON user_nav_profiles (user_id, subscription_id) WHERE is_default = TRUE;\nCREATE UNIQUE INDEX uniq_profile_position\n  ON user_nav_profiles (user_id, subscription_id, position);\nCREATE UNIQUE INDEX uniq_profile_label\n  ON user_nav_profiles (user_id, subscription_id, LOWER(label));</code></pre><p><strong>Caps:</strong> max 10 profiles per user. <strong>Default rules:</strong> <code>label = 'Default'</code>, <code>is_default = TRUE</code>, <code>position = 0</code>; cannot rename, delete, or reorder; always exists.</p><h3>New table: <code>user_nav_profile_groups</code> (junction)</h3><pre><code>CREATE TABLE user_nav_profile_groups (\n  profile_id  UUID NOT NULL REFERENCES user_nav_profiles(id) ON DELETE CASCADE,\n  group_id    UUID NOT NULL REFERENCES user_nav_groups(id)  ON DELETE CASCADE,\n  position    INTEGER NOT NULL,\n  PRIMARY KEY (profile_id, group_id)\n);\n\nCREATE UNIQUE INDEX uniq_profile_group_position\n  ON user_nav_profile_groups (profile_id, position);</code></pre><p><strong>Invariant:</strong> if any pin in profile P references group G via <code>user_nav_prefs.group_id</code>, there must be a matching <code>user_nav_profile_groups</code> row. Frontend creates the placement when first dragging a pin into a group; backend rejects writes that violate the invariant. Removing the placement is allowed only if no pins in this profile reference the group.</p><h3>Existing-table changes</h3><pre><code>-- enforce profile_id (currently nullable, always NULL)\nALTER TABLE user_nav_prefs ALTER COLUMN profile_id SET NOT NULL;\nALTER TABLE user_nav_prefs ADD CONSTRAINT fk_user_nav_prefs_profile\n  FOREIGN KEY (profile_id) REFERENCES user_nav_profiles(id) ON DELETE CASCADE;\n\n-- user_nav_groups: NO CHANGE. Stays user-level (shared).\n\n-- server-side active selection\nALTER TABLE users ADD COLUMN active_nav_profile_id UUID\n  REFERENCES user_nav_profiles(id) ON DELETE SET NULL;</code></pre><h3>Migration steps (story S3)</h3><ol><li>CREATE <code>user_nav_profiles</code>.</li><li>CREATE <code>user_nav_profile_groups</code>.</li><li>INSERT one Default per (user_id, subscription_id) found in <code>user_nav_prefs ∪ user_nav_groups</code> (label='Default', is_default=TRUE, position=0, start_page_key=NULL).</li><li>UPDATE <code>user_nav_prefs</code> SET profile_id = &lt;default.id&gt; WHERE profile_id IS NULL.</li><li>ALTER <code>user_nav_prefs.profile_id</code> SET NOT NULL + add FK.</li><li>INSERT INTO <code>user_nav_profile_groups</code> (profile_id, group_id, position) — pre-seed every existing group as placed in that user's Default.</li><li>UPDATE <code>users</code> SET <code>active_nav_profile_id</code> = &lt;default&gt; for every user with a Default row.</li><li>(No backfill needed for users with no nav data — lazy-seed on first prefs read.)</li></ol><p><code>user_nav_groups</code> itself is unchanged by this migration.</p><h2>Role-Aware Default Seeding</h2><p>When a Default profile is <strong>first created</strong> for a user (via migration or lazy-seed for a brand-new user), it inherits the role-appropriate starting layout:</p><ul><li><strong>Pinned items:</strong> drawn from <code>catalogue.entry.defaultPinned === true</code> AND <code>entry.roles</code> includes the user's role. Already filtered by <code>/api/nav/catalogue</code> (<code>backend/internal/nav/handler.go:31</code>).</li><li><strong>Start page:</strong> picked by role — gadmin → first gadmin landing in catalogue, padmin → first padmin landing, user → <code>/dashboard</code>. Implemented as a small role→key map server-side; user can change it later by clicking the star on any pin.</li><li><strong>Position order:</strong> uses each catalogue entry's <code>defaultOrder</code>.</li></ul><p>Non-default profiles created by the user start <strong>empty</strong> — user pins what they want.</p><h2>Active Profile — Server-Side, Sticky</h2><ul><li>Stored on <code>users.active_nav_profile_id</code>. Loaded as part of the auth bootstrap.</li><li>On cold start with NULL: backend resolves to the user's Default profile.</li><li>Switching profiles (via Save Profile) writes the new value.</li><li>ON DELETE SET NULL: if a user deletes their currently-active non-Default profile, the column nulls out and next read falls back to Default.</li></ul><p>No localStorage involvement. Same profile follows the user from laptop to office desk to phone.</p><h2>API Surface</h2><h3>Profiles CRUD (card 00119)</h3><pre><code>GET    /api/nav/profiles                         → Profile[] sorted by position\nPOST   /api/nav/profiles      body: { label }    → Profile (cap=10, label uniqueness, length 1–32)\nPATCH  /api/nav/profiles/:id  body: { label? }   → Profile (rejects if is_default)\nDELETE /api/nav/profiles/:id                     → 204 (rejects if is_default; cascades)\nPATCH  /api/nav/profiles/order body: [{id,position}] → Profile[] (two-phase write)\nPUT    /api/nav/profiles/:id/activate            → 204 (sets users.active_nav_profile_id)</code></pre><h3>Extended endpoints (card 00120)</h3><p>All accept <code>?profile=&lt;uuid&gt;</code>; omitting falls back to caller's active profile.</p><pre><code>GET    /api/nav/prefs?profile=&lt;uuid&gt;             → PrefRow[] + groups for that profile\nPUT    /api/nav/prefs?profile=&lt;uuid&gt;             → atomic replace within that profile\nDELETE /api/nav/prefs?profile=&lt;uuid&gt;             → reset that profile only\nGET    /api/nav/start-page?profile=&lt;uuid&gt;        → resolved href for that profile\nGET    /api/nav/catalogue                        → unchanged (catalogue is profile-agnostic)\nPOST   /api/nav/bookmark   body adds: profile_id → pin to specific profile\nDELETE /api/nav/bookmark   body adds: profile_id\nGET    /api/nav/bookmark/check?profile=&lt;uuid&gt;    → check within profile</code></pre><p><strong>Tenancy (hard requirement):</strong> no RLS, so every profile-scoped query must filter <code>WHERE profile_id = $X AND user_id = $Y AND subscription_id = $Z</code>. Card 00119 ships <code>requireOwnedProfile(ctx, userID, subID, profileID) error</code> — a sentinel <code>ErrProfileNotFound</code> guard that every endpoint touching <code>user_nav_prefs</code>, <code>user_nav_profile_groups</code>, or <code>user_nav_profiles</code> calls first.</p><h2>UX — Edit-Mode Lockdown</h2><h3>Quick-bar states</h3><p><strong>Idle (no edits in flight):</strong></p><ul><li>All profile buttons enabled and clickable.</li><li>Active profile shown in white-bg / black-border / black-text style (already shipped).</li><li>Inactive profiles in <code>btn--primary</code> (dark fill).</li><li>Trailing <code>+ Add</code> button visible if under cap of 10.</li><li>Default has no rename/delete affordance; non-default profiles get a <code>⋯</code> menu.</li><li>No Save / Cancel buttons.</li></ul><p><strong>Edit mode (entered by clicking any profile button):</strong></p><ul><li>That profile's data is loaded into the editor (replacing previous draft).</li><li>All <strong>other</strong> profile buttons greyed out and non-functional.</li><li>The clicked profile button stays interactive but locked-in.</li><li><code>+ Add</code> button greyed out.</li><li>Two new buttons appear: <strong>Save Profile</strong> (primary) and <strong>Cancel</strong> (ghost).</li><li>Editor area (drag/drop, pin, rename group) is fully active.</li></ul><p><strong>On Save Profile:</strong> PUT <code>/api/nav/prefs?profile=&lt;id&gt;</code> with draft → PUT <code>/api/nav/profiles/:id/activate</code> → bar returns to idle with this profile highlighted as active → sidebar elsewhere re-renders against the new active profile (free, since <code>NavPrefsContext</code> is shared).</p><p><strong>On Cancel:</strong> discard draft, revert editor to previously-active profile. Active profile in DB is unchanged.</p><h3>Add / Rename / Delete</h3><ul><li><strong>Add:</strong> clicking <code>+</code> opens inline input for label. Server creates the profile, frontend auto-enters edit mode for it.</li><li><strong>Rename:</strong> <code>⋯</code> → Rename → inline editable label. Save commits via PATCH; Escape cancels. Only on non-default profiles, only when bar is idle.</li><li><strong>Delete:</strong> <code>⋯</code> → Delete → modal with <strong>full-width red-bordered warning banner</strong> (uses <code>--danger</code> / <code>--danger-bg</code> tokens):<blockquote><strong>Deleting this profile removes its pinned layout only.</strong> Your custom pages and groups are not deleted — they go back to the Available pool and remain available to every other profile.</blockquote>Confirm → DELETE the profile. Cascades wipe <code>user_nav_prefs</code> rows and <code>user_nav_profile_groups</code> placements via FK. <code>user_custom_pages</code> and <code>user_nav_groups</code> are untouched. If the deleted profile was active, server nulls <code>users.active_nav_profile_id</code>.</li></ul><h2>Frontend Changes</h2><h3>NavPrefsContext (card 00121)</h3><p>Add to state: <code>profiles: Profile[]</code>, <code>activeProfileId: string</code> (from <code>users.active_nav_profile_id</code>, falls back to Default), <code>editingProfileId: string | null</code> (in-memory; non-null = edit mode), <code>dirty: boolean</code>.</p><p>Add to API: <code>setEditingProfile(id)</code>, <code>saveEditing()</code>, <code>cancelEditing()</code>, <code>createProfile(label)</code>, <code>renameProfile(id, label)</code>, <code>deleteProfile(id)</code>, <code>reorderProfiles(ids[])</code>.</p><p>When <code>editingProfileId === null</code>, the sidebar everywhere else in the app reads the active profile's prefs. When non-null, recommend keeping the sidebar showing the active profile (predictable) over previewing the editing draft (showy but confusing).</p><h3>Profile-bar component (card 00122)</h3><p>Replace lines 1494–1500 of <code>app/(user)/preferences/navigation/page.tsx</code>. Component owns: render profiles + Add + (in edit mode) Save / Cancel; disabled states driven by <code>editingProfileId !== null</code>; <code>⋯</code> menu for non-default profiles. CSS: extend existing <code>.nav-prefs__quick-btn--active</code>; add <code>.nav-prefs__quick-btn--locked</code> and Save/Cancel pair styling.</p><h3>Editor reactivity (card 00123)</h3><p><code>setEditingProfile</code> triggers a clean reload of <code>bucketOrder</code>, <code>itemsByBucket</code>, <code>childrenByParent</code>, <code>customGroups</code>, <code>iconOverrides</code>, <code>startPageKey</code>. Live sidebar updates on Save are automatic via shared context. Test matrix: switch from a profile with custom groups to one without; switch from many pins to none; delete the active profile and confirm fallback to Default.</p><h2>Caps</h2><ul><li>10 profiles per user (matches <code>user_nav_groups</code> cap).</li><li>Profile label: 1–32 chars, trimmed, case-insensitive unique per user.</li><li>Existing per-profile caps stay: 50 pinned, 10 custom groups, 8 children per parent, 64-char group label.</li></ul><h2>Risks</h2><ul><li><strong>Reorder unique-index collision:</strong> <code>PATCH /api/nav/profiles/order</code> must do a two-phase write (set all to negative offsets first, then to final values) or use a deferrable unique constraint.</li><li><strong>Cascade delete</strong> wipes pins and per-profile group placements but <strong>not</strong> <code>user_custom_pages</code> — pages survive and remain in the Available pool of remaining profiles.</li><li><strong>Active-profile race:</strong> if user has the editor open in two tabs and saves different profiles, last-write-wins on <code>users.active_nav_profile_id</code>. Acceptable.</li><li><strong>Backfill cost:</strong> single INSERT-from-SELECT plus an UPDATE per tenant. Sub-second; confirm with <code>EXPLAIN</code> if user table is large.</li><li><strong><code>requireOwnedProfile</code> helper</strong> — every endpoint that accepts a <code>profile</code> param must call it before any write. Easy to forget on the third or fourth endpoint.</li></ul><h2>Story Breakdown — 14 Trackable Stories</h2><p>The original six cards (00118–00123) were too coarse for traceable progress. Replaced with the following decomposition. Dependencies noted; everything within a layer can be done in parallel unless called out.</p><h3>Schema (3 stories) — runs first; everything else depends on it</h3><ul style=\"list-style:none;padding-left:0\"><li><input type=\"checkbox\" disabled checked> <strong>S1 — SQL: create <code>user_nav_profiles</code> + <code>user_nav_profile_groups</code>.</strong> Both new tables, indexes (one Default per user, position uniqueness, label uniqueness), check constraints (label 1–32, position ≥ 0), caps trigger or app-layer cap of 10 profiles.</li><li><input type=\"checkbox\" disabled checked> <strong>S2 — SQL: alter existing tables for profiles.</strong> <code>user_nav_prefs.profile_id</code> SET NOT NULL + FK ON DELETE CASCADE. <code>users.active_nav_profile_id</code> UUID NULL + FK ON DELETE SET NULL. <code>user_nav_groups</code> is <strong>not</strong> altered.</li><li><input type=\"checkbox\" disabled checked> <strong>S3 — SQL: data migration script.</strong> Insert one Default per user found in <code>user_nav_prefs ∪ user_nav_groups</code>; UPDATE prefs to set profile_id; INSERT <code>user_nav_profile_groups</code> rows from existing <code>user_nav_groups</code>; UPDATE <code>users.active_nav_profile_id</code>. Idempotent + dry-run mode via <code>cmd/migrate -dry-run</code>.</li></ul><h3>Backend (6 stories) — S1–S3 must be merged first</h3><ul style=\"list-style:none;padding-left:0\"><li><input type=\"checkbox\" disabled checked> <strong>B1 — API: <code>requireOwnedProfile</code> helper.</strong> <code>nav.RequireOwnedProfile(ctx, userID, subID, profileID) error</code> returns sentinel <code>ErrProfileNotFound</code>. Unit tests: owned-OK, wrong-user, wrong-tenant, missing. <strong>Foundational — every other API story depends on this.</strong></li><li><input type=\"checkbox\" disabled checked> <strong>B2 — API: profiles CRUD.</strong> <code>GET /api/nav/profiles</code>, <code>POST</code> (cap of 10, label 1–32, case-insensitive uniqueness), <code>PATCH /:id</code> (rejects <code>is_default</code>), <code>DELETE /:id</code> (rejects <code>is_default</code>, cascades).</li><li><input type=\"checkbox\" disabled checked> <strong>B3 — API: order + activate.</strong> <code>PATCH /api/nav/profiles/order</code> with two-phase write to dodge unique-index collision. <code>PUT /api/nav/profiles/:id/activate</code> sets <code>users.active_nav_profile_id</code>.</li><li><input type=\"checkbox\" disabled> <strong>B4 — API: extend prefs/start-page/bookmark with profile scope.</strong> <code>?profile=</code> query param on <code>GET/PUT/DELETE /api/nav/prefs</code> and <code>GET /api/nav/start-page</code>. <code>profile_id</code> body field on <code>POST/DELETE /api/nav/bookmark</code> + <code>?profile=</code> on <code>/check</code>. Default behaviour when omitted: caller's active profile.</li><li><input type=\"checkbox\" disabled> <strong>B5 — API: lazy-seed + role-aware Default.</strong> First <code>GET /api/nav/prefs</code> for a user with no Default → server creates one, populates pins from catalogue's <code>defaultPinned ∧ roles ⊇ user.role</code>, picks role-appropriate <code>start_page_key</code>.</li><li><input type=\"checkbox\" disabled> <strong>B6 — API: group-placement invariant on prefs PUT.</strong> Reject any pin that references a <code>group_id</code> lacking a <code>user_nav_profile_groups</code> row for the target profile. On group placement removal, refuse if any pin still references it. Cleanup logic for orphans.</li></ul><h3>Frontend context (2 stories) — depends on B-layer</h3><ul style=\"list-style:none;padding-left:0\"><li><input type=\"checkbox\" disabled> <strong>C1 — Context: profiles + activeProfileId + mutators.</strong> <code>profiles[]</code>, <code>activeProfileId</code> (read from auth bootstrap or <code>/api/nav/profiles</code>), <code>createProfile / renameProfile / deleteProfile / reorderProfiles</code>. No edit-mode logic yet — read-only profile listing.</li><li><input type=\"checkbox\" disabled> <strong>C2 — Context: edit-mode + dirty tracking.</strong> <code>editingProfileId</code>, <code>setEditingProfile(id)</code>, <code>saveEditing()</code> (PUT prefs + PUT activate), <code>cancelEditing()</code> (revert to active), <code>dirty</code> boolean.</li></ul><h3>Frontend bar UI (3 stories) — C-layer must exist</h3><ul style=\"list-style:none;padding-left:0\"><li><input type=\"checkbox\" disabled> <strong>U1 — Bar: render + active highlight + edit-mode lockdown.</strong> Replace <code>page.tsx:1494–1500</code>. Renders profiles in position order; active gets the white-bg/black-border style (already shipped); edit-mode disables non-active buttons + shows Save/Cancel. CSS: <code>.nav-prefs__quick-btn--locked</code> + Save/Cancel inline-pair styling.</li><li><input type=\"checkbox\" disabled> <strong>U2 — Bar: + Add button + creation flow.</strong> Trailing <code>+</code> opens inline label input; on submit calls <code>createProfile(label)</code>, then auto-enters edit mode for the new profile. Greys out when at cap. Default's button never gets Add affordance.</li><li><input type=\"checkbox\" disabled> <strong>U3 — Bar: ⋯ menu (Rename + Delete with warning).</strong> Non-default profiles get a <code>⋯</code> menu. Rename → inline editable label + PATCH on Enter. Delete → modal with full-width red-bordered warning banner using <code>--danger</code> / <code>--danger-bg</code>, copy: <em>\"Deleting this profile removes its pinned layout only. Your custom pages and groups are not deleted — they go back to the Available pool and remain available to every other profile.\"</em> Confirm → DELETE; if active, falls back to Default on next read.</li></ul><h3>Editor reactivity (2 stories)</h3><ul style=\"list-style:none;padding-left:0\"><li><input type=\"checkbox\" disabled> <strong>E1 — Editor: clean draft rehydrate on profile switch + live sidebar.</strong> <code>setEditingProfile</code> fully reloads <code>bucketOrder</code>, <code>itemsByBucket</code>, <code>childrenByParent</code>, <code>customGroups</code> (from <code>user_nav_profile_groups</code> + shared <code>user_nav_groups</code>), <code>iconOverrides</code>, <code>startPageKey</code>. On Save, sidebar elsewhere in app re-renders against new active profile (verify shared context wiring).</li><li><input type=\"checkbox\" disabled> <strong>E2 — Editor: fallback-to-Default + e2e test matrix.</strong> When active profile is deleted (own action or another tab), next read sees <code>active_nav_profile_id = NULL</code> and silently falls back to Default. E2E test matrix: switch with-groups → without-groups; switch many-pins → empty; delete the active profile; rename then switch; reorder profiles then refresh.</li></ul><h3>Dependency Graph</h3><pre><code>S1 ─┐\nS2 ─┼─→ S3 ─→ B1 ─┬─→ B2 ─┬─→ C1 ─→ U1 ─→ U2 ─→ U3 ─┐\n                  ├─→ B3 ─┘                          │\n                  ├─→ B4 ─→ C2 ─────────────────────┤\n                  ├─→ B5                             │\n                  └─→ B6 ─────────────────────────────┴─→ E1 ─→ E2</code></pre><p>Total: <strong>14 stories</strong> (was 6). Each lands in roughly the F2–F5 estimate range. Better burn-down visibility, smaller blast radius per merge, easier to slot work between sessions.</p>"
}
