{
  "id": "R047",
  "title": "DATABASE Schema — Per-Workspace Portfolio Adoption Cutover to vector_artefacts",
  "category": "Database",
  "topic": "Consolidated database design for the portfolio model adoption cutover from legacy mmff_vector.obj_* tables to vector_artefacts. Establishes per-workspace portfolio architecture (Tenant → Workspace → Model), single-table artefact_types substrate (strategy + work merged, parent_type_id chain), three-tier scope discriminator (global/tenant/workspace) on artefact_field_library, workspace whitelisting via artefact_workspace_fields join, master_record_portfolio (model fields only), drop of strategy_layers_adopted, re-adoption placeholder flow that preserves orphaned work items, adoption saga rewrite, frontend cutover, and gradual mmff_vector sanitisation register. Naming convention: domain prefix on every new table for procurement audit transparency.",
  "date": "2026-05-07",
  "summary": "Per-workspace portfolio adoption rebuilt on vector_artefacts: single artefact_types substrate, scope-discriminated EAV, master_record_portfolio, gradual legacy drop.",
  "content": "<div class=\"dui-toc-layout\">\n  <aside class=\"dui-toc\">\n    <div class=\"dui-toc__label\">Contents</div>\n    <ol class=\"dui-toc__list\">\n      <li><a href=\"#1-overview\">1. Overview</a></li>\n      <li><a href=\"#2-architecture\">2. Architecture</a></li>\n      <li><a href=\"#3-naming-rules\">3. Naming Rules</a></li>\n      <li><a href=\"#4-schema-changes\">4. Schema Changes</a></li>\n      <li><a href=\"#5-eav-substrate\">5. EAV Field Substrate</a></li>\n      <li><a href=\"#6-master-record-portfolio\">6. master_record_portfolio</a></li>\n      <li><a href=\"#7-adoption-saga\">7. Adoption Saga Rewrite</a></li>\n      <li><a href=\"#8-readoption-flow\">8. Re-adoption Flow</a></li>\n      <li><a href=\"#9-frontend-cutover\">9. Frontend Cutover</a></li>\n      <li><a href=\"#10-sanitisation-register\">10. Sanitisation Register</a></li>\n      <li><a href=\"#11-migration-order\">11. Migration Order</a></li>\n      <li><a href=\"#12-risks-gaps\">12. Risks &amp; Gaps</a></li>\n      <li><a href=\"#13-story-decomposition\">13. Story Decomposition</a></li>\n    </ol>\n  </aside>\n  <div class=\"dui-doc\">\n<h2 id=\"1-overview\">1. Overview</h2>\n<p>The portfolio model adoption flow currently writes to legacy <code>mmff_vector.obj_*</code> tables and reads model description prose live from <code>mmff_library</code> at render time. Both are wrong for the new substrate: <code>vector_artefacts</code> is the canonical tenant database, and the library DB must never be read at runtime by tenant-facing surfaces. This design rewires adoption to write to <code>vector_artefacts</code>, introduces per-workspace portfolio scoping, and consolidates strategy and work artefact types into a single <code>artefact_types</code> table with a <code>parent_type_id</code> chain. Provenance from the library catalogue is preserved as plain columns on <code>artefact_types</code> rather than via a separate <code>strategy_layers_adopted</code> mapping table, which is dropped.</p>\n<p>Four guiding principles drive every decision below: (1) per-workspace scoping on every new artefact-domain row; (2) explicit scope discriminator instead of nullable trapdoors; (3) domain prefix on every new table name for procurement audit transparency (finance and defence); (4) gradual sanitisation of <code>mmff_vector</code> — never a big-bang demolition.</p>\n<h2 id=\"2-architecture\">2. Architecture</h2>\n<p>Hierarchy:</p>\n<pre><code>Tenant (mmff_vector.subscriptions  →  becoming master_record_tenant)\n  └─ Workspace A | Workspace B | Workspace * (mmff_vector.workspaces, retained for now)\n        └─ Portfolio Model (vector_artefacts.master_record_portfolio, ONE row per workspace)\n              ├─ Strategy artefact_types (scope='strategy', parent_type_id chain — depth 1..10)\n              └─ Work artefact_types     (scope='work',     parent_type_id chain — depth 1..N)</code></pre>\n<p>Each workspace owns its own portfolio architecture. Two workspaces under the same tenant may run completely different models — different layer counts, different field schemas, different terminology — without interference. The model is hung below the workspace, not below the tenant.</p>\n<p>Cross-database references (<code>workspace_id</code> in <code>vector_artefacts</code> pointing at <code>mmff_vector.workspaces.id</code>, <code>model_id</code> pointing at <code>mmff_library.portfolio_templates.id</code>) follow the existing pattern used by <code>vector_artefacts.artefacts.workspace_id</code>: <strong>no enforced FK</strong>, app-layer integrity only. This is the same trade-off already accepted across the substrate.</p>\n<h2 id=\"3-naming-rules\">3. Naming Rules</h2>\n<p>Every new table in the artefact substrate carries the <code>artefact_</code> prefix. Generic-sounding names are rejected on sight because they hide subsystem ownership and ambush future readers — a serious problem under procurement audit conditions in finance and defence. The single grep <code>\\bartefact_</code> must return every table in the substrate.</p>\n<table class=\"dui-table\">\n<thead><tr><th>Generic name (rejected)</th><th>Prefixed name (canonical)</th></tr></thead>\n<tbody>\n<tr><td><code>field_library</code></td><td><code>artefact_field_library</code></td></tr>\n<tr><td><code>workspace_fields</code></td><td><code>artefact_workspace_fields</code></td></tr>\n<tr><td><code>strategy_layers_adopted</code></td><td>(dropped — provenance lives on <code>artefact_types</code> as columns)</td></tr>\n</tbody>\n</table>\n<p>Master single-row-per-scope tables follow the <code>master_record_*</code> family: <code>master_record_tenant</code> (one row per tenant), <code>master_record_portfolio</code> (one row per workspace). Flow tables (<code>flows</code>, <code>flow_states</code>, <code>flow_transitions</code>) are a self-contained subsystem and follow their own naming pattern — they are not renamed.</p>\n<h2 id=\"4-schema-changes\">4. Schema Changes</h2>\n<h3>4.1 <code>artefact_types</code> — additive change</h3>\n<table class=\"dui-table\">\n<thead><tr><th>Column</th><th>Type</th><th>Notes</th></tr></thead>\n<tbody>\n<tr><td><code>workspace_id</code></td><td><code>uuid NOT NULL</code></td><td>NEW — per-workspace scope on every row</td></tr>\n<tr><td><code>library_layer_id</code></td><td><code>uuid NULL</code></td><td>NEW — provenance, points at <code>mmff_library.portfolio_template_layer_definitions.id</code></td></tr>\n<tr><td><code>library_layer_tag</code></td><td><code>text NULL</code></td><td>NEW — denormalised tag (e.g. <code>SO</code>, <code>PO</code>, <code>BE</code>, <code>BC</code>, <code>FE</code>) for fast filtering</td></tr>\n<tr><td><code>scope</code></td><td><code>text NOT NULL</code></td><td>existing — values: <code>strategy</code> | <code>work</code></td></tr>\n<tr><td><code>source</code></td><td><code>text NOT NULL</code></td><td>existing — provenance source enum (<code>library</code> | <code>tenant_authored</code> | <code>placeholder</code>)</td></tr>\n<tr><td><code>parent_type_id</code></td><td><code>uuid NULL</code></td><td>existing — chain pointer; root layer has NULL</td></tr>\n<tr><td><code>layer_depth</code></td><td><code>integer NOT NULL</code></td><td>existing — 1-based depth from root</td></tr>\n<tr><td><code>prefix</code></td><td><code>text NOT NULL</code></td><td>existing — short tag for IDs (e.g. <code>EP</code>, <code>US</code>)</td></tr>\n<tr><td><code>name</code></td><td><code>text NOT NULL</code></td><td>existing — display label</td></tr>\n<tr><td><code>sort_order</code></td><td><code>integer NOT NULL</code></td><td>existing — within-scope ordering</td></tr>\n<tr><td><code>archived_at</code></td><td><code>timestamptz NULL</code></td><td>existing — soft archive</td></tr>\n</tbody></table>\n<p>New indexes:</p>\n<pre><code>CREATE INDEX idx_artefact_types_ws_scope_sort\n  ON vector_artefacts.artefact_types (workspace_id, scope, sort_order)\n  WHERE archived_at IS NULL;\n\nCREATE UNIQUE INDEX uq_artefact_types_ws_scope_prefix\n  ON vector_artefacts.artefact_types (workspace_id, scope, prefix)\n  WHERE archived_at IS NULL;</code></pre>\n<h3>4.2 <code>field_library</code> → <code>artefact_field_library</code> — rename + scope discriminator</h3>\n<table class=\"dui-table\">\n<thead><tr><th>Column</th><th>Type</th><th>Notes</th></tr></thead>\n<tbody>\n<tr><td><code>id</code></td><td><code>uuid PK</code></td><td>existing</td></tr>\n<tr><td><code>scope</code></td><td><code>text NOT NULL</code></td><td>NEW — values: <code>global</code> | <code>tenant</code> | <code>workspace</code></td></tr>\n<tr><td><code>subscription_id</code></td><td><code>uuid NULL</code></td><td>becomes nullable; NULL only when <code>scope='global'</code></td></tr>\n<tr><td><code>field_name</code></td><td><code>text NOT NULL</code></td><td>existing</td></tr>\n<tr><td><code>data_type</code></td><td><code>text NOT NULL</code></td><td>existing — <code>text</code>|<code>number</code>|<code>date</code>|<code>boolean</code>|<code>enum</code>|<code>reference</code></td></tr>\n<tr><td><code>config</code></td><td><code>jsonb NOT NULL DEFAULT '{}'</code></td><td>existing — type-specific config (enum options, ref target, validation)</td></tr>\n<tr><td><code>archived_at</code></td><td><code>timestamptz NULL</code></td><td>existing</td></tr>\n</tbody></table>\n<p>Constraints:</p>\n<pre><code>ALTER TABLE vector_artefacts.artefact_field_library\n  ADD CONSTRAINT chk_afl_scope_values\n    CHECK (scope IN ('global','tenant','workspace')),\n  ADD CONSTRAINT chk_afl_global_no_subscription\n    CHECK ( (scope = 'global'    AND subscription_id IS NULL)\n         OR (scope IN ('tenant','workspace') AND subscription_id IS NOT NULL) );</code></pre>\n<p>Why explicit scope rather than NULL=global: NULL semantics rot under load. <code>WHERE subscription_id IS NULL</code> for &quot;global&quot; tangles every query with NULL-handling branches and is invisible in audit reports. <code>WHERE scope = 'global'</code> reads cleanly, indexes cleanly, and surfaces in audit logs as a discrete value finance and defence reviewers can challenge directly.</p>\n<h3>4.3 <code>strategy_layers_adopted</code> — DROP</h3>\n<p>Provenance moves to <code>artefact_types.library_layer_id</code> + <code>library_layer_tag</code>. The mapping table added zero behavioural value once <code>artefact_types</code> already carried <code>workspace_id</code>, <code>scope</code>, and <code>parent_type_id</code>. The drop is gradual: the table stays in place until the last reader (<code>adopt.go</code>) is rewritten, then a follow-up migration removes it.</p>\n<h3>4.4 <code>artefact_workspace_fields</code> — NEW (workspace whitelist)</h3>\n<p>Used only when <code>artefact_field_library.scope = 'workspace'</code>. Expresses &quot;field is valid in workspace A and C but not B&quot; — a strong audit primitive for procurement transparency. Absence of a row equals exclusion (deny by default).</p>\n<table class=\"dui-table\">\n<thead><tr><th>Column</th><th>Type</th><th>Notes</th></tr></thead>\n<tbody>\n<tr><td><code>workspace_id</code></td><td><code>uuid NOT NULL</code></td><td>cross-DB reference to <code>mmff_vector.workspaces.id</code></td></tr>\n<tr><td><code>field_library_id</code></td><td><code>uuid NOT NULL</code></td><td>FK <code>artefact_field_library(id) ON DELETE CASCADE</code></td></tr>\n<tr><td><code>created_at</code></td><td><code>timestamptz NOT NULL DEFAULT now()</code></td><td>—</td></tr>\n<tr><td><code>created_by</code></td><td><code>uuid NULL</code></td><td>user who whitelisted the field</td></tr>\n</tbody></table>\n<pre><code>PRIMARY KEY (workspace_id, field_library_id);\nCREATE INDEX idx_awf_field ON vector_artefacts.artefact_workspace_fields (field_library_id);</code></pre>\n<h2 id=\"5-eav-substrate\">5. EAV Field Substrate</h2>\n<p>The four-table substrate already exists for artefact extension — only <code>artefact_field_library</code> changes shape. The flow is unchanged in spirit:</p>\n<table class=\"dui-table\">\n<thead><tr><th>Table</th><th>Role</th></tr></thead>\n<tbody>\n<tr><td><code>artefact_types</code></td><td>Defines the <em>kind</em> of artefact (Epic, Story, Defect, Strategic Goal). Owns the parent chain.</td></tr>\n<tr><td><code>artefact_type_fields</code></td><td>Per-type schema row — which fields apply to this kind, in what order, with what defaults.</td></tr>\n<tr><td><code>artefact_field_library</code></td><td>Field <em>definitions</em> — name, data type, config. Scoped global/tenant/workspace.</td></tr>\n<tr><td><code>artefact_field_values</code></td><td>The values themselves, keyed (artefact_id, field_id).</td></tr>\n<tr><td><code>artefact_workspace_fields</code></td><td>Whitelist for <code>scope='workspace'</code> entries — which workspaces may use this field.</td></tr>\n</tbody>\n</table>\n<p>Resolution algorithm at write time, given (workspace_id, artefact_type_id, field_name):</p>\n<ol>\n<li>Find the <code>artefact_field_library</code> row for <code>field_name</code>.</li>\n<li>If <code>scope = 'global'</code> → admit.</li>\n<li>If <code>scope = 'tenant'</code> → require <code>subscription_id</code> matches the workspace's tenant.</li>\n<li>If <code>scope = 'workspace'</code> → require a row in <code>artefact_workspace_fields (workspace_id, field_library_id)</code>.</li>\n<li>Otherwise reject with a 4xx, surface in audit log.</li>\n</ol>\n<h2 id=\"6-master-record-portfolio\">6. <code>master_record_portfolio</code></h2>\n<p>One row per workspace. Holds the <strong>portfolio model</strong> persistent record only — no workspace identity duplication. Workspace name, description, and owner remain in <code>mmff_vector.workspaces</code> until that table is itself sanitised in a later, deliberate cutover.</p>\n<table class=\"dui-table\">\n<thead><tr><th>Column</th><th>Type</th><th>Notes</th></tr></thead>\n<tbody>\n<tr><td><code>workspace_id</code></td><td><code>uuid PRIMARY KEY</code></td><td>cross-DB reference to <code>mmff_vector.workspaces.id</code></td></tr>\n<tr><td><code>model_id</code></td><td><code>uuid NULL</code></td><td>cross-DB reference to <code>mmff_library.portfolio_templates.id</code></td></tr>\n<tr><td><code>model_name</code></td><td><code>text NOT NULL</code></td><td>copied at adoption — survives library deletion</td></tr>\n<tr><td><code>model_description</code></td><td><code>text NULL</code></td><td>prose copied at adoption — replaces the live read from <code>mmff_library</code></td></tr>\n<tr><td><code>adopted_at</code></td><td><code>timestamptz NOT NULL DEFAULT now()</code></td><td>—</td></tr>\n<tr><td><code>adopted_by_user_id</code></td><td><code>uuid NULL</code></td><td>—</td></tr>\n<tr><td><code>created_at</code></td><td><code>timestamptz NOT NULL DEFAULT now()</code></td><td>—</td></tr>\n<tr><td><code>updated_at</code></td><td><code>timestamptz NOT NULL DEFAULT now()</code></td><td>maintained by trigger</td></tr>\n<tr><td><code>archived_at</code></td><td><code>timestamptz NULL</code></td><td>soft archive</td></tr>\n</tbody></table>\n<p>Trigger <code>trg_master_record_portfolio_touch_updated_at</code> mirrors the pattern from <code>master_record_tenant</code> (function <code>fn_master_record_portfolio_touch_updated_at</code> sets <code>NEW.updated_at = now()</code> on UPDATE).</p>\n<h2 id=\"7-adoption-saga\">7. Adoption Saga Rewrite</h2>\n<p>The current 7-step saga in <code>backend/internal/portfolio/adopt.go</code> writes to legacy <code>obj_*</code> tables in <code>mmff_vector</code>. Each step is rewired to <code>vector_artefacts</code>:</p>\n<table class=\"dui-table\">\n<thead><tr><th>Step</th><th>Old target (mmff_vector)</th><th>New target (vector_artefacts)</th></tr></thead>\n<tbody>\n<tr><td>1. validate</td><td>—</td><td>—</td></tr>\n<tr><td>2. layers</td><td><code>obj_strategy_types_layers</code></td><td><code>artefact_types</code> (scope=strategy, parent chain by <code>parent_type_id</code>)</td></tr>\n<tr><td>3. workflows</td><td><code>subscription_workflows</code></td><td><code>flows</code></td></tr>\n<tr><td>4. transitions</td><td><code>subscription_workflow_transitions</code></td><td><code>flow_transitions</code></td></tr>\n<tr><td>5. artifacts</td><td><code>subscription_artifacts</code></td><td><code>artefact_types</code> (scope=work)</td></tr>\n<tr><td>6. terminology</td><td><code>subscription_terminology</code></td><td>(deferred — open question 12.2)</td></tr>\n<tr><td>7. finalize</td><td>—</td><td>INSERT/UPDATE <code>master_record_portfolio</code></td></tr>\n</tbody></table>\n<p>Each step copies provenance from the library row into the new <code>artefact_types</code> row: <code>library_layer_id</code> from <code>portfolio_template_layer_definitions.id</code>, <code>library_layer_tag</code> from the matching tag column, <code>source = 'library'</code>. The model description prose is copied into <code>master_record_portfolio.model_description</code> at step 7 — never read live afterwards.</p>\n<p>The saga is wrapped in a single transaction per step in <code>vector_artefacts</code>. SSE progress events stream after each step commits. Idempotency keys remain at the saga level so a re-fired adoption is a no-op rather than a duplicate insert.</p>\n<h2 id=\"8-readoption-flow\">8. Re-adoption Flow</h2>\n<p>Re-adoption is the case where a workspace already has a model and switches to a different one. The hard requirement: <strong>existing work artefacts must survive</strong> with their parent relationships intact, even though the strategy chain changes underneath them.</p>\n<ol>\n<li>Identify all current <code>artefact_types</code> rows where <code>scope='strategy'</code> for the workspace.</li>\n<li>Insert a single <strong>placeholder</strong> strategy artefact at the bottom layer of the OLD chain (<code>source='placeholder'</code>, <code>name='Pending re-adoption'</code>, <code>library_layer_id=NULL</code>).</li>\n<li>UPDATE every existing work <code>artefacts</code> row whose <code>parent_artefact_id</code> pointed at the now-deprecated bottom strategy artefact → repoint at the placeholder.</li>\n<li>Delete old strategy <code>artefacts</code> rows (now zero references — the orphans were just re-parented).</li>\n<li>Delete old strategy <code>artefact_types</code> rows. <code>artefact_type_fields</code> CASCADEs.</li>\n<li>Insert the new strategy chain via the standard adoption saga (steps 2–4).</li>\n<li>UPDATE <code>master_record_portfolio</code> with the new model_id / model_name / model_description and stamp <code>adopted_at = now()</code>.</li>\n<li>Surface the placeholder in the UI as a visible &quot;needs re-classification&quot; bin so the workspace owner can manually move work items into the new model.</li>\n</ol>\n<p>The placeholder pattern keeps <code>parent_artefact_id NOT NULL</code> on work items — the schema invariant never breaks during re-adoption.</p>\n<h2 id=\"9-frontend-cutover\">9. Frontend Cutover</h2>\n<table class=\"dui-table\">\n<thead><tr><th>Surface</th><th>Old read</th><th>New read</th></tr></thead>\n<tbody>\n<tr><td>BundleView model description</td><td><code>GET /api/library/portfolio_templates/:id</code> (mmff_library)</td><td><code>GET /api/portfolio/master_record</code> (vector_artefacts.master_record_portfolio for current workspace)</td></tr>\n<tr><td>Adoption stream</td><td><code>GET /api/portfolio/adopt-stream/:workspaceId</code> (writes obj_*)</td><td>same endpoint, rewritten saga writes <code>vector_artefacts</code></td></tr>\n<tr><td>Adoption state</td><td><code>GET /api/portfolio/adoption-state/:workspaceId</code> (reads obj_strategy_types_layers)</td><td>same endpoint, rewritten to read <code>artefact_types</code> + <code>master_record_portfolio</code></td></tr>\n<tr><td>Layer list</td><td><code>GET /api/subscription/layers</code> (subscription-scoped)</td><td><code>GET /api/workspace/:id/portfolio/layers</code> (workspace-scoped)</td></tr>\n<tr><td>Field schema</td><td>direct DB read of <code>field_library</code></td><td><code>GET /api/workspace/:id/fields</code> with scope-resolution applied server-side</td></tr>\n</tbody></table>\n<p>No frontend component reads <code>mmff_library</code> directly after this cutover. The library DB becomes a build-time content source consumed by the adoption saga only — never a runtime dependency for tenant-facing pages.</p>\n<h2 id=\"10-sanitisation-register\">10. Sanitisation Register (mmff_vector)</h2>\n<p>Tracked in <a href=\"docs/c_c_v2_workitems_cutover_followups.md\"><code>docs/c_c_v2_workitems_cutover_followups.md</code></a>. Each row stays read-only or write-frozen until the trigger condition fires, then a follow-up migration drops it. Never deleted in the same PR that introduces the replacement.</p>\n<table class=\"dui-table\">\n<thead><tr><th>Legacy table (mmff_vector)</th><th>Trigger to drop</th><th>Status</th></tr></thead>\n<tbody>\n<tr><td><code>obj_strategy_types_layers</code></td><td>Last reader is rewritten in <code>adopt.go</code></td><td>open</td></tr>\n<tr><td><code>subscription_workflows</code></td><td>Adoption saga step 3 cuts over to <code>flows</code></td><td>open</td></tr>\n<tr><td><code>subscription_workflow_transitions</code></td><td>Adoption saga step 4 cuts over to <code>flow_transitions</code></td><td>open</td></tr>\n<tr><td><code>subscription_artifacts</code></td><td>Adoption saga step 5 cuts over to <code>artefact_types</code> (work)</td><td>open</td></tr>\n<tr><td><code>subscription_terminology</code></td><td>Terminology surface migrated (open question 12.2)</td><td>deferred</td></tr>\n<tr><td><code>strategy_layers_adopted</code></td><td>Provenance now lives on <code>artefact_types</code> columns</td><td>open</td></tr>\n<tr><td><code>obj_*</code> work-item residue</td><td>v2 work-items live on <code>vector_artefacts.artefacts</code> for ≥7 days</td><td>open</td></tr>\n<tr><td><code>workspaces</code> (mmff_vector)</td><td>Workspace identity moved to <code>vector_artefacts.master_record_workspace</code> in a future cutover</td><td>not started</td></tr>\n</tbody>\n</table>\n<h2 id=\"11-migration-order\">11. Migration Order</h2>\n<p>One migration per landing, in this order. Each runs through <code>backend/cmd/migrate</code> against <code>vector_artefacts</code>.</p>\n<ol>\n<li><code>NNN_artefact_field_library_rename.sql</code> — RENAME <code>field_library</code> → <code>artefact_field_library</code>; ALTER for <code>scope</code> column with backfill (<code>UPDATE … SET scope = CASE WHEN subscription_id IS NULL THEN 'global' ELSE 'tenant' END</code>); add CHECK constraints; ALTER <code>subscription_id</code> NULL.</li>\n<li><code>NNN_artefact_workspace_fields.sql</code> — CREATE <code>artefact_workspace_fields</code> with PK and index.</li>\n<li><code>NNN_artefact_types_workspace_scope.sql</code> — ADD <code>workspace_id</code>, <code>library_layer_id</code>, <code>library_layer_tag</code> columns to <code>artefact_types</code>; backfill <code>workspace_id</code> from prior tenant→default-workspace mapping; new indexes.</li>\n<li><code>NNN_master_record_portfolio.sql</code> — CREATE <code>master_record_portfolio</code> + trigger function + trigger.</li>\n<li><code>NNN_adoption_saga_rewrite.sql</code> — backfill <code>master_record_portfolio</code> rows for currently-adopted workspaces (read existing <code>obj_strategy_types_layers</code> + <code>subscription_artifacts</code> + library prose, INSERT one row per workspace).</li>\n<li><code>NNN_drop_strategy_layers_adopted.sql</code> — DROP <code>strategy_layers_adopted</code> after the <code>adopt.go</code> rewrite has been deployed for ≥1 day.</li>\n<li>Sanitisation drops (one migration per legacy table, only after each replacement is verified live for ≥7 days).</li>\n</ol>\n<h2 id=\"12-risks-gaps\">12. Risks &amp; Gaps</h2>\n<h3>12.1 Cross-DB integrity</h3>\n<p><code>workspace_id</code> in <code>vector_artefacts</code> tables has no enforced FK to <code>mmff_vector.workspaces</code>. A workspace deletion in mmff_vector would orphan rows in <code>vector_artefacts</code>. Mitigation: workspace deletion is already a soft-archive operation app-side; add a deletion-policy hook that scans <code>vector_artefacts</code> for orphans on archive and rejects if any remain. Deferred to a follow-up story.</p>\n<h3>12.2 Terminology table</h3>\n<p>Adoption saga step 6 (terminology) has no obvious target in <code>vector_artefacts</code>. Open design: either add <code>artefact_terminology</code> (workspace-scoped key/value table) or fold terminology into <code>artefact_types.name</code> + a per-type display-overrides JSONB. Deferred to a brainstorm before storification.</p>\n<h3>12.3 Re-adoption placeholder UX</h3>\n<p>The placeholder strategy artefact will appear in the UI as a re-classification bucket. UX flow for moving items out of the placeholder is not designed yet — current scope only guarantees data survival, not a smooth user experience around re-classification. Deferred to a frontend story under PLA-0026.</p>\n<h3>12.4 Library DB future role</h3>\n<p>After this cutover, <code>mmff_library</code> is consumed only by the adoption saga at adoption time. Whether the library remains co-located with the backend or migrates to a separate read-only catalogue service is an open question — out of scope for this design.</p>\n<h3>12.5 Backfill correctness</h3>\n<p>The migration step 5 backfill (one <code>master_record_portfolio</code> row per currently-adopted workspace) needs a parity test: count of adopted workspaces in legacy === count of <code>master_record_portfolio</code> rows post-migration. Story owner adds the parity assertion as part of the migration story.</p>\n<h3>12.6 Field-library scope backfill assumption</h3>\n<p>The migration assumes existing <code>field_library</code> rows with <code>subscription_id IS NULL</code> are global. If any rows were created with a NULL <code>subscription_id</code> for a different reason (e.g. a tenant-scoped field with a bug), they will be silently miscategorised as global. Mitigation: pre-migration audit query asserts there are no &quot;orphan&quot; NULL rows that map to a known tenant via another path.</p>\n<h2 id=\"13-story-decomposition\">13. Story Decomposition</h2>\n<p>Per the project's stories rule (hard): every observable layer carries its own card before <code>/stories</code> is invoked. PLA number to be allocated at storification time. Ordering across layers is loose — migrations land first within their stack, then the backend service that depends on each table, then the handler, then the frontend that calls the handler, with tests landing alongside their target.</p>\n<p>Risk legend: <strong>S1</strong> = data-loss risk (migrations, backfills, saga rewrites), <strong>S2</strong> = behavioural regression risk (handler/UI cutovers), <strong>S3</strong> = mechanical (UI URL swaps, additive table creates).</p>\n<h3>13.1 Migration layer (database)</h3>\n<table class=\"dui-table\">\n<thead><tr><th>#</th><th>Story</th><th>Risk</th><th>Notes</th></tr></thead>\n<tbody>\n<tr><td>M1</td><td>Pre-migration audit: assert zero <code>field_library</code> rows with <code>subscription_id IS NULL</code> that map to a known tenant via any other path</td><td>S1</td><td>Gate for M2 — fix any miscategorised rows manually before migrating</td></tr>\n<tr><td>M2</td><td>Rename <code>field_library</code> → <code>artefact_field_library</code>; add <code>scope</code> column with backfill (<code>NULL → 'global'</code>, else <code>'tenant'</code>); make <code>subscription_id</code> nullable; add CHECK constraints</td><td>S1</td><td>Includes DOWN migration; touches every reader call-site rename</td></tr>\n<tr><td>M3</td><td>Create <code>artefact_workspace_fields</code> (PK, FK to <code>artefact_field_library</code>, index)</td><td>S3</td><td>Additive only</td></tr>\n<tr><td>M4</td><td>Extend <code>artefact_types</code> with <code>workspace_id NOT NULL</code>, <code>library_layer_id NULL</code>, <code>library_layer_tag NULL</code>; backfill <code>workspace_id</code> from prior tenant→default-workspace mapping; add (workspace_id, scope, sort_order) index + (workspace_id, scope, prefix) unique index</td><td>S1</td><td>Backfill must be deterministic and parity-tested before NOT NULL is enforced</td></tr>\n<tr><td>M5</td><td>Create <code>master_record_portfolio</code> table + <code>fn_master_record_portfolio_touch_updated_at</code> + trigger</td><td>S3</td><td>Additive — mirrors <code>master_record_tenant</code> pattern</td></tr>\n<tr><td>M6</td><td>Backfill <code>master_record_portfolio</code> for currently-adopted workspaces (read legacy <code>obj_strategy_types_layers</code> + <code>subscription_artifacts</code> + library prose, INSERT one row per workspace)</td><td>S1</td><td>Includes parity assertion (count match) — see T1</td></tr>\n<tr><td>M7</td><td>Drop <code>strategy_layers_adopted</code> (gated on B5 deployed ≥1 day)</td><td>S2</td><td>Follow-up PR — never land in same PR as B5</td></tr>\n</tbody>\n</table>\n<h3>13.2 Backend layer (Go)</h3>\n<table class=\"dui-table\">\n<thead><tr><th>#</th><th>Story</th><th>Risk</th><th>Notes</th></tr></thead>\n<tbody>\n<tr><td>B1</td><td><code>portfolio.MasterRecordService</code> — CRUD on <code>master_record_portfolio</code> (sole writer boundary, mirrors <code>internal/tenant.Service</code>)</td><td>S2</td><td>Depends on M5</td></tr>\n<tr><td>B2</td><td>Field scope resolver — server-side <code>(workspace_id, field_id) → admit/deny</code> against <code>artefact_field_library.scope</code> + <code>artefact_workspace_fields</code></td><td>S2</td><td>Depends on M2 + M3</td></tr>\n<tr><td>B3</td><td>Adoption saga step 2 (layers) rewrite: write <code>artefact_types</code> scope=strategy with <code>library_layer_id</code> + <code>library_layer_tag</code> provenance</td><td>S1</td><td>Depends on M4; carries idempotency key</td></tr>\n<tr><td>B4</td><td>Adoption saga step 3+4 (workflows + transitions) rewrite: write <code>flows</code> + <code>flow_transitions</code></td><td>S1</td><td>Within same saga transaction</td></tr>\n<tr><td>B5</td><td>Adoption saga step 5 (artifacts) rewrite: write <code>artefact_types</code> scope=work with parent chain</td><td>S1</td><td>Depends on M4</td></tr>\n<tr><td>B6</td><td>Adoption saga step 7 (finalize): INSERT/UPDATE <code>master_record_portfolio</code> with copied <code>model_id</code>, <code>model_name</code>, <code>model_description</code> prose</td><td>S1</td><td>Depends on B1; the prose copy replaces the live library read</td></tr>\n<tr><td>B7</td><td>Adoption saga: SSE progress events refitted for new step targets; idempotency keys preserved</td><td>S2</td><td>Cross-cuts B3–B6</td></tr>\n<tr><td>B8</td><td>Re-adoption flow: insert placeholder strategy artefact at OLD bottom layer, repoint orphaned work <code>artefacts.parent_artefact_id</code>, delete old strategy rows, run new adoption</td><td>S1</td><td>The hard requirement: <code>parent_artefact_id NOT NULL</code> never breaks during re-adoption</td></tr>\n<tr><td>B9</td><td>Handler <code>GET /api/portfolio/master_record</code> (workspace-scoped read of <code>master_record_portfolio</code>)</td><td>S3</td><td>Depends on B1</td></tr>\n<tr><td>B10</td><td>Handler <code>GET /api/workspace/:id/portfolio/layers</code> — replaces <code>/api/subscription/layers</code>; reads <code>artefact_types</code> scope=strategy for the workspace</td><td>S2</td><td>Old endpoint stays live until F3 lands</td></tr>\n<tr><td>B11</td><td>Handler <code>GET /api/workspace/:id/fields</code> — invokes B2 scope resolver server-side</td><td>S2</td><td>Depends on B2</td></tr>\n<tr><td>B12</td><td>Handler <code>GET /api/portfolio/adoption-state/:workspaceId</code> — rewrite to read <code>artefact_types</code> + <code>master_record_portfolio</code> instead of <code>obj_strategy_types_layers</code></td><td>S2</td><td>Currently 404s post-reset (per memory) — fix in this story</td></tr>\n<tr><td>B13</td><td>Workspace deletion hook: scan <code>vector_artefacts</code> for orphans on archive, reject if any remain (deferred per 12.1)</td><td>S2</td><td>Deferred follow-up — file but do not block on it</td></tr>\n</tbody>\n</table>\n<h3>13.3 Frontend layer (Next.js / React)</h3>\n<table class=\"dui-table\">\n<thead><tr><th>#</th><th>Story</th><th>Risk</th><th>Notes</th></tr></thead>\n<tbody>\n<tr><td>F1</td><td>BundleView: switch model description read from <code>/api/library/portfolio_templates/:id</code> → <code>/api/portfolio/master_record</code></td><td>S2</td><td>Depends on B9; removes the runtime <code>mmff_library</code> dependency</td></tr>\n<tr><td>F2</td><td>Adoption stream: verify SSE event handling renders correctly against rewritten saga; no code change expected, but assertion test required</td><td>S3</td><td>Depends on B7</td></tr>\n<tr><td>F3</td><td>Layer list: swap <code>/api/subscription/layers</code> → <code>/api/workspace/:id/portfolio/layers</code> across all callers (and any default-fallback paths)</td><td>S2</td><td>Depends on B10; grep call-sites and update together</td></tr>\n<tr><td>F4</td><td>Field schema consumer: switch to <code>/api/workspace/:id/fields</code>; remove direct DB reads of <code>field_library</code> from any frontend code</td><td>S2</td><td>Depends on B11</td></tr>\n<tr><td>F5</td><td>Re-adoption UX: surface the placeholder strategy artefact as a visible &quot;Pending re-classification&quot; bucket with a move-out flow (deferred per 12.3)</td><td>S2</td><td>Deferred follow-up — file under PLA-0026 but do not block landing</td></tr>\n</tbody>\n</table>\n<h3>13.4 Test layer</h3>\n<table class=\"dui-table\">\n<thead><tr><th>#</th><th>Story</th><th>Risk</th><th>Notes</th></tr></thead>\n<tbody>\n<tr><td>T1</td><td>Migration parity test: count of legacy adopted workspaces === count of <code>master_record_portfolio</code> rows post-M6</td><td>S1</td><td>Lands inside M6 — gate for promotion</td></tr>\n<tr><td>T2</td><td>Pre-migration audit assertion: zero NULL <code>subscription_id</code> orphans</td><td>S1</td><td>Lands inside M1</td></tr>\n<tr><td>T3</td><td>Integration test: adoption saga end-to-end writes only to <code>vector_artefacts</code> (assert zero writes to <code>obj_*</code>)</td><td>S1</td><td>Cross-cuts B3–B7</td></tr>\n<tr><td>T4</td><td>Integration test: re-adoption flow — orphan work items repointed, placeholder visible, no <code>parent_artefact_id</code> NULL, no FK violation</td><td>S1</td><td>Depends on B8</td></tr>\n<tr><td>T5</td><td>Integration test: scope resolver admit/deny matrix — (global, tenant-match, tenant-mismatch, workspace-whitelisted, workspace-not-whitelisted) × (read, write)</td><td>S2</td><td>Depends on B2</td></tr>\n<tr><td>T6</td><td>Canary test: cross-DB <code>workspace_id</code> integrity — every <code>vector_artefacts</code> row's <code>workspace_id</code> exists in <code>mmff_vector.workspaces</code></td><td>S2</td><td>Schedules nightly</td></tr>\n<tr><td>T7</td><td>Lint rule <code>lint:portfolio-library-read</code> — fails on any frontend file importing <code>/api/library/</code> URLs in tenant code paths</td><td>S2</td><td>Lands with F1</td></tr>\n</tbody>\n</table>\n<h3>13.5 Sanitisation follow-ups (legacy mmff_vector)</h3>\n<p>One PR per legacy table, only after each replacement is verified live for ≥7 days. Tracked in <a href=\"docs/c_c_v2_workitems_cutover_followups.md\"><code>docs/c_c_v2_workitems_cutover_followups.md</code></a>. Filed as cards but not blocking the main cutover.</p>\n<table class=\"dui-table\">\n<thead><tr><th>#</th><th>Drop</th><th>Trigger</th></tr></thead>\n<tbody>\n<tr><td>S1</td><td><code>obj_strategy_types_layers</code></td><td>B3 deployed ≥7 days, zero reads observed</td></tr>\n<tr><td>S2</td><td><code>subscription_workflows</code> + <code>subscription_workflow_transitions</code></td><td>B4 deployed ≥7 days</td></tr>\n<tr><td>S3</td><td><code>subscription_artifacts</code></td><td>B5 deployed ≥7 days</td></tr>\n<tr><td>S4</td><td><code>strategy_layers_adopted</code></td><td>B5 deployed ≥1 day (M7)</td></tr>\n<tr><td>S5</td><td><code>subscription_terminology</code></td><td>Deferred until terminology design lands (12.2)</td></tr>\n</tbody>\n</table>\n<h3>13.6 Out-of-scope / brainstorm-first</h3>\n<ul>\n<li><strong>Terminology table design</strong> (12.2) — needs its own brainstorm before storification; either <code>artefact_terminology</code> table or <code>artefact_types</code> JSONB overrides</li>\n<li><strong>Library DB future role</strong> (12.4) — separate decision, out of this PLA</li>\n<li><strong>Workspace identity migration</strong> — moving <code>mmff_vector.workspaces</code> → <code>vector_artefacts.master_record_workspace</code> is a separate cutover, gated on this one landing first</li>\n</ul>\n<h3>13.7 Tally</h3>\n<table class=\"dui-table\">\n<thead><tr><th>Layer</th><th>Stories</th><th>S1</th><th>S2</th><th>S3</th></tr></thead>\n<tbody>\n<tr><td>Migration</td><td>7</td><td>5</td><td>1</td><td>2</td></tr>\n<tr><td>Backend</td><td>13</td><td>5</td><td>7</td><td>1</td></tr>\n<tr><td>Frontend</td><td>5</td><td>0</td><td>4</td><td>1</td></tr>\n<tr><td>Tests</td><td>7</td><td>3</td><td>4</td><td>0</td></tr>\n<tr><td>Sanitisation (deferred)</td><td>5</td><td>0</td><td>5</td><td>0</td></tr>\n<tr><td><strong>Total</strong></td><td><strong>37</strong></td><td><strong>13</strong></td><td><strong>21</strong></td><td><strong>4</strong></td></tr>\n</tbody>\n</table>\n<h3>13.8 Card Tracking (living working memory)</h3>\n<p>Marks each story's state through the implementation. To mark done, wrap the row's <code>&lt;td&gt;</code> values in <code>&lt;s&gt;…&lt;/s&gt;</code> and set status to <code>done</code>. Card URLs target the local Planka instance.</p>\n<table class=\"dui-table\">\n<thead><tr><th>Tag</th><th>Story ID</th><th>Title</th><th>Card</th><th>Status</th></tr></thead>\n<tbody>\n<tr id=\"track-M1\"><td><s>M1</s></td><td><s>00476</s></td><td><s>Pre-migration audit of artefact_field_library NULL subscription_id rows</s></td><td><a href=\"http://localhost:3333/cards/1769516989446882318\">card</a></td><td><strong>done</strong></td></tr>\n<tr id=\"track-M2\"><td>M2</td><td>00477</td><td>Rename field_library to artefact_field_library and add scope discriminator</td><td><a href=\"http://localhost:3333/cards/1769516994043839510\">card</a></td><td>todo</td></tr>\n<tr id=\"track-M3\"><td>M3</td><td>00478</td><td>Create artefact_workspace_fields whitelist join table</td><td><a href=\"http://localhost:3333/cards/1769516998498190366\">card</a></td><td>todo</td></tr>\n<tr id=\"track-M4\"><td>M4</td><td>00479</td><td>Extend artefact_types with workspace_id, library_layer_id, library_layer_tag</td><td><a href=\"http://localhost:3333/cards/1769517003002872870\">card</a></td><td>todo</td></tr>\n<tr id=\"track-M5\"><td>M5</td><td>00480</td><td>Create master_record_portfolio table with touch trigger</td><td><a href=\"http://localhost:3333/cards/1769517007331394606\">card</a></td><td>todo</td></tr>\n<tr id=\"track-M6\"><td>M6</td><td>00481</td><td>Backfill master_record_portfolio for currently-adopted workspaces</td><td><a href=\"http://localhost:3333/cards/1769517011559253046\">card</a></td><td>todo</td></tr>\n<tr id=\"track-M7\"><td>M7</td><td>00482</td><td>Drop strategy_layers_adopted after B5 deployed >=1 day</td><td><a href=\"http://localhost:3333/cards/1769517016156210238\">card</a></td><td>todo</td></tr>\n<tr id=\"track-B1\"><td>B1</td><td>00490</td><td>portfolio.MasterRecordService — CRUD on master_record_portfolio</td><td><a href=\"http://localhost:3333/cards/1769517051581301886\">card</a></td><td>todo</td></tr>\n<tr id=\"track-B2\"><td>B2</td><td>00491</td><td>Field scope resolver — admit/deny against scope + workspace whitelist</td><td><a href=\"http://localhost:3333/cards/1769517055859491974\">card</a></td><td>todo</td></tr>\n<tr id=\"track-B3\"><td>B3</td><td>00492</td><td>Adoption saga step 2 rewrite: write artefact_types scope=strategy</td><td><a href=\"http://localhost:3333/cards/1769517060146070670\">card</a></td><td>todo</td></tr>\n<tr id=\"track-B4\"><td>B4</td><td>00493</td><td>Adoption saga steps 3+4 rewrite: write flows + flow_transitions</td><td><a href=\"http://localhost:3333/cards/1769517064399094934\">card</a></td><td>todo</td></tr>\n<tr id=\"track-B5\"><td>B5</td><td>00494</td><td>Adoption saga step 5 rewrite: write artefact_types scope=work with parent chain</td><td><a href=\"http://localhost:3333/cards/1769517068626953374\">card</a></td><td>todo</td></tr>\n<tr id=\"track-B6\"><td>B6</td><td>00495</td><td>Adoption saga step 7 finalize: upsert master_record_portfolio</td><td><a href=\"http://localhost:3333/cards/1769517072921920678\">card</a></td><td>todo</td></tr>\n<tr id=\"track-B7\"><td>B7</td><td>00496</td><td>Adoption saga SSE progress events refitted for new step targets</td><td><a href=\"http://localhost:3333/cards/1769517077292385454\">card</a></td><td>todo</td></tr>\n<tr id=\"track-B8\"><td>B8</td><td>00497</td><td>Re-adoption flow: insert placeholder strategy artefact and repoint orphaned work artefacts</td><td><a href=\"http://localhost:3333/cards/1769517081503466678\">card</a></td><td>todo</td></tr>\n<tr id=\"track-B9\"><td>B9</td><td>00498</td><td>Handler GET /api/portfolio/master_record</td><td><a href=\"http://localhost:3333/cards/1769517085941040318\">card</a></td><td>todo</td></tr>\n<tr id=\"track-B10\"><td>B10</td><td>00499</td><td>Handler GET /api/workspace/:id/portfolio/layers</td><td><a href=\"http://localhost:3333/cards/1769517090303116486\">card</a></td><td>todo</td></tr>\n<tr id=\"track-B11\"><td>B11</td><td>00500</td><td>Handler GET /api/workspace/:id/fields invoking scope resolver</td><td><a href=\"http://localhost:3333/cards/1769517094522586318\">card</a></td><td>todo</td></tr>\n<tr id=\"track-B12\"><td>B12</td><td>00501</td><td>Handler GET /api/portfolio/adoption-state/:workspaceId rewrite</td><td><a href=\"http://localhost:3333/cards/1769517098817553622\">card</a></td><td>todo</td></tr>\n<tr id=\"track-B13\"><td>B13</td><td>00502</td><td>Workspace deletion hook: scan vector_artefacts for orphans and reject</td><td><a href=\"http://localhost:3333/cards/1769517103154463966\">card</a></td><td>todo</td></tr>\n<tr id=\"track-F1\"><td>F1</td><td>00507</td><td>BundleView: switch model description read to /api/portfolio/master_record</td><td><a href=\"http://localhost:3333/cards/1769517125468161286\">card</a></td><td>todo</td></tr>\n<tr id=\"track-F2\"><td>F2</td><td>00508</td><td>Adoption stream: assertion test for SSE event handling against rewritten saga</td><td><a href=\"http://localhost:3333/cards/1769517129746351374\">card</a></td><td>todo</td></tr>\n<tr id=\"track-F3\"><td>F3</td><td>00509</td><td>Layer list: swap /api/subscription/layers to /api/workspace/:id/portfolio/layers</td><td><a href=\"http://localhost:3333/cards/1769517134175536406\">card</a></td><td>todo</td></tr>\n<tr id=\"track-F4\"><td>F4</td><td>00510</td><td>Field schema consumer: switch to /api/workspace/:id/fields</td><td><a href=\"http://localhost:3333/cards/1769517138487280926\">card</a></td><td>todo</td></tr>\n<tr id=\"track-F5\"><td>F5</td><td>00511</td><td>Re-adoption UX: surface placeholder strategy artefact as Pending re-classification bucket</td><td><a href=\"http://localhost:3333/cards/1769517142832579878\">card</a></td><td>todo</td></tr>\n<tr id=\"track-T1\"><td>T1</td><td>00483</td><td>Migration parity test: adopted workspace count matches master_record_portfolio rowcount</td><td><a href=\"http://localhost:3333/cards/1769517020518286406\">card</a></td><td>todo</td></tr>\n<tr id=\"track-T2\"><td>T2</td><td>00484</td><td>Pre-migration audit assertion: zero NULL subscription_id orphans</td><td><a href=\"http://localhost:3333/cards/1769517024981025870\">card</a></td><td>todo</td></tr>\n<tr id=\"track-T3\"><td>T3</td><td>00503</td><td>Integration test: adoption saga writes only to vector_artefacts</td><td><a href=\"http://localhost:3333/cards/1769517108607059174\">card</a></td><td>todo</td></tr>\n<tr id=\"track-T4\"><td>T4</td><td>00504</td><td>Integration test: re-adoption preserves parent_artefact_id NOT NULL</td><td><a href=\"http://localhost:3333/cards/1769517112969135342\">card</a></td><td>todo</td></tr>\n<tr id=\"track-T5\"><td>T5</td><td>00505</td><td>Integration test: scope resolver admit/deny matrix</td><td><a href=\"http://localhost:3333/cards/1769517117440263414\">card</a></td><td>todo</td></tr>\n<tr id=\"track-T6\"><td>T6</td><td>00506</td><td>Canary nightly test: cross-DB workspace_id integrity</td><td><a href=\"http://localhost:3333/cards/1769517121651344638\">card</a></td><td>todo</td></tr>\n<tr id=\"track-T7\"><td>T7</td><td>00512</td><td>Lint rule lint:portfolio-library-read</td><td><a href=\"http://localhost:3333/cards/1769517147186267438\">card</a></td><td>todo</td></tr>\n<tr id=\"track-S1\"><td>S1</td><td>00485</td><td>Drop obj_strategy_types_layers after B3 deployed 7 days zero reads</td><td><a href=\"http://localhost:3333/cards/1769517030081299542\">card</a></td><td>todo</td></tr>\n<tr id=\"track-S2\"><td>S2</td><td>00486</td><td>Drop subscription_workflows and subscription_workflow_transitions after B4 7 days</td><td><a href=\"http://localhost:3333/cards/1769517034426598494\">card</a></td><td>todo</td></tr>\n<tr id=\"track-S3\"><td>S3</td><td>00487</td><td>Drop subscription_artifacts after B5 deployed 7 days</td><td><a href=\"http://localhost:3333/cards/1769517038830617702\">card</a></td><td>todo</td></tr>\n<tr id=\"track-S4\"><td>S4</td><td>00488</td><td>Drop strategy_layers_adopted register row after M7 lands</td><td><a href=\"http://localhost:3333/cards/1769517042974590062\">card</a></td><td>todo</td></tr>\n<tr id=\"track-S5\"><td>S5</td><td>00489</td><td>Drop subscription_terminology after terminology design lands</td><td><a href=\"http://localhost:3333/cards/1769517047252780150\">card</a></td><td>todo</td></tr>\n</tbody>\n</table>\n\n  </div>\n</div>"
}