{
  "note": "Cross-DB writer packages awaiting a *crossdb_test.go documenting the partial-failure boundary. The lint was tightened 2026-06-05 in two ways: (1) it requires a `.Exec(` write before flagging — pure cross-DB READERS (erd, workspaceresolver) now pass on their own; (2) it counts pool fields PER STRUCT, not per package — so a package with two unrelated single-pool structs (auth's Service.Pool + JTICache.pool; mentions after the scopeAware-flag refactor; nav) is no longer a phantom cross-DB writer and passes on its own. Those packages were removed from this ledger (fixed at source, not exempted). The entries below are genuine: each has ONE struct holding >1 *pgxpool.Pool spanning vector_artefacts + mmff_library. RF1.5.1-.5 wrote real doc-only stub tests for the 5 high-risk cross-DB writers (portfoliomodels, artefactitems, libraryreleases, errorsreport, + the portfolio adopt path). The remaining entries get their own crossdb stub as each is reviewed; end state is an empty array.",
  "exempt_paths": [
    "backend/internal/fields",
    "backend/internal/flows",
    "backend/internal/librarydb",
    "backend/internal/portfolio",
    "backend/internal/topology",
    "backend/internal/sentinel"
  ]
}
