{
  "$comment": [
    "TD-NAME-001 / RF1.4.4 pay-down ledger. Every package whose sql.go",
    "still uses bare column names against a renamed (§2.6 root family)",
    "table is listed here. As each domain's column-rename migration",
    "ships, the package is REMOVED from this list — that's how the",
    "ledger shrinks. End-state: empty array → lint becomes fail-on-",
    "violation in lint_column_prefix_convention.py and warn-only mode",
    "is retired.",
    "",
    "Lint: dev/scripts/lint_column_prefix_convention.py",
    "Reference: docs/c_tech_debt.md TD-NAME-001 + docs/c_c_naming_conventions.md §2.3",
    "Baseline captured: 2026-05-14 (245 findings, 9 packages, post RF1.4.2 sweep).",
    "Per-package counts at baseline (sanity for next run):",
    "  artefactitemsv2:  6",
    "  auth:            15  →  9 after mig 186 (users_password_resets done)",
    "  flows:           26",
    "  nav:             87",
    "  portfoliomodels: 74",
    "  roles:           17",
    "  tenantmasterrecord: 1",
    "  users:            5  →  1 after mig 186 (users_password_resets done; users_sessions still pending)",
    "  workspaces:      14",
    "",
    "Pay-down log (newest first):",
    "  2026-05-14 — mig 189 — RBAC triangle column-prefixed (users_roles 12 cols + users_permissions 6 cols + users_roles_permissions 4 cols + ~12 idx/constraint + dedicated trigger fn). §2.4 multi-FK role suffix applied (created_by / granted_by). roles + permissions + auth + users all rewritten. 191→174 (-17). roles package OFF ledger. 4 → 3.",
    "  2026-05-14 — mig 188 — users_roles_workspaces fully prefixed (12 cols + 3 idx + 5 FK + 2 check + dedicated trigger fn; workspaces/sql.go + topology/sql.go + portfoliomodels/sql.go). §2.4 multi-FK role suffix applied (granted_by / revoked_by). 205→191 (-14). workspaces package OFF ledger. 5 → 4.",
    "  2026-05-14 — mig 064 (VA) — artefacts_fields_values fully prefixed (10 cols + 5 idx + 3 constraint + trigger). Bundled with Go package rename artefactitemsv2 → artefactitems (v-suffix dropped per §1.1.2 since v1 obj_* substrate is fully retired). lint sample: 211→205 (-6). artefactitems package OFF ledger. 6 → 5.",
    "  2026-05-14 — mig 187 — users_sessions fully prefixed (11 cols + 6 index + 1 FK; auth/sql.go + users/sql.go rewrites) → 222→211 total (-11). users + auth packages BOTH OFF ledger. 8 packages → 6.",
    "  2026-05-14 — mig 063 (VA) — master_record_tenants fully prefixed (17 cols + tenantmasterrecord/service.go + portfoliomodels/sql.go upsert) → 236→222 total (-14). tenantmasterrecord package OFF ledger. portfoliomodels: 74→61.",
    "  2026-05-14 — mig 186 — users_password_resets fully prefixed → 245→236 total (-9). users + auth packages stay on ledger (users_sessions still bare in both)."
  ],
  "exempt_packages": [
    "flows",
    "nav",
    "portfoliomodels"
  ]
}
