"use client";

// The layout handles the redirect to the first type tab once flows load.
// This page is only rendered briefly while that redirect fires.
export default function FlowStatesRoot() {
  return null;
}
